const ACTS=[

{n:6,t:"암호 공격 (crypto)",s:"약한 암호·잘못된 구현을 깨뜨려 비밀을 되찾는다. 수학이 무기가 되는 곳.",days:[

{d:111,t:"crypto란 — 깨는 것이 아니라 실수를 찾는 것",g:"CTF 암호 분류의 그림과, '암호를 깬다'의 진짜 의미를 잡는다.",
c:"<p>CTF의 <b>crypto**분류는 암호를 다룹니다. 하지만 오해하지 마세요 — <b>AES 같은 튼튼한 암호를 정면으로 깨는 게 아닙니다**. 대부분 <b>약한 암호·잘못된 사용·구현 실수**를 찾아 뚫습니다.</p><p><b>'암호를 깬다'의 진짜 의미**<br>· <b>약한 알고리즘**: 옛 암호(Caesar·Vigenère)·약한 해시(MD5).<br>· <b>잘못된 사용**: 강한 알고리즘도 <b>잘못 쓰면**깨집니다 — 같은 키/논스 재사용, 약한 파라미터, 패딩 오류.<br>· <b>구현 실수**: 타이밍 유출, 오류 메시지 유출(패딩 오라클).<br>· <b>수학적 약점**: RSA의 작은 소인수·공통 모듈러스.<br><b>정면돌파(brute force)는 거의 안 통합니다**. 실수를 찾는 게 crypto입니다.</p><p><b>기초 개념**<br>· <b>대칭키**: 같은 키로 암·복호(AES·DES). 빠름. 키 공유가 문제.<br>· <b>비대칭키(공개키)**: 공개키로 암호화, 개인키로 복호(RSA·ECC). 키 공유 해결.<br>· <b>해시**: 단방향(SHA·MD5). 복호 불가, 무결성·비밀번호 저장.<br>· <b>인코딩 ≠ 암호화**: Base64·hex는 <b>암호가 아닙니다**(키 없음). 그냥 표현 변환.</p><p><b>필요한 것**<br>· <b>기초 수학**: 모듈러 연산(<code>mod</code>), 소수, 최대공약수. 겁먹지 마세요 — 필요한 만큼만.<br>· <b>파이썬**: <code>pycryptodome</code>(암호 라이브러리), <code>gmpy2</code>(큰 수), <b>sage**(수학 CAS), <b>z3**(제약).<br>· <b>도구**: CyberChef(인코딩·고전암호), RsaCtfTool(RSA 자동 공격), hashcat(해시 크랙), factordb(소인수).</p><p><b>접근 순서**<br>① <b>식별**: 무슨 암호·인코딩인가(형식·길이·문자 집합).<br>② <b>약점 탐색**: 알려진 취약점·잘못된 파라미터가 있나.<br>③ <b>공격 적용**: 맞는 기법·도구.</p><p><b>방어와의 연결**<br>암호 공격을 알면 <b>왜 그 방어 규칙들이 있는지**압니다 — 왜 MD5를 쓰면 안 되고, 왜 논스를 재사용하면 안 되고, 왜 '직접 암호를 구현하지 말라'고 하는지. 방어 코스의 해시·암호화 권고가 여기서 이해됩니다.</p><p><b>마음가짐:</b> crypto는 <b>수학적이지만 퍼즐**입니다. '이 암호의 실수는 무엇인가'를 찾는 탐정 놀이죠. 라이브러리·도구가 계산을 대신하니, 핵심은 <b>어떤 약점인지 알아보는 눈**입니다. 이번 ACT가 그 눈을 길러줍니다.</p>",
term:"# crypto의 3단계: 식별 -> 약점 -> 공격\n$ echo 'U0ZTRXt...' | base64 -d      # 인코딩? (암호 아님)\n# RSA면 파라미터 확인:\n#   n이 factordb에 있나? e가 3인가? 같은 n 재사용?\n# 해시면: 길이로 식별 -> hashcat / 온라인 조회\n# 정면돌파(brute)는 거의 안 통한다 -> 실수를 찾아라",
lab:["암호를 '깬다'가 실수를 찾는 것임을 이해한다","대칭키·비대칭키·해시의 차이를 정리한다","인코딩(Base64)과 암호화의 차이를 명확히 한다","crypto 접근 3단계(식별→약점→공격)를 외운다","pycryptodome·gmpy2·sage 등 도구를 파악한다","모듈러 연산·소수의 기초를 복습한다","암호 공격이 방어 권고와 어떻게 이어지는지 적는다"],
q:[{t:"CTF crypto에서 '암호를 깬다'는 것의 실제 의미는?",o:["AES를 정면으로 무차별 대입","약한 알고리즘·잘못된 사용(키/논스 재사용)·구현 실수·수학적 약점을 찾아 뚫는 것","슈퍼컴퓨터 사용","양자컴퓨터"],a:1,
  e:"강한 암호를 정면돌파하는 게 아니라, <b>실수를 찾습니다**. 약한 알고리즘·잘못된 파라미터·논스 재사용·구현 결함 — 이런 약점이 crypto의 표적입니다. brute force는 거의 안 통합니다."},
   {t:"Base64·hex 인코딩과 암호화의 결정적 차이는?",o:["없음","인코딩은 키가 없는 표현 변환(누구나 되돌림)이고, 암호화는 키가 있어야 복호할 수 있음","인코딩이 더 안전","암호화가 더 빠름"],a:1,
    e:"<b>인코딩은 암호가 아닙니다**. Base64·hex는 <b>키 없이 누구나 되돌립니다** — 그냥 표현 변환입니다. 암호화는 <b>키**가 있어야 복호됩니다. '인코딩됐으니 안전'은 착각입니다."},
   {t:"강한 알고리즘(AES 등)도 crypto 문제에서 깨지는 흔한 이유는?",o:["알고리즘이 약해서","같은 키/논스 재사용, 약한 파라미터, 패딩 오라클 등 '잘못된 사용·구현'이 취약점을 만들기 때문","오래돼서","키가 짧아서"],a:1,
    e:"AES 자체는 튼튼해도 <b>잘못 쓰면 깨집니다**. 논스 재사용(스트림 암호 치명적), 패딩 오라클, 약한 모드 — <b>사용·구현의 실수**가 표적입니다. '직접 암호 구현 금지'의 이유죠."}]},

{d:112,t:"고전 암호 — 치환과 전치",g:"Caesar·Vigenère 등 고전 암호를 식별하고 자동으로 푼다.",
c:"<p>CTF 입문 crypto는 <b>고전 암호**입니다. 현대 암호 전, 손으로 만들던 것들 — 패턴을 알면 쉽게 풀립니다.</p><p><b>치환 암호(Substitution)**<br><b>Caesar(시저)**: 알파벳을 <b>일정 칸 이동**. <code>A→D</code>(shift 3). 경우의 수 25개 → <b>전수 시도(brute)**로 즉시. ROT13은 shift 13.<br><b>단일 치환**: 각 글자를 다른 글자로 임의 매핑. <b>빈도 분석**으로 풀음(영어에서 e·t가 많음). quipqiup 같은 자동 도구.<br><b>Atbash**: 알파벳 뒤집기(<code>A↔Z</code>).</p><p><b>Vigenère(비제네르)**<br>키워드로 <b>글자마다 다른 shift**. 오래 안 깨졌던 다표식 암호.<br><b>공격**: 키 길이를 찾고(Kasiski·자기상관), 각 위치를 Caesar처럼 빈도 분석. 온라인 도구·<code>dcode.fr</code>가 자동으로.</p><p><b>전치 암호(Transposition)**<br>글자를 <b>바꾸지 않고 순서만 섞음**.<br><b>Rail Fence(레일 펜스)**: 지그재그로 쓰고 행별로 읽음.<br><b>열 전치**: 표에 쓰고 열 순서를 바꿔 읽음.<br>글자 빈도는 원문과 같으니(순서만 바뀜), <b>빈도가 정상인데 뜻이 없으면 전치**를 의심.</p><p><b>기타 인코딩·암호**<br>· <b>Morse**·<b>Bacon**·<b>Baudot**.<br>· <b>Base 계열**: Base64·32·58·85(형식으로 구분).<br>· <b>진법**: 2진·8진·16진 → ASCII.<br>· <b>치환 표**: A1Z26(A=1), 전화 키패드, 좌표.</p><p><b>식별이 핵심**<br>암호문의 <b>특징**으로 무엇인지 맞힙니다:<br>· 대문자 26자만 → 고전 치환/전치.<br>· <code>=</code>로 끝남 → Base64.<br>· <code>.</code>과 <code>-</code> → Morse.<br>· 0-9a-f만 → hex.<br>· 숫자 쌍 → A1Z26·ASCII.</p><p><b>도구 — CyberChef·dcode**<br><b>CyberChef**의 <b>Magic**은 인코딩을 자동 추측. <b>dcode.fr**은 거의 모든 고전 암호를 식별·해독. 손으로 안 풀고 도구로 빠르게 — 다만 <b>무엇인지 알아보는 눈**은 있어야 도구를 맞게 씁니다.</p><p><b>왜 배우나:</b> 고전 암호는 crypto의 <b>준비운동**입니다. '암호문을 보고 무엇인지 식별하는 눈'을 기르고, 인코딩·치환·전치의 감각을 잡습니다. 실전 CTF에서도 <b>인코딩 체인·고전 암호 조합**이 자주 나옵니다(방어 코스의 CyberChef가 여기서).</p><p><b>실전:</b> 암호문의 형식·문자집합·길이로 종류를 추정하고, CyberChef Magic·dcode로 해독합니다. 여러 겹이면(Base64 → ROT → ...) 한 겹씩 벗깁니다.</p>",
term:"# 식별 -> 해독\n암호문: 'FVFR{p13n5_p1cu3e}'  # 대문자+{} -> Caesar/치환\n$ # ROT13/brute로 shift 찾기 -> SFSE{...}\n\n# CyberChef Magic이 자동 추측\n# dcode.fr가 고전 암호 식별·해독\n# 인코딩 체인은 한 겹씩: base64 -> rot -> ascii",
lab:["Caesar 암호를 25가지 shift로 전수 해독한다","단일 치환을 빈도 분석(quipqiup)으로 푼다","Vigenère의 키 길이를 찾아 해독한다","Rail Fence 등 전치 암호를 이해한다","암호문 형식으로 종류를 식별하는 연습을 한다","CyberChef Magic으로 자동 추측한다","여러 겹 인코딩을 한 겹씩 벗긴다"],
q:[{t:"Caesar(시저) 암호를 푸는 가장 간단한 방법은?",o:["빈도 분석","경우의 수가 25개뿐이라 모든 shift를 전수 시도(brute force)","Vigenère 공격","z3"],a:1,
  e:"Caesar는 <b>shift 값이 1~25뿐**입니다. 전부 시도해 뜻이 통하는 것을 고르면 됩니다. ROT13은 shift 13인 특수한 경우입니다."},
   {t:"글자 빈도는 원문과 정상인데 뜻이 통하지 않는 암호문에서 의심할 것은?",o:["Caesar","전치 암호(Transposition) — 글자를 바꾸지 않고 순서만 섞은 것","Base64","해시"],a:1,
    e:"<b>전치 암호**는 글자를 <b>바꾸지 않고 순서만 섞습니다**. 그래서 빈도는 원문과 같지만 읽을 수 없습니다. Rail Fence·열 전치가 대표적입니다."},
   {t:"암호문 식별에서 <code>=</code>로 끝나고 A-Za-z0-9+/로 이루어진 문자열은?",o:["Morse","Base64 (패딩 = 과 64문자 집합)","Caesar","hex"],a:1,
    e:"<code>=</code> 패딩과 <b>A-Za-z0-9+/**문자 집합은 <b>Base64**의 특징입니다(방어 코스). 형식으로 인코딩·암호 종류를 식별하는 게 crypto의 첫 단계입니다."}]},

{d:113,t:"XOR 암호",g:"XOR 기반 암호의 원리와, 키를 복원하는 여러 기법을 익힌다.",
c:"<p><b>XOR**는 crypto·rev·pwn 전반에 나오는 핵심 연산입니다. 단순하지만 <b>잘못 쓰면**쉽게 깨지고, CTF에 매우 자주 등장합니다.</p><p><b>XOR 복습**<br>· <code>0^0=0, 1^1=0, 0^1=1, 1^0=1</code>(같으면 0, 다르면 1).<br>· <b>자기 역함수**: <code>a^k^k = a</code>. 암호화·복호화가 <b>같은 연산**.<br>· <b>교환·결합**: 순서 무관.<br>암호화: <code>cipher = plain ^ key</code>. 복호화: <code>plain = cipher ^ key</code>.</p><p><b>단일 바이트 XOR**<br>키가 <b>1바이트**면 경우의 수 <b>256개**. 전수 시도하면 즉시 풀립니다.<br><code>for k in range(256): try_decrypt(cipher, k)</code> → 읽히는 것 선택. CyberChef의 <b>XOR Brute Force**.</p><p><b>반복 키 XOR(Vigenère류)**<br>키가 여러 바이트고 <b>반복**되면(<code>plain ^ key ^ key ^ ...</code>).<br><b>공격**:<br>① <b>키 길이 추정**: 같은 간격의 반복 패턴, 자기상관(Hamming distance 최소).<br>② 각 키 위치를 <b>단일 바이트 XOR처럼**빈도 분석으로 복원.<br><b>xortool**이 자동으로: <code>xortool cipher.bin</code>이 키 길이·키를 추정.</p><p><b>알려진 평문 공격(Known Plaintext)**<br>평문 일부를 알면(예: 플래그가 <code>SFSE{</code>로 시작), <b>키를 즉시 복원**: <code>key = cipher ^ known_plain</code>. 파일 형식의 <b>고정 헤더**(PNG의 <code>\\x89PNG</code>, ZIP의 <code>PK</code>)를 known plaintext로 씁니다 — 방어 코스의 매직바이트가 무기로.</p><p><b>같은 키 재사용(치명적 실수)**<br>같은 키로 두 평문을 XOR하면(<code>c1=p1^k, c2=p2^k</code>), <code>c1^c2 = p1^p2</code> — <b>키가 사라지고 두 평문의 XOR**이 남습니다. crib dragging으로 둘 다 복원. <b>OTP(일회용 패드)를 재사용하면 안 되는 이유**입니다.</p><p><b>왜 중요한가:</b> XOR은 <b>단순해서 자주 쓰이고, 자주 잘못 쓰입니다**. 단일 바이트·반복 키·키 재사용 — 각각 명확한 공격이 있습니다. 그리고 XOR을 이해하면 <b>스트림 암호(다음 날)·rev의 XOR 복호화·간단한 난독화**가 다 풀립니다. crypto의 기초 근육입니다.</p><p><b>실전:</b> XOR 암호를 만나면 키 길이를 추정하고(1바이트면 brute, 여러 바이트면 xortool), known plaintext(플래그 접두사·파일 헤더)가 있으면 그걸로 키를 복원합니다. 키 재사용이 보이면 <code>c1^c2</code>로 공격합니다.</p>",
term:"# 단일 바이트 XOR — 전수\nfor k in range(256):\n    out = bytes(c ^ k for c in cipher)\n    if b'SFSE' in out: print(k, out)\n\n# known plaintext로 키 복원\nkey = bytes(c ^ p for c,p in zip(cipher, b'SFSE{'))\n\n# 반복 키: xortool cipher.bin  (키 길이·키 자동)",
lab:["XOR의 자기 역함수 성질을 이해한다","단일 바이트 XOR을 256가지로 전수 해독한다","known plaintext로 키를 즉시 복원한다","파일 헤더(매직바이트)를 known plaintext로 쓴다","반복 키 XOR을 xortool로 공격한다","키 재사용(c1^c2=p1^p2)의 위험을 이해한다","XOR이 rev·스트림 암호와 어떻게 이어지는지 적는다"],
q:[{t:"단일 바이트 XOR 암호를 푸는 방법은?",o:["빈도 분석만","키가 256가지뿐이라 전수 시도(brute force)해 읽히는 것을 고름","Vigenère","불가능"],a:1,
  e:"1바이트 키는 <b>256가지**뿐입니다. 전부 XOR해보고 <b>읽히는(또는 SFSE{ 포함)**것을 고르면 됩니다. CyberChef의 XOR Brute Force가 자동으로 합니다."},
   {t:"암호문이 <code>SFSE{</code>로 시작함을 알 때(known plaintext) 키를 구하는 방법은?",o:["불가능","<code>key = cipher ^ known_plain</code> — XOR의 자기 역함수 성질로 키를 즉시 복원","빈도 분석","무차별 대입"],a:1,
    e:"<code>cipher = plain ^ key</code>이므로 <code>key = cipher ^ plain</code>입니다. 아는 평문(<code>SFSE{</code>·파일 헤더)과 암호문을 XOR하면 <b>키가 바로 나옵니다**. 반복 키의 첫 부분을 복원하는 강력한 기법입니다."},
   {t:"같은 키로 두 평문을 XOR 암호화하면(키 재사용) 생기는 치명적 문제는?",o:["없음","<code>c1 ^ c2 = p1 ^ p2</code>가 되어 키가 사라지고 두 평문의 XOR만 남아, crib dragging으로 복원 가능","더 안전","속도 저하"],a:1,
    e:"<code>c1^c2 = (p1^k)^(p2^k) = p1^p2</code> — <b>키가 소거**됩니다. 두 평문의 XOR에서 crib dragging으로 둘 다 복원됩니다. <b>OTP·스트림 암호 키를 재사용하면 안 되는**결정적 이유입니다."}]},

{d:114,t:"RSA 공격 I — 구조와 약한 파라미터",g:"RSA의 원리를 이해하고, 작은 소수·factordb 등으로 깨는 기본기를 익힌다.",
c:"<p><b>RSA**는 CTF crypto의 <b>왕**입니다. 공개키 암호의 대표이고, <b>잘못 쓰면**깨지는 방법이 무궁무진합니다.</p><p><b>RSA 원리(필요한 만큼)**<br>· <b>키 생성**: 두 소수 <code>p, q</code> → <code>n = p*q</code>(모듈러스). <code>φ(n)=(p-1)(q-1)</code>. 공개 지수 <code>e</code>(보통 65537). 개인 지수 <code>d = e⁻¹ mod φ(n)</code>.<br>· <b>공개키**: <code>(n, e)</code>. <b>개인키**: <code>(n, d)</code>.<br>· <b>암호화**: <code>c = m^e mod n</code>. <b>복호화**: <code>m = c^d mod n</code>.<br><b>보안의 핵심**: <code>n</code>에서 <code>p, q</code>를 구하기 어려움(<b>소인수분해**). <code>p,q</code>를 알면 <code>d</code>를 계산해 <b>완전히 깨짐**.</p><p><b>공격 ① — n이 작거나 알려짐**<br><code>n</code>이 작으면(수백 비트 이하) 소인수분해가 됩니다.<br><b>factordb.com**: 이미 분해된 <code>n</code>의 거대한 DB. <code>n</code>을 검색해 <code>p,q</code>가 나오면 끝.<br><b>도구**: <code>yafu</code>·<code>msieve</code>(로컬 분해), <code>sympy.factorint</code>.</p><p><b>공격 ② — p와 q가 가까움(Fermat)**<br><code>p</code>와 <code>q</code>가 <b>비슷한 크기**면 <b>페르마 소인수분해**로 빠르게 분해. <code>n</code> 근처의 완전제곱수를 찾습니다.</p><p><b>공격 ③ — 공통 인수(GCD)**<br>여러 <code>n</code>이 <b>같은 소수를 공유**하면(약한 난수생성기), <code>gcd(n1, n2)</code>가 공통 소수 → 둘 다 분해. 여러 공개키를 받으면 시도.</p><p><b>공격 ④ — 작은 e (e=3)**<br><code>e=3</code>이고 <code>m</code>이 작아 <code>m³ &lt; n</code>이면, <code>c = m³</code>(mod 없음) → <b>세제곱근**으로 <code>m</code> 복원. <code>gmpy2.iroot(c, 3)</code>.</p><p><b>RsaCtfTool — 자동 공격**<br><code>RsaCtfTool --publickey key.pub --uncipher c</code>가 <b>수십 가지 공격을 자동**시도(factordb·Fermat·Wiener·공통인수 등). CTF RSA의 1순위 도구. 다만 <b>왜 통했는지**이해하며 씁니다.</p><p><b>왜 RSA가 crypto의 중심인가:</b> RSA는 <b>수학적 구조가 명확**해, 파라미터의 <b>작은 실수 하나하나가 공격**이 됩니다 — 작은 소수, 가까운 소수, 공유 소수, 작은 지수, 큰 지수(Wiener, 다음 날)... 각 실수마다 우아한 수학 공격이 있어, RSA를 배우면 <b>'암호를 깬다'가 무엇인지**제대로 체득합니다.</p><p><b>실전:</b> 공개키에서 <code>n, e</code>를 추출하고(<code>openssl</code>·pycryptodome), factordb·RsaCtfTool로 자동 시도합니다. 안 되면 <code>e</code>·<code>n</code>의 특징(작은 e, 가까운 p·q, 여러 n 공유)을 보고 맞는 공격을 손으로 적용합니다.</p>",
term:"# RSA 기본 공격 흐름 (파이썬)\nfrom Crypto.Util.number import long_to_bytes, inverse\n# 1) factordb 등으로 p,q 획득\np, q = ...    # factordb.com에서 n 검색\nphi = (p-1)*(q-1)\nd = inverse(e, phi)\nm = pow(c, d, n)\nprint(long_to_bytes(m))   # 플래그\n\n# 자동: RsaCtfTool --publickey key.pub --uncipher c",
lab:["RSA의 n·e·d·φ(n) 관계를 정리한다","공개키에서 n·e를 추출한다","factordb.com에서 n을 검색해 분해한다","p,q로 d를 계산해 복호화한다","e=3·작은 m이면 세제곱근으로 복원한다","여러 n의 gcd로 공통 소수를 찾는다","RsaCtfTool로 자동 공격을 시도한다"],
q:[{t:"RSA의 보안이 의존하는 '어려운 문제'는?",o:["로그 계산","큰 수 n을 두 소수 p·q로 소인수분해하는 것 — p·q를 알면 개인키 d를 계산해 완전히 깨짐","해시 충돌","정렬"],a:1,
  e:"RSA는 <b>n = p*q의 소인수분해가 어렵다**는 데 의존합니다. <code>p,q</code>를 알면 <code>φ(n)</code>과 <code>d</code>를 계산해 <b>완전히 복호**됩니다. n이 작거나 특수하면 분해되어 깨집니다."},
   {t:"CTF에서 RSA의 n이 주어졌을 때 가장 먼저 시도할 것은?",o:["무차별 대입","factordb.com에서 n을 검색 — 이미 분해된 값이면 p,q가 즉시 나옴","양자컴퓨터","슈퍼컴퓨터"],a:1,
    e:"<b>factordb**는 이미 분해된 n의 거대한 DB입니다. CTF의 약한 n은 여기 있는 경우가 많아, <b>검색 한 번**으로 p,q가 나옵니다. 그다음 RsaCtfTool로 자동 시도합니다."},
   {t:"RSA에서 <code>e=3</code>이고 평문 m이 작아 <code>m³ &lt; n</code>일 때의 공격은?",o:["불가능","c = m³에 모듈러가 실질적으로 적용 안 되므로, c의 정수 세제곱근을 구하면 m 복원","무차별","Fermat"],a:1,
    e:"<code>m³ &lt; n</code>이면 <code>c = m³ mod n = m³</code>(mod 무의미)입니다. 그냥 <b>세제곱근**(<code>gmpy2.iroot(c,3)</code>)을 구하면 m이 나옵니다. 작은 e + 작은 m의 전형적 실수입니다."}]},

{d:115,t:"RSA 공격 II — 심화 기법",g:"Wiener·공통 모듈러스·Håstad 등 RSA의 정교한 수학 공격을 익힌다.",
c:"<p>RSA의 <b>더 정교한 실수**들과 그 공격입니다. 각각이 우아한 수학입니다.</p><p><b>Wiener 공격 — 작은 d**<br>개인 지수 <code>d</code>가 <b>너무 작으면**(d &lt; n^0.25), 연분수(continued fraction)로 <code>d</code>를 <b>복원**할 수 있습니다. 큰 <code>e</code>(d가 작으면 e가 큼)가 신호. RsaCtfTool·<code>owiener</code>가 자동으로.</p><p><b>공통 모듈러스(Common Modulus)**<br>같은 <code>n</code>, <b>다른 <code>e</code>**로 <b>같은 평문**을 암호화한 두 암호문(<code>c1=m^e1, c2=m^e2</code>)이 있고 <code>gcd(e1,e2)=1</code>이면, <b>확장 유클리드**로 <code>m</code>을 복원(개인키 없이!). 같은 메시지를 여러 명에게 다른 e로 보낼 때.</p><p><b>Håstad 방송 공격(Broadcast)**<br>같은 평문 <code>m</code>을 <b>작은 <code>e</code>(예: 3)**로 <b>서로 다른 <code>n</code>**여러 개에 암호화하면(<code>e</code>개 이상), <b>중국인의 나머지 정리(CRT)**로 <code>m^e</code>를 합치고 <code>e</code>제곱근으로 <code>m</code> 복원. 같은 메시지 방송의 실수.</p><p><b>Franklin-Reiter / 관련 메시지**<br>두 평문이 <b>알려진 선형 관계**(<code>m2 = a*m1 + b</code>)이고 같은 n·e면 복원.</p><p><b>부분 키 노출·LSB 오라클**<br>· <b>패리티/LSB 오라클**: 복호화 결과의 <b>마지막 비트만**알려주는 서비스가 있으면, 이분 탐색으로 <b>평문 전체**를 복원. 오라클 공격의 대표.<br>· <b>부분 p 노출**: <code>p</code>의 일부 비트를 알면 Coppersmith로 나머지 복원.</p><p><b>Coppersmith — 부분 정보**<br>평문·소수의 <b>일부**를 알 때, 격자(lattice) 기반으로 나머지를 복원하는 강력한 기법. sage의 <code>small_roots</code>. 고급이지만 CTF 상급에 자주.</p><p><b>도구와 sage**<br>· <b>RsaCtfTool**이 Wiener·공통인수·Fermat 등 대부분 자동.<br>· <b>sage**(수학 CAS): Coppersmith·격자·CRT 등 수동 구현. crypto 상급의 필수 도구.<br>· <b>factordb·owiener·개별 스크립트**.</p><p><b>왜 이렇게 많은가:</b> RSA는 <b>수학적 대상**이라, 파라미터·사용 방식의 각 실수가 <b>고유한 수학 공격**을 낳습니다. '작은 d → Wiener', '같은 n 다른 e → 공통 모듈러스', '작은 e 방송 → Håstad'... 이 <b>실수→공격 매핑**을 알아보는 게 RSA crypto의 실력입니다. 자동 도구가 계산하지만, <b>어떤 실수인지 식별**하는 눈이 핵심입니다.</p><p><b>실전:</b> 파라미터의 특징을 봅니다 — e가 크면 Wiener, 같은 n에 여러 e면 공통 모듈러스, 작은 e에 여러 n이면 Håstad. 식별 후 RsaCtfTool 또는 sage 스크립트로 공격합니다.</p>",
term:"# RSA 심화 — 실수 식별 -> 공격 매핑\ne가 매우 큼           -> Wiener (작은 d)\n같은 n, 다른 e, 같은 m -> 공통 모듈러스 (확장 유클리드)\n작은 e, 여러 n, 같은 m -> Håstad 방송 (CRT + e제곱근)\np의 상위 비트 노출     -> Coppersmith (sage small_roots)\nLSB 오라클 서비스      -> 이분 탐색으로 평문 복원",
lab:["e가 매우 클 때 Wiener 공격을 시도한다","같은 n·다른 e·같은 m이면 공통 모듈러스로 푼다","작은 e·여러 n이면 Håstad 방송 공격을 적용한다","LSB 오라클을 이분 탐색으로 공격하는 원리를 이해한다","Coppersmith(sage small_roots)의 용도를 파악한다","파라미터로 어떤 공격인지 식별하는 연습을 한다","RsaCtfTool·sage로 각 공격을 실행한다"],
q:[{t:"RSA에서 개인 지수 d가 너무 작을 때(따라서 e가 클 때) 쓰는 공격은?",o:["Fermat","Wiener 공격 — 연분수로 작은 d를 복원","factordb","세제곱근"],a:1,
  e:"<b>Wiener 공격**은 <code>d</code>가 작을 때(<code>d &lt; n^0.25</code>) <b>연분수**로 d를 복원합니다. 큰 e가 신호입니다. 작은 d로 복호를 빠르게 하려던 실수가 이 공격을 부릅니다."},
   {t:"같은 평문을 같은 n·다른 e로 암호화한 두 암호문이 있을 때(gcd(e1,e2)=1)의 공격은?",o:["Fermat","공통 모듈러스 공격 — 확장 유클리드로 개인키 없이 평문 m을 복원","Wiener","brute"],a:1,
    e:"<b>공통 모듈러스 공격**입니다. <code>gcd(e1,e2)=1</code>이면 확장 유클리드로 <code>a*e1+b*e2=1</code>을 찾아 <code>c1^a * c2^b = m</code>을 계산 — <b>개인키 없이**평문을 복원합니다."},
   {t:"RSA에 공격이 유독 많은 근본 이유는?",o:["약해서","수학적 구조가 명확해, 파라미터·사용 방식의 각 실수(작은 d·공유 소수·작은 e·오라클)마다 고유한 수학 공격이 존재하기 때문","오래돼서","느려서"],a:1,
    e:"RSA는 <b>수학적 대상**이라 각 실수가 <b>고유한 공격**을 낳습니다. 실력은 '어떤 실수인가'를 <b>식별**하는 눈입니다 — 도구가 계산은 대신하지만 진단은 사람이 합니다."}]},

{d:116,t:"대칭키·블록 암호 공격",g:"AES 등 블록 암호의 잘못된 운용 모드와 패딩 오라클을 공략한다.",
c:"<p><b>AES**같은 대칭 블록 암호 자체는 튼튼합니다. 하지만 <b>운용 모드·패딩·구현**의 실수가 공격을 만듭니다.</p><p><b>블록 암호 운용 모드**<br>블록 암호는 고정 크기(AES=16바이트) 블록을 암호화합니다. 긴 데이터는 <b>모드**로 이어 붙입니다.<br>· <b>ECB**: 각 블록을 독립 암호화. <b>치명적 약점**: 같은 평문 블록 → 같은 암호문 블록. <b>패턴이 그대로**드러남(ECB로 암호화한 이미지는 윤곽이 보임). 같은 블록 탐지·블록 재배열(cut-and-paste) 공격.<br>· <b>CBC**: 이전 블록과 XOR 후 암호화. 패턴은 숨지만 <b>패딩 오라클·비트 플립**취약.<br>· <b>CTR·GCM**: 스트림처럼. 논스 재사용이 치명적.</p><p><b>ECB 공격**<br>· <b>ECB 탐지**: 반복되는 16바이트 블록이 보이면 ECB.<br>· <b>Byte-at-a-time(ECB oracle)**: 내 입력 + 비밀이 함께 ECB 암호화되면, 한 바이트씩 정렬을 맞춰 <b>비밀을 한 글자씩**복원. crypto CTF 단골.<br>· <b>cut-and-paste**: 블록 단위로 잘라 붙여 <code>role=user</code>를 <code>role=admin</code> 블록으로 교체.</p><p><b>CBC 비트 플립(Bit Flipping)**<br>CBC에서 <b>이전 블록의 한 바이트를 바꾸면**다음 블록 복호 결과의 같은 위치가 <b>예측 가능하게**바뀝니다(XOR 관계). 암호화 없이 <b>평문을 조작**(<code>admin=0</code>→<code>admin=1</code>). IV를 바꾸면 첫 블록을 조작.</p><p><b>패딩 오라클(Padding Oracle) — CBC의 왕**<br>서버가 복호 시 <b>패딩이 올바른지 오류로 알려주면**(다르게 반응), <b>암호문만으로 평문 전체를 복호**할 수 있습니다(키 없이!). 한 바이트씩, 패딩이 맞을 때까지 조작하며 복원. <b>PadBuster**·<b>padding oracle 스크립트**가 자동으로. 대표적 오라클 공격.</p><p><b>논스·IV 재사용**<br>· <b>CTR/스트림에서 논스 재사용**: XOR 키 재사용과 동일(Day 113) → <code>c1^c2=p1^p2</code>.<br>· <b>GCM 논스 재사용**: 인증 키까지 복원되어 위조 가능. 치명적.</p><p><b>왜 '모드가 전부'인가:</b> AES는 안전해도 <b>어떻게 쓰느냐(모드·패딩·논스)**가 보안을 결정합니다. ECB는 패턴 유출, CBC는 패딩 오라클·비트 플립, 논스 재사용은 키 소거 — <b>알고리즘이 아니라 운용의 실수**가 뚫립니다. 이게 '직접 암호를 구현하지 말라'는 방어 원칙의 핵심 이유입니다.</p><p><b>실전:</b> 암호문에서 <b>ECB 반복 블록**을 먼저 확인하고, CBC면 <b>패딩 오라클**(오류 반응 차이)과 비트 플립을 시험합니다. 논스가 재사용되면 XOR 공격으로 갑니다. PadBuster 등 도구가 자동화합니다.</p>",
term:"# ECB 탐지 — 반복 블록\ncipher를 16바이트씩 나눠 중복 블록이 있으면 ECB\n\n# CBC 패딩 오라클 (키 없이 복호!)\n서버가 '패딩 오류' vs '복호 오류'를 다르게 반응 -> 오라클\n한 바이트씩 조작하며 패딩이 맞는 순간 -> 평문 복원\n$ padbuster URL cipher blocksize -encoding 0",
lab:["암호문에서 반복 블록으로 ECB를 탐지한다","ECB byte-at-a-time으로 비밀을 한 글자씩 복원한다","ECB cut-and-paste로 블록을 재배열한다","CBC 비트 플립으로 평문을 조작한다","패딩 오라클의 원리(오류 반응 차이)를 이해한다","PadBuster로 패딩 오라클을 자동 공격한다","논스/IV 재사용의 위험을 정리한다"],
q:[{t:"ECB 모드의 치명적 약점은?",o:["느림","같은 평문 블록이 항상 같은 암호문 블록이 되어 패턴이 드러나고, 블록 재배열·byte-at-a-time 공격이 가능","키가 짧음","IV 없음"],a:1,
  e:"<b>ECB는 블록을 독립 암호화**해, 같은 평문 → 같은 암호문입니다. 패턴이 유출되고(ECB 이미지의 윤곽), 블록 단위 조작·비밀 복원(byte-at-a-time)이 됩니다. 절대 쓰면 안 되는 모드입니다."},
   {t:"패딩 오라클(Padding Oracle) 공격이 강력한 이유는?",o:["빠름","서버가 패딩 유효성을 오류로 알려주면, 암호문만으로 키 없이 평문 전체를 한 바이트씩 복호할 수 있어서","암호화해서","IV 때문"],a:1,
    e:"서버가 '<b>패딩이 맞나 틀리나**'를 다르게 반응하면, 그 정보만으로 <b>키 없이 평문을 복원**합니다. 한 바이트씩 조작해 패딩이 맞는 순간을 찾아 역산합니다 — 오라클 공격의 대표입니다."},
   {t:"CBC 모드의 '비트 플립' 공격이 하는 일은?",o:["키 복구","이전 블록(또는 IV)의 바이트를 조작해 다음 블록 복호 결과를 예측 가능하게 바꿔, 암호화 없이 평문을 조작","블록 삭제","해시 위조"],a:1,
    e:"CBC 복호는 <b>이전 블록과 XOR**입니다. 이전 블록의 바이트를 바꾸면 다음 평문의 같은 위치가 <b>XOR 관계로 예측 가능하게**바뀝니다. 키 없이 <code>admin=0</code>→<code>1</code> 같은 조작이 됩니다."}]},

{d:117,t:"해시 공격과 비밀번호 크래킹",g:"해시의 약점을 이용해 원문·충돌·인증 우회를 달성한다.",
c:"<p>해시는 단방향이지만, <b>약점·오용**이 공격을 만듭니다. 방어 코스의 hashcat이 여기서 공격 도구로.</p><p><b>해시 복습**<br>단방향 함수: <code>hash(m)</code>은 쉽지만 역산은 어려움. 용도: 무결성·비밀번호 저장·서명.<br>· <b>약한 해시**: MD5·SHA1(충돌 취약). <b>강한 해시**: SHA-256+. <b>비밀번호용**: bcrypt·scrypt·Argon2(<b>느리게**설계 — 크래킹 방어).</p><p><b>비밀번호 크래킹(방어→공격 hashcat)**<br>해시를 얻으면 <b>오프라인**으로 원문을 찾습니다.<br>· <b>사전 공격**: rockyou.txt 등 유출 목록. <code>hashcat -m 0 hash rockyou.txt</code>(MD5).<br>· <b>규칙 변형**: <code>-r rules/best64.rule</code>(대문자·숫자 추가 변형).<br>· <b>마스크(brute)**: <code>-a 3 ?d?d?d?d</code>(패턴).<br>· <b>모드**: 0=MD5, 100=SHA1, 1400=SHA256, 1800=sha512crypt, 3200=bcrypt, 13100=Kerberoast. 해시 <b>길이·형식**으로 식별(<code>hashid</code>).<br><b>느린 해시(bcrypt)는 크래킹이 매우 느림**— 그게 방어의 목적.</p><p><b>레인보우 테이블·조회**<br>약한 해시(MD5)는 <b>온라인 DB**(crackstation)에 이미 원문이 있는 경우가 많음. 소금(salt) 없는 해시의 약점.</p><p><b>충돌 공격(Collision)**<br>· <b>MD5·SHA1 충돌**: 서로 다른 두 입력이 <b>같은 해시**. 실제로 만들어졌습니다(SHAttered). 악성 파일을 정상 파일과 같은 해시로 위장. CTF에선 주어진 조건의 충돌 생성.<br>· <b>길이 확장 공격(Length Extension)**: <code>hash(secret || message)</code>를 인증에 쓰면(MD5·SHA1·SHA256), secret을 몰라도 <b>message를 확장**해 유효한 해시를 위조. <b>hashpump**·<b>HMAC을 안 쓴**실수. HMAC이 올바른 방어.</p><p><b>타이밍 공격**<br>해시·MAC 비교를 <b>바이트별로**하고 다르면 즉시 종료하면, <b>시간 차이**로 한 바이트씩 정답을 알아냄. <b>constant-time 비교**가 방어.</p><p><b>왜 배우나:</b> 해시 공격은 <b>공격·방어가 직결**됩니다. 왜 MD5·SHA1을 쓰면 안 되는지(충돌·조회), 왜 salt와 bcrypt를 써야 하는지(레인보우·속도), 왜 HMAC을 써야 하는지(길이 확장), 왜 constant-time 비교인지(타이밍) — <b>공격을 알아야 방어를 이해**합니다. 방어 코스의 해시 권고가 전부 여기서 설명됩니다.</p><p><b>실전:</b> 해시를 <code>hashid</code>로 식별하고, 약하면 crackstation 조회, 아니면 hashcat 사전+규칙으로 크랙합니다. 인증에 <code>hash(secret||msg)</code>가 보이면 길이 확장을, 비교가 느리면 타이밍을 시험합니다.</p>",
term:"# 해시 식별 -> 크랙\n$ hashid '5f4dcc3b5aa765d61d8327deb882cf99'\n[+] MD5\n$ hashcat -m 0 hash.txt rockyou.txt -r rules/best64.rule\n5f4dcc3b...:password\n\n# 길이 확장 공격 (hash(secret||msg) 인증)\n$ hashpump -s <sig> -d <msg> -k <keylen> -a '&admin=1'",
lab:["<code>hashid</code>로 해시 종류를 식별한다","약한 해시(MD5)를 crackstation으로 조회한다","hashcat 사전+규칙으로 비밀번호를 크랙한다","bcrypt가 왜 크래킹이 느린지 이해한다","MD5·SHA1 충돌(SHAttered)의 의미를 정리한다","길이 확장 공격(hashpump)의 조건을 이해한다","salt·HMAC·constant-time 비교가 각각 무엇을 막는지 적는다"],
q:[{t:"비밀번호 저장용 해시로 bcrypt·scrypt·Argon2를 쓰는 이유는?",o:["더 짧아서","일부러 느리게(계산량 크게) 설계되어, 오프라인 무차별 대입(크래킹)을 어렵게 만들기 때문","암호화라서","충돌이 없어서"],a:1,
  e:"이들은 <b>일부러 느립니다**. MD5는 초당 수십억 번 크랙되지만 bcrypt는 훨씬 느려, 무차별 대입 비용을 <b>극적으로 높입니다**. 그게 비밀번호 해시의 방어 목적입니다."},
   {t:"길이 확장 공격(Length Extension)이 가능한 잘못된 인증 방식은?",o:["HMAC 사용","<code>hash(secret || message)</code>로 MAC을 만드는 것 — secret을 몰라도 message를 확장해 유효한 해시를 위조 가능","bcrypt","salt 사용"],a:1,
    e:"<code>hash(secret||msg)</code>(MD5·SHA256 등)는 <b>길이 확장 공격**에 취약합니다. secret 없이도 msg를 확장해 유효 해시를 위조합니다. <b>HMAC**이 올바른 방어입니다."},
   {t:"MD5·SHA1을 보안 용도로 쓰면 안 되는 이유는?",o:["느려서","충돌 공격이 실제로 가능하고(SHAttered), salt 없으면 레인보우·온라인 조회로 원문이 바로 나오기 때문","너무 길어서","최신이라서"],a:1,
    e:"MD5·SHA1은 <b>충돌이 실증**되었고(서로 다른 입력이 같은 해시), 약해서 <b>온라인 DB 조회**로 원문이 바로 나옵니다. 그래서 서명·비밀번호에 쓰면 안 됩니다 — 방어 코스의 그 권고입니다."}]},

{d:118,t:"스테가노그래피와 포렌식형 crypto",g:"파일 속에 숨겨진 데이터를 찾아내는 CTF의 실무형 기법을 익힌다.",
c:"<p>CTF의 <b>스테가노그래피(stego)·forensic**은 crypto·misc와 겹칩니다. <b>파일 속에 숨겨진 데이터**를 찾는 실무형 기법이고, 방어 코스의 포렌식 도구가 그대로 무기가 됩니다.</p><p><b>스테가노그래피 — 숨기기**<br>이미지·오디오·파일에 데이터를 <b>몰래 숨깁니다**.<br>· <b>LSB(최하위 비트)**: 이미지 픽셀의 마지막 비트에 데이터를 심음(육안 무변화). <code>zsteg</code>(PNG)·<code>stegsolve</code>로 추출.<br>· <b>메타데이터**: EXIF에 텍스트(<code>exiftool</code>).<br>· <b>부가 데이터**: 파일 끝에 데이터 추가(이미지 뒤에 ZIP). <code>binwalk</code>로 탐지·추출.<br>· <b>steghide**: 비밀번호로 파일에 숨김(<code>steghide extract</code>).<br>· <b>오디오**: 스펙트로그램에 그림·텍스트(Audacity·Sonic Visualiser).</p><p><b>파일 분석(방어 포렌식 그대로)**<br>· <code>file**: 실제 형식(확장자 위장 간파, 매직바이트).<br>· <code>binwalk**: 파일 안의 <b>숨은 파일·아카이브**탐지·추출(<code>binwalk -e</code>).<br>· <code>strings**·<code>xxd**·<code>hexdump**: 문자열·바이트.<br>· <code>foremost**·<b>파일 카빙**: 데이터에서 파일 복원.</p><p><b>매직바이트로 위장 간파(방어 코스!)**<br>확장자가 <code>.jpg</code>여도 <code>xxd</code>로 보면 <code>PK</code>(ZIP)·<code>%PDF</code>일 수 있습니다. 헤더를 고쳐 진짜 형식으로 열거나, 잘못된 헤더를 수정해 손상된 파일을 복구합니다.</p><p><b>QR·바코드·기타**<br>이미지가 QR·바코드면 디코드. 픽셀 좌표가 데이터일 수도.</p><p><b>PCAP(네트워크 포렌식, 방어 코스)**<br>패킷 캡처에서 전송된 <b>파일·자격증명·플래그**추출: Wireshark <b>Export Objects**, <code>tshark</code>, Follow TCP Stream. USB 키보드 트래픽 복원 등.</p><p><b>접근 순서**<br>① <code>file</code>·<code>exiftool</code>·<code>strings</code>로 훑기. ② <code>binwalk</code>로 숨은 파일. ③ 이미지면 <code>zsteg</code>·stegsolve·steghide. ④ 매직바이트·헤더 확인. ⑤ 오디오면 스펙트로그램. <b>여러 겹**인 경우가 많으니 하나씩.</p><p><b>왜 방어와 이어지는가:</b> stego·forensic CTF는 <b>디지털 포렌식·침해 분석과 같은 도구·사고**를 씁니다(방어 ACT 5). 숨겨진 데이터를 찾는 능력이 <b>데이터 유출 탐지·멀웨어 분석·증거 수집**과 직결됩니다. 공격 CTF의 forensic이 방어 실무 그 자체입니다.</p><p><b>실전:</b> <code>file</code>·<code>strings</code>·<code>exiftool</code>·<code>binwalk</code>로 순서대로 훑고, 이미지면 zsteg/steghide, 오디오면 스펙트로그램을 봅니다. 매직바이트로 위장을 간파하고, 여러 겹이면 한 층씩 벗깁니다.</p>",
term:"# stego/forensic 훑기 순서\n$ file mystery.png          # 실제 형식?\n$ exiftool mystery.png      # 메타데이터에 단서?\n$ strings mystery.png | grep -i flag\n$ binwalk -e mystery.png    # 숨은 파일 추출 (뒤에 ZIP?)\n$ zsteg mystery.png         # LSB 스테가노\n$ steghide extract -sf mystery.jpg   # 비번 숨김",
lab:["<code>file</code>·<code>exiftool</code>로 파일과 메타데이터를 본다","<code>strings</code>로 숨은 문자열을 찾는다","<code>binwalk -e</code>로 파일에 숨은 아카이브를 추출한다","<code>zsteg</code>·stegsolve로 이미지 LSB를 분석한다","<code>steghide</code>로 비밀번호 숨김 데이터를 추출한다","매직바이트로 확장자 위장을 간파한다","오디오면 스펙트로그램을 확인한다"],
q:[{t:"이미지 파일에서 <code>binwalk -e</code>를 쓰는 이유는?",o:["압축","이미지 데이터 뒤에 숨겨진(부가된) 파일·아카이브(예: ZIP)를 탐지하고 추출하기 위해","암호화","크기 축소"],a:1,
  e:"<code>binwalk**는 파일 안의 <b>다른 파일 시그니처**를 찾습니다. 이미지 뒤에 ZIP·파일을 붙여 숨기는 스테가노에서, <code>-e</code>로 그 숨은 파일을 <b>추출**합니다."},
   {t:"확장자가 <code>.jpg</code>인데 <code>xxd</code>로 보니 <code>PK</code>로 시작한다면?",o:["손상됨","실제로는 ZIP 파일이 확장자만 위장한 것 — 매직바이트로 진짜 형식을 간파(방어 코스의 그 기법)","암호화됨","정상"],a:1,
    e:"<code>PK</code>는 <b>ZIP의 매직바이트**입니다. 확장자는 위장일 뿐, <b>헤더(매직바이트)가 진짜 형식**을 말합니다. 방어 코스에서 배운 파일 시그니처가 stego 간파의 핵심입니다."},
   {t:"stego·forensic CTF가 방어 실무와 직결되는 이유는?",o:["무관","디지털 포렌식·침해 분석과 같은 도구(binwalk·strings·Wireshark)·사고를 써, 데이터 유출 탐지·증거 수집 능력과 같기 때문","더 쉬워서","암호라서"],a:1,
    e:"숨겨진 데이터를 찾는 <b>도구와 사고**가 포렌식(방어 ACT 5)과 같습니다. PCAP 분석·파일 카빙·매직바이트 — CTF forensic이 곧 <b>침해 분석 실무**입니다. 공격·방어가 만나는 또 하나의 지점입니다."}]},

{d:119,t:"현대 암호와 프로토콜 공격 개관",g:"ECC·디피헬만·TLS·JWT 등 실전 프로토콜의 암호 약점을 이해한다.",
c:"<p>고전·RSA·대칭을 넘어, <b>실전 프로토콜**의 암호 약점을 개관합니다. crypto 상급이자 실무와 직결됩니다.</p><p><b>디피-헬만(DH) 키 교환**<br>공개된 채널에서 <b>공유 비밀**을 만드는 방법. 약점:<br>· <b>약한 파라미터**: 작은 소수·안전하지 않은 군 → 이산로그 풀림.<br>· <b>Logjam·small subgroup**: 약한 그룹 강제.<br>· <b>MITM**: 인증 없는 DH는 중간자에 취약.</p><p><b>타원곡선(ECC)**<br>더 짧은 키로 같은 보안. 약점:<br>· <b>약한 곡선**·<b>invalid curve 공격**(곡선 위에 없는 점 주입).<br>· <b>논스 재사용(ECDSA)**: 서명의 논스 <code>k</code>를 <b>재사용하면 개인키가 복원**됩니다(소니 PS3 해킹의 그것!). CTF 단골.<br>· <b>편향된 논스**: 격자 공격.</p><p><b>스트림 암호·PRNG**<br>· <b>약한 난수(PRNG)**: <code>rand()`·`Math.random()`·`Mersenne Twister</code>는 <b>예측 가능**. 출력 몇 개로 내부 상태 복원 → 이후 전부 예측. 토큰·키에 약한 PRNG를 쓴 실수. <b>randcrack**도구.<br>· <b>LCG**(선형 합동): 파라미터 복원 가능.<br>· <b>시드 추측**: 시간 기반 시드는 브루트포스.</p><p><b>JWT(방어→공격 ACT 2 심화)**<br>· <code>alg:none</code>, 약한 HS256 키(hashcat -m 16500), RS256→HS256 혼동. crypto 관점에서 서명 검증의 실수.</p><p><b>TLS/SSL 약점 개관**<br>· <b>다운그레이드**(POODLE·FREAK)·<b>약한 암호군**·<b>하트블리드**(구현 버그로 메모리 유출). 방어 코스의 취약점들.</p><p><b>양자 이후(Post-Quantum) 맛보기**<br>양자컴퓨터가 RSA·ECC를 깰 수 있어(Shor 알고리즘), <b>양자내성 암호(PQC)**로 이행 중. 지금 배우는 공격의 미래 배경.</p><p><b>공통 원리 — 논스·난수·인증**<br>현대 프로토콜 공격의 큰 축:<br>· <b>논스/키 재사용**(XOR·ECDSA·GCM) → 비밀 복원.<br>· <b>약한 난수**→ 예측.<br>· <b>인증 누락**→ MITM.<br>· <b>구현 버그**→ 유출(하트블리드).<br>알고리즘이 아니라 <b>사용·구현·난수**의 실수가 반복됩니다.</p><p><b>왜 개관하나:</b> 실전 시스템은 <b>프로토콜(TLS·DH·JWT·서명)**로 돌아갑니다. 이들의 암호 약점을 알면 <b>실무 취약점 진단**이 됩니다. CTF crypto 상급도 여기서 나옵니다. 깊이는 각 주제가 한 세계지만, <b>'논스 재사용·약한 난수·인증 누락'의 패턴**을 아는 것만으로 많은 문제가 열립니다.</p><p><b>실전:</b> ECDSA면 논스 재사용을, 토큰·키면 약한 PRNG를, DH면 파라미터를, JWT면 서명 검증을 봅니다. 각 프로토콜의 '반복되는 실수 패턴'을 먼저 점검합니다.</p>",
term:"# 현대 프로토콜 — 반복되는 실수 패턴\nECDSA 논스 k 재사용   -> 개인키 복원 (PS3 해킹)\n약한 PRNG(MT/LCG)     -> 출력 몇 개로 이후 전부 예측 (randcrack)\n인증 없는 DH/ECC      -> MITM\nJWT alg:none/약한 키  -> 서명 우회\nDH 작은 소수          -> 이산로그 -> 공유키 복원",
lab:["디피-헬만의 약한 파라미터 위험을 이해한다","ECDSA 논스 재사용으로 개인키가 복원됨을 안다","약한 PRNG(Mersenne Twister)의 예측 가능성을 이해한다","randcrack으로 PRNG 상태 복원 개념을 파악한다","JWT 서명 공격을 crypto 관점에서 정리한다","TLS 약점(POODLE·하트블리드)을 개관한다","'논스·난수·인증'의 반복 패턴을 정리한다"],
q:[{t:"ECDSA 서명에서 논스 <code>k</code>를 재사용하면?",o:["안전","두 서명에서 개인키가 수학적으로 복원됨 (PS3 해킹 사례) — 논스 재사용은 치명적","속도 향상","서명 무효"],a:1,
  e:"ECDSA는 서명마다 <b>고유한 논스 k**가 필요합니다. k를 <b>재사용하면**두 서명 방정식에서 <b>개인키가 복원**됩니다. 소니 PS3 해킹이 이 실수였고, CTF 단골입니다."},
   {t:"약한 PRNG(rand·Mersenne Twister)를 토큰·키 생성에 쓰면 위험한 이유는?",o:["느려서","출력 몇 개를 관찰하면 내부 상태를 복원해 이후 모든 난수를 예측할 수 있어, 토큰·키가 예측 가능해지기 때문","너무 길어서","충돌 때문"],a:1,
    e:"<code>rand()`·`Math.random()</code> 등은 <b>암호학적으로 안전하지 않습니다**. 출력 몇 개로 <b>내부 상태를 복원**(randcrack)하면 이후 난수를 전부 예측합니다. 키·토큰엔 CSPRNG를 써야 합니다."},
   {t:"현대 암호 프로토콜 공격에서 반복되는 실수 패턴이 아닌 것은?",o:["논스·키 재사용","약한 난수","인증 누락(MITM)","알고리즘 자체가 수학적으로 틀림"],a:3,
    e:"AES·RSA·ECC 알고리즘 <b>자체는 안전**합니다. 문제는 언제나 <b>사용·구현**입니다 — 논스 재사용, 약한 난수, 인증 누락, 구현 버그. '알고리즘이 틀렸다'가 아니라 '잘못 썼다'가 crypto 공격의 본질입니다."}]},

{d:120,t:"[crypto 종합] 암호를 깨뜨려라",g:"식별·약점 진단·공격 적용을 총동원해 암호 문제를 풀어낸다.",boss:true,
c:"<p><b>ACT 6을 마쳤습니다.</b> 이제 <b>약한 암호·잘못된 사용·구현 실수**를 찾아 뚫을 수 있습니다. crypto는 수학 퍼즐이자, '직접 암호를 만들지 말라'는 방어 원칙을 <b>몸으로 이해**하는 곳입니다.</p><p><b>crypto 공략 표준 흐름 — 몸에 익힐 순서**</p><p><b>1. 식별**— 무슨 암호·인코딩인가? 형식·길이·문자집합·주어진 파라미터(<code>n,e,c</code>?)로 판단.<br><b>2. 인코딩 벗기기**— Base64·hex 등은 먼저 디코드(암호 아님). 여러 겹이면 한 층씩.<br><b>3. 약점 진단**— 이 암호의 <b>실수**는? 작은 파라미터·재사용·약한 난수·오라클·알려진 취약점.<br><b>4. 공격 적용**— 맞는 기법·도구: 고전(dcode), RSA(RsaCtfTool·factordb·sage), 대칭(패딩 오라클·ECB), 해시(hashcat), XOR(xortool), stego(binwalk·zsteg).<br><b>5. 역산·복원**— 플래그를 얻음. 필요시 z3·sage로 수학을 자동화.</p><p><b>도구 지도**<br>· <b>인코딩·고전**: CyberChef·dcode.<br>· <b>RSA**: RsaCtfTool·factordb·sage.<br>· <b>대칭·오라클**: 스크립트·PadBuster.<br>· <b>해시**: hashid·hashcat·crackstation.<br>· <b>XOR**: xortool·CyberChef.<br>· <b>stego**: binwalk·zsteg·steghide·stegsolve.<br>· <b>수학**: sage·z3·gmpy2·pycryptodome.</p><p><b>막히면?</b> 정말 무슨 암호인지 맞게 식별했나? 인코딩을 다 벗겼나? 파라미터에 실수(작은 값·재사용)가 없나? 이 알고리즘의 알려진 CTF 공격을 다 시도했나? 오라클(오류 반응 차이)이 있나?</p><p><b>당신이 얻은 것**<br>· 암호문을 보고 <b>무엇인지 식별**하는 눈.<br>· 고전·XOR·RSA·대칭·해시·프로토콜의 <b>약점과 공격**.<br>· stego·forensic으로 <b>숨은 데이터**찾기.<br>· <b>왜 그 방어 규칙들이 있는지**— MD5 금지, 논스 재사용 금지, HMAC, salt, '직접 구현 금지'.<br>crypto는 <b>공격이 곧 방어 이해**인 분류입니다.</p><p><b>CTF의 봉우리들 — 거의 다 왔습니다**<br>웹(1-2)·pwn(4)·rev(5)·crypto(6)를 정복했습니다. CTF의 핵심 분류를 <b>전부**갖췄습니다. 남은 것은 이 모든 것을 <b>실전에서 엮는**힘(다음 ACT)입니다.</p><p><b>이제 당신은</b> 웹을 뚫고, 시스템을 장악하고, 바이너리를 읽고 익스플로잇하고, <b>암호를 깨뜨립니다**. 그리고 그 모든 공격의 <b>방어**를 압니다. 공격과 방어, 다섯 봉우리를 아우르는 — 진짜 보안 엔지니어의 폭넓은 시야를 갖췄습니다.</p><p>수고하셨습니다. 정말 멀리 왔습니다.</p>",
term:"# crypto 공략 — 이 순서를 외운다\n[1] 식별: 형식·길이·파라미터로 무슨 암호인지\n[2] 인코딩 벗기기 (base64/hex는 암호 아님)\n[3] 약점 진단: 작은 값·재사용·약한 난수·오라클\n[4] 공격: dcode/RsaCtfTool/factordb/hashcat/xortool/binwalk\n[5] 역산 (z3/sage로 자동화) -> 플래그",
lab:["crypto 문제(picoCTF·CryptoHack 등)를 하나 고른다","형식·파라미터로 암호 종류를 식별한다","인코딩을 먼저 다 벗긴다","이 암호의 알려진 약점·실수를 진단한다","맞는 도구(RsaCtfTool·hashcat·xortool 등)로 공격한다","필요시 z3·sage로 수학을 자동화한다","crypto 전 과정 치트시트를 완성한다","각 공격이 어떤 방어 규칙과 대응하는지 정리한다"],
q:[{t:"crypto 문제를 받았을 때 올바른 첫 두 단계는?",o:["바로 무차별 대입","① 무슨 암호·인코딩인지 식별 → ② 인코딩(base64·hex)을 먼저 벗기기","즉시 RsaCtfTool","양자컴퓨터"],a:1,
  e:"<b>식별 → 인코딩 벗기기**가 시작입니다. 무엇인지 알아야 맞는 공격을 고르고, 인코딩(암호 아님)은 먼저 디코드해야 진짜 암호가 보입니다. 정면 무차별 대입은 거의 안 통합니다."},
   {t:"CTF crypto 전반을 관통하는 핵심 통찰은?",o:["알고리즘이 약하다","알고리즘 자체가 아니라 '잘못된 사용·구현·약한 파라미터·재사용'의 실수를 찾는 것이 공격의 본질","슈퍼컴퓨터가 필요","항상 brute force"],a:1,
    e:"AES·RSA는 안전합니다. crypto는 <b>실수를 찾는 탐정 놀이**입니다 — 작은 파라미터·논스 재사용·약한 난수·오라클. 이 통찰이 '직접 암호 구현 금지' 등 방어 원칙의 근거입니다."},
   {k:"cmd",t:"RSA 공개키 <code>key.pub</code>와 암호문으로 여러 공격을 자동 시도하는 대표 도구를 실행하세요. (공개키만 지정, 도구명 그대로)",
    ph:"RsaCtfTool ...","a":["RsaCtfTool --publickey key.pub"],
    "re":["^rsactftool\\s+--publickey\\s+\\S*key\\.pub"],
    hint:"RSA CTF 자동 공격 도구에 --publickey로 공개키를 줍니다.",
    e:"<code>RsaCtfTool --publickey key.pub</code> — factordb·Fermat·Wiener·공통인수 등 수십 가지를 자동 시도합니다. crypto·RSA의 1순위 도구죠. ACT 6 crypto 완주를 축하합니다 — 이제 CTF의 다섯 핵심 분류(웹·pwn·rev·crypto)를 모두 갖췄습니다."}]}

]}
];
