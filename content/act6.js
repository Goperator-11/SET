{n:6,t:"클라우드와 컨테이너",s:"서버실이 사라진 자리. 로그도 공격도 API를 타고 흐른다.",days:[

{d:101,t:"클라우드는 무엇이 다른가",g:"온프렘 조사 방식이 왜 그대로 안 통하는지 이해한다.",
c:"<p>ACT 5에서 클라우드를 맛봤습니다. 여기서는 <b>본격적으로</b> 다룹니다. 먼저 사고방식을 바꿔야 합니다.</p><p><b>디스크를 뽑을 수 없습니다.</b> 물리 접근이 없고, 인스턴스는 몇 분 만에 생겼다 사라집니다. 대신 <b>모든 행위가 API 호출로 기록</b>됩니다. 온프렘에서 '누가 서버를 껐지?'를 밝히려면 고생해야 하지만, 클라우드에서는 <code>StopInstances</code> 이벤트 한 줄에 누가·언제·어디서가 다 있습니다.</p><p><b>경계가 IP가 아니라 자격증명입니다.</b> 방화벽 안쪽/바깥쪽 개념이 흐려지고, <b>액세스 키 하나가 곧 성문 열쇠</b>입니다. 그래서 클라우드 침해의 압도적 다수는 취약점이 아니라 <b>키 유출과 권한 설정 실수</b>에서 옵니다.</p><p><b>속도가 다릅니다.</b> 공격자가 키를 얻으면 몇 분 안에 수백 개 인스턴스를 띄우거나 버킷 전체를 내려받을 수 있습니다. 사람이 알아채고 대응하는 속도로는 못 따라갑니다. <em>자동 대응</em>이 선택이 아니라 필수가 되는 이유입니다.</p>",
lab:["AWS 프리티어 계정을 만들거나 기존 계정에 로그인한다 (없으면 문서만 따라 읽어도 됨)","<code>aws sts get-caller-identity</code> 로 내가 누구인지 확인한다","온프렘 조사와 클라우드 조사의 차이를 3열 표(증거 위치 / 수집 방법 / 휘발성)로 정리한다","'책임 공유 모델' 그림을 IaaS·PaaS·SaaS 세 가지로 각각 그려본다","최근 대형 클라우드 침해 사고 1건을 찾아 최초 원인을 확인한다","자기 계정의 액세스 키가 몇 개 있고 마지막 사용일이 언제인지 확인한다"],
q:[{t:"클라우드 침해 사고의 최초 원인으로 가장 흔한 것은?",o:["하이퍼바이저 취약점","유출된 액세스 키와 과도한 IAM 권한","DDoS","물리적 침입"],a:1,e:"<b>자격증명과 권한 설정</b>이 압도적입니다. 인프라 자체는 클라우드 제공자가 지키므로, 고객 책임 영역인 <b>키 관리와 IAM</b>이 실질적 공격면입니다."},
{t:"내 신원과 계정을 확인하는 AWS CLI 명령을 입력하세요.",k:"cmd",ph:"aws ...",a:["aws sts get-caller-identity"],re:["^aws\\s+sts\\s+get-caller-identity"],hint:"STS(Security Token Service)에서 '호출자 신원'을 얻어옵니다.",e:"<code>aws sts get-caller-identity</code> 는 현재 자격증명의 계정 ID·ARN·UserId 를 돌려줍니다. 침해 조사에서 <b>얻은 키가 누구 것인지</b> 확인하는 첫 명령이자, 공격자도 가장 먼저 치는 명령입니다."}]},

{d:102,t:"IAM — 사용자, 역할, 정책",g:"클라우드 권한 모델의 뼈대를 정확히 잡는다.",
c:"<p><b>IAM</b>은 클라우드 보안의 중심입니다. 세 가지만 정확히 구분하면 됩니다.</p><p><b>사용자(User)</b> — 사람이나 프로그램에 붙는 영구 신원. <b>액세스 키가 발급되고, 이 키가 유출의 주범</b>입니다.<br><b>역할(Role)</b> — 잠깐 빌려 쓰는 신원. 키가 없고 <b>임시 자격증명</b>을 발급받습니다. EC2·Lambda·다른 계정이 <em>맡아서(assume)</em> 씁니다.<br><b>정책(Policy)</b> — 무엇을 할 수 있는지 적은 JSON. <code>Effect</code>(Allow/Deny), <code>Action</code>(<code>s3:GetObject</code>), <code>Resource</code>(ARN), <code>Condition</code>.</p><p><b>핵심 원칙 — 사용자보다 역할을 쓰세요.</b> 역할은 자격증명이 몇 시간이면 만료되므로, 유출돼도 피해 기간이 짧습니다. 영구 액세스 키는 깃허브에 올라가면 몇 년째 유효합니다.</p><p>평가 규칙도 외워두세요. <b>명시적 Deny가 항상 이깁니다.</b> Allow가 아무리 많아도 Deny 하나면 차단됩니다. 그리고 <b>아무것도 안 적혀 있으면 기본 거부</b>입니다.</p>",
lab:["<code>aws iam list-users</code> 와 <code>aws iam list-roles</code> 를 실행해 차이를 본다","<code>aws iam list-attached-user-policies --user-name &lt;이름&gt;</code> 로 붙은 정책을 확인한다","AWS 관리 정책 <code>AdministratorAccess</code> 의 JSON을 열어 <code>Action: \"*\"</code> 를 눈으로 확인한다","<code>Effect / Action / Resource / Condition</code> 네 필드를 가진 정책을 직접 하나 써본다","'사용자 vs 역할'의 차이를 유출 시 피해 관점에서 3줄로 정리한다","<code>aws iam get-account-authorization-details</code> 출력이 왜 조사에 유용한지 확인한다"],
q:[{t:"IAM 정책 평가에서 Allow와 명시적 Deny가 충돌하면?",o:["Allow가 우선한다","명시적 Deny가 항상 우선한다","나중에 붙은 정책이 우선한다","오류가 발생한다"],a:1,e:"<b>명시적 Deny는 절대적</b>입니다. 그래서 조직 차원의 가드레일(SCP)을 Deny로 걸면 하위 계정이 아무리 Allow를 붙여도 못 뚫습니다."},
{t:"영구 액세스 키를 가진 IAM 사용자 대신 역할(Role) 사용을 권장하는 이유는?",o:["더 빠르기 때문","역할은 만료되는 임시 자격증명을 쓰므로 유출되어도 악용 가능 기간이 짧기 때문","비용이 저렴해서","로그가 더 많이 남아서"],a:1,e:"역할의 임시 자격증명은 보통 <b>1~12시간</b>이면 만료됩니다. 깃허브에 올라가도 이미 죽은 키입니다. 영구 키는 회수하기 전까지 계속 살아 있습니다."}]},

{d:103,t:"IAM 권한 상승 경로",g:"멀쩡해 보이는 권한 조합이 어떻게 관리자로 이어지는지 안다.",
c:"<p>공격자가 낮은 권한 키를 얻었습니다. 여기서 관리자까지 가는 길이 <b>의외로 많습니다.</b> 대표적인 것들만 봅시다.</p><p><b><code>iam:CreateAccessKey</code></b> — 다른(더 높은 권한) 사용자에게 새 키를 만들어 붙일 수 있습니다. 그 키로 로그인하면 끝입니다.<br><b><code>iam:AttachUserPolicy</code></b> — 자기 자신에게 <code>AdministratorAccess</code>를 붙입니다.<br><b><code>iam:PassRole</code> + <code>ec2:RunInstances</code></b> — 관리자 역할을 붙인 EC2를 띄우고 그 안에서 메타데이터 서비스로 자격증명을 꺼냅니다.<br><b><code>lambda:CreateFunction</code> + <code>iam:PassRole</code></b> — 같은 원리를 람다로.<br><b><code>iam:CreatePolicyVersion</code></b> — 기존 정책의 새 버전을 만들어 권한을 넓힙니다.</p><p><b>핵심 통찰: 위험한 건 개별 권한이 아니라 조합입니다.</b> <code>PassRole</code> 하나만으로는 아무것도 못 하지만 <code>RunInstances</code>와 만나면 관리자가 됩니다. 그래서 정책을 하나씩 보면 안 되고 <b>전체 그래프를 봐야</b> 합니다.</p><p>도구: <b>Pacu</b>(공격자용), <b>PMapper</b>·<b>Cloudsplaining</b>(방어자용). AD의 BloodHound와 정확히 같은 발상입니다.</p>",
lab:["Cloudsplaining 또는 PMapper 문서를 읽고 무엇을 시각화하는지 확인한다","위 권한 상승 경로 5개를 표로 정리한다 (필요 권한 / 공격 흐름 / 탐지 지표)","<code>iam:PassRole</code> 이 왜 단독으로는 무해한지 3줄로 설명해본다","CloudTrail에서 <code>AttachUserPolicy</code>, <code>CreateAccessKey</code> 를 탐지하는 조건을 작성한다","'권한 조합 위험'을 AD의 BloodHound 개념과 비교해 적어본다","자기 계정에서 관리자 권한을 가진 주체가 몇 개인지 세어본다"],
q:[{t:"<code>iam:PassRole</code> 과 <code>ec2:RunInstances</code> 를 함께 가진 사용자가 위험한 이유는?",o:["인스턴스 비용이 많이 나와서","관리자 역할을 붙인 인스턴스를 띄우고 그 안에서 자격증명을 꺼내 권한 상승이 가능하기 때문","네트워크가 노출되어서","로그가 남지 않아서"],a:1,e:"인스턴스 메타데이터 서비스(<code>169.254.169.254</code>)에서 역할의 임시 자격증명을 읽을 수 있습니다. <b>개별 권한은 무해해도 조합이 치명적</b>인 대표 사례입니다."},
{t:"공격자가 다른 IAM 사용자에게 새 액세스 키를 만들어 붙일 때 CloudTrail에 남는 이벤트 이름을 입력하세요.",k:"input",ph:"이벤트 이름",a:["CreateAccessKey"],re:["^createaccesskey$"],hint:"IAM에서 '액세스 키를 만든다'는 동작 그대로입니다.",e:"<b>CreateAccessKey</b>는 지속성 확보의 핵심 신호입니다. 원래 키가 회수돼도 새로 만든 키로 계속 들어올 수 있습니다. <b>자기 자신이 아닌 사용자에 대한 CreateAccessKey</b>는 특히 강한 알럿감입니다."}]},

{d:104,t:"CloudTrail 깊이 읽기",g:"이벤트 한 건의 모든 필드를 조사에 활용한다.",
c:"<p>CloudTrail 이벤트는 JSON 한 덩어리입니다. 필드를 알아야 조사가 됩니다.</p><p><b><code>eventTime</code></b> — UTC. <b><code>eventName</code></b> — API 이름. <b><code>eventSource</code></b> — 서비스(<code>s3.amazonaws.com</code>).<br><b><code>userIdentity</code></b> — <b>가장 중요합니다.</b> <code>type</code>(IAMUser·AssumedRole·Root), <code>userName</code>, <code>accessKeyId</code>, <code>sessionContext</code>(역할을 맡은 경우 원래 누구였는지).<br><b><code>sourceIPAddress</code></b> — 호출 위치. <b><code>userAgent</code></b> — <code>aws-cli</code>인지 콘솔인지 SDK인지. <b>공격자는 대개 CLI를 씁니다.</b><br><b><code>errorCode</code></b> — <code>AccessDenied</code>가 연달아 찍히면 <b>권한 탐색 중</b>이라는 뜻입니다.<br><b><code>requestParameters</code> / <code>responseElements</code></b> — 실제 대상.</p><p><b>조사 요령 세 가지.</b> ① <b>AccessDenied 폭증을 먼저 찾으세요.</b> 공격자가 뭘 할 수 있는지 더듬는 흔적이고, 성공한 것보다 오히려 눈에 잘 띕니다. ② <b>accessKeyId 로 묶으세요.</b> 한 키의 전체 행적이 한 번에 나옵니다. ③ <b>sourceIPAddress + userAgent 조합</b>이 평소와 다른지 보세요.</p><p>주의: CloudTrail 기본 설정은 <b>관리 이벤트만</b> 기록합니다. S3 객체 읽기 같은 <b>데이터 이벤트는 따로 켜야</b> 하고, 안 켜두면 '무엇이 유출됐는지' 영영 모릅니다.</p>",
lab:["<code>aws cloudtrail lookup-events --max-results 5</code> 로 실제 이벤트 JSON을 본다","이벤트 하나를 골라 위 필드를 전부 찾아 주석을 달아본다","<code>--lookup-attributes AttributeKey=EventName,AttributeValue=ConsoleLogin</code> 으로 로그인 이력을 본다","<code>jq</code> 로 <code>errorCode</code> 가 있는 이벤트만 걸러내는 명령을 만든다","자기 계정에 <b>데이터 이벤트</b>가 켜져 있는지 확인한다","'관리 이벤트'와 '데이터 이벤트'의 차이를 3줄로 정리한다"],
term:"$ jq -r '.Records[] | select(.errorCode) | \"\\(.eventTime) \\(.sourceIPAddress) \\(.eventName) \\(.errorCode)\"' trail.json\n2026-06-20T02:41:44Z 203.0.113.66 <b>AttachUserPolicy AccessDenied</b>\n2026-06-20T02:43:32Z 203.0.113.66 <b>StopLogging AccessDenied</b>",
q:[{t:"CloudTrail에서 <code>AccessDenied</code> 오류가 짧은 시간에 여러 API에 걸쳐 발생했다. 가장 유력한 해석은?",o:["서비스 장애","공격자가 탈취한 자격증명으로 무엇이 가능한지 권한을 탐색 중","네트워크 지연","정상적인 배포 과정"],a:1,e:"<b>권한 열거(enumeration)</b>입니다. 공격자는 자기가 얻은 키로 뭘 할 수 있는지 모르므로 이것저것 찔러봅니다. 성공한 호출보다 <b>실패 로그가 공격을 더 잘 드러냅니다</b>."},
{t:"S3 버킷에서 어떤 객체가 실제로 읽혔는지 CloudTrail에 남기려면 무엇이 필요한가?",o:["기본 설정으로 자동 기록된다","데이터 이벤트(Data events)를 별도로 활성화해야 한다","S3 버전 관리를 켜야 한다","CloudWatch를 연동해야 한다"],a:1,e:"기본은 <b>관리 이벤트만</b>입니다. 데이터 이벤트를 안 켜두면 침해 후 '무엇이 유출됐는가'를 산정할 수 없어 신고 범위조차 정하지 못합니다."}]},

{d:105,t:"관리형 탐지 서비스",g:"GuardDuty·Security Hub가 무엇을 대신 봐주는지 안다.",
c:"<p>클라우드 제공자가 만든 탐지 서비스를 쓰면 <b>직접 룰을 안 짜도</b> 상당 부분이 커버됩니다. 다만 무엇을 보고 무엇을 못 보는지는 알아야 합니다.</p><p><b>GuardDuty</b>는 CloudTrail·VPC Flow Logs·DNS 로그를 자동으로 먹고 위협 인텔·머신러닝으로 판단합니다. 대표 탐지: <code>UnauthorizedAccess:IAMUser/InstanceCredentialExfiltration</code>(EC2 역할 자격증명이 <b>외부에서</b> 쓰임 — 매우 강력한 신호), <code>CryptoCurrency:EC2/BitcoinTool</code>, <code>Recon:IAMUser/MaliciousIPCaller</code>, <code>Impact:S3/AnomalousBehavior</code>.</p><p><b>Security Hub</b>는 여러 서비스의 결과를 한곳에 모으고 CIS·PCI 같은 <b>기준 대비 점검</b>을 해줍니다. <b>AWS Config</b>는 설정 변경 이력과 규칙 위반을 봅니다.</p><p><b>한계를 알아야 합니다.</b> 관리형 탐지는 <b>일반적인 위협</b>에 강하고 <b>우리 조직에 특화된 것</b>에는 약합니다. '우리 회사에선 이 버킷을 이 역할만 읽어야 한다' 같은 건 직접 룰을 짜야 합니다. 그리고 <b>비용이 로그량에 비례</b>해 커집니다.</p><p>실무에서는 <b>관리형으로 바닥을 깔고, 조직 특화 탐지를 그 위에 얹는</b> 구성이 표준입니다.</p>",
lab:["GuardDuty 탐지 유형(Finding types) 목록 문서를 열어 카테고리를 훑는다","<code>InstanceCredentialExfiltration</code> 탐지가 왜 강력한지 원리를 정리한다","GuardDuty 샘플 탐지(Generate sample findings)를 만들어 어떻게 보이는지 확인한다","Security Hub의 CIS 벤치마크 항목 5개를 읽어본다","'관리형 탐지가 못 잡는 것' 3가지를 적어본다","관리형 탐지 + 커스텀 룰의 역할 분담을 그림으로 그린다"],
q:[{t:"GuardDuty의 <code>InstanceCredentialExfiltration</code> 탐지가 강력한 이유는?",o:["암호화를 해제해서","EC2 역할의 임시 자격증명이 해당 인스턴스 밖의 IP에서 사용되면 탈취가 거의 확실하기 때문","비용이 저렴해서","실시간이 아니라서"],a:1,e:"EC2 역할 자격증명은 <b>그 인스턴스 안에서만</b> 쓰여야 정상입니다. 다른 IP에서 같은 자격증명이 보이면 메타데이터에서 훔쳐 나갔다는 뜻이라 오탐이 거의 없습니다."},
{t:"관리형 탐지 서비스만으로 충분하지 않은 이유는?",o:["정확도가 낮아서","조직 고유의 정상 기준(어느 역할이 어느 자원을 써야 하는지)은 알 수 없어 특화 탐지를 대신할 수 없기 때문","로그를 못 읽어서","실시간이 아니라서"],a:1,e:"관리형은 <b>보편적 악성 패턴</b>에 최적화돼 있습니다. '우리 환경의 정상'은 우리만 알기 때문에, ACT 4에서 배운 탐지 엔지니어링이 클라우드에서도 그대로 필요합니다."}]},

{d:106,t:"S3와 데이터 노출",g:"가장 흔한 클라우드 사고 유형을 이해하고 점검한다.",
c:"<p>'S3 버킷이 공개로 열려 있었다'는 뉴스는 지금도 계속 나옵니다. 왜 반복될까요.</p><p>접근 제어 경로가 <b>여러 겹</b>이기 때문입니다. <b>버킷 정책</b>(리소스 기반), <b>IAM 정책</b>(주체 기반), <b>ACL</b>(구식), <b>퍼블릭 액세스 차단</b>(계정·버킷 단위 오버라이드), <b>VPC 엔드포인트 정책</b>. 하나만 잘못 열려도 노출됩니다.</p><p>위험 패턴: 버킷 정책의 <code>\"Principal\": \"*\"</code>, ACL의 <code>AllUsers</code>/<code>AuthenticatedUsers</code>(후자는 <b>AWS 계정 아무나</b>라는 뜻이라 사실상 공개), 그리고 <b>정적 웹사이트 호스팅을 켠 채 민감 파일을 둔 경우</b>.</p><p>방어의 정석은 <b>계정 수준 Public Access Block을 켜는 것</b>입니다. 개별 버킷 설정 실수를 통째로 무력화합니다. 여기에 기본 암호화, 버전 관리, <b>객체 잠금</b>(랜섬웨어 대비), 액세스 로깅을 더합니다.</p><p>탐지 관점: <b><code>PutBucketPolicy</code>, <code>PutBucketAcl</code>, <code>DeletePublicAccessBlock</code></b> 은 즉시 알럿감입니다. 그리고 <code>GetObject</code>가 평소보다 <b>수백 배 튀면</b> 대량 유출입니다.</p>",
lab:["<code>aws s3api get-public-access-block --bucket &lt;버킷&gt;</code> 으로 차단 설정을 확인한다","<code>aws s3api get-bucket-policy --bucket &lt;버킷&gt;</code> 로 정책을 읽고 Principal을 확인한다","<code>\"Principal\": \"*\"</code> 가 들어간 정책이 왜 위험한지 3줄로 적는다","계정 수준 Public Access Block을 켜는 방법을 조사해 정리한다","<code>PutBucketPolicy</code> 를 탐지하는 CloudTrail 조건을 작성한다","'객체 잠금(Object Lock)'이 랜섬웨어 방어에 어떻게 쓰이는지 조사한다"],
q:[{t:"S3 ACL의 <code>AuthenticatedUsers</code> 그룹이 위험한 이유는?",o:["내 계정 사용자만 접근 가능해서","'인증된 모든 AWS 계정'을 의미하므로 사실상 전 세계 누구나 접근할 수 있기 때문","암호화가 해제되어서","비용이 증가해서"],a:1,e:"이름이 오해를 부릅니다. <b>내 조직 사용자가 아니라 AWS 계정을 가진 아무나</b>입니다. AWS 계정은 누구나 무료로 만들 수 있으니 사실상 공개와 같습니다."},
{t:"개별 버킷의 설정 실수를 계정 차원에서 한 번에 막는 기능의 이름을 입력하세요.",k:"input",ph:"기능 이름 (한글 또는 영문)",a:["Public Access Block","퍼블릭 액세스 차단","Block Public Access"],re:["public\\s*access\\s*block","block\\s*public\\s*access","퍼블릭\\s*액세스\\s*차단"],hint:"S3 콘솔 계정 설정에 있는 '퍼블릭 ...' 항목입니다.",e:"<b>계정 수준 Public Access Block</b>을 켜두면 누가 실수로 버킷을 공개해도 무시됩니다. 사람의 실수를 <b>구조로 막는</b> 대표적인 가드레일이라, 신규 계정에서 가장 먼저 켜야 할 설정입니다."}]},

{d:107,t:"자격증명 유출 대응",g:"키가 새어나갔을 때의 대응 순서를 손에 익힌다.",
c:"<p>깃허브에 키가 올라갔습니다. 몇 분 안에 스캐너가 찾아냅니다. <b>순서가 중요합니다.</b></p><p><b>① 비활성화가 먼저, 삭제는 나중.</b> <code>aws iam update-access-key --status Inactive</code>. 바로 삭제하면 <b>그 키로 뭘 했는지 추적이 어려워집니다.</b> 비활성화는 즉시 차단하면서 흔적은 남깁니다.</p><p><b>② 그 키의 전체 행적 조사.</b> CloudTrail에서 <code>accessKeyId</code> 로 필터링해 <b>유출 시점부터 지금까지</b> 전부 훑습니다.</p><p><b>③ 공격자가 심어둔 지속성 제거.</b> 새로 만들어진 <b>액세스 키</b>, <b>IAM 사용자</b>, <b>역할과 신뢰 정책</b>, <b>람다 함수</b>, <b>EC2 인스턴스</b>. 원래 키만 끄고 끝내면 <b>공격자가 만든 두 번째 키로 계속 들어옵니다.</b></p><p><b>④ 영향 범위 산정.</b> 어떤 데이터에 접근했는가 — 여기서 데이터 이벤트가 켜져 있었는지가 갈립니다.</p><p><b>⑤ 재발 방지.</b> 키를 아예 없애고 역할로 전환, <code>git-secrets</code>·pre-commit 훅, 시크릿 매니저 도입.</p><p><b>깃 히스토리 주의:</b> 커밋을 되돌려도 <b>히스토리에는 남습니다.</b> 반드시 키를 무효화해야 하고, 파일만 지우는 건 대응이 아닙니다.</p>",
lab:["<code>aws iam list-access-keys --user-name &lt;이름&gt;</code> 로 키 목록과 생성일을 본다","<code>aws iam get-access-key-last-used --access-key-id &lt;키&gt;</code> 로 마지막 사용 정보를 확인한다","유출 대응 5단계를 체크리스트 파일로 만든다","<code>git-secrets</code> 또는 <code>gitleaks</code> 를 설치해 자기 저장소를 스캔해본다","'키를 지우기 전에 비활성화하는 이유'를 3줄로 적는다","AWS Secrets Manager 또는 SSM Parameter Store 사용법을 문서로 훑는다"],
q:[{t:"유출된 액세스 키를 발견했을 때 즉시 삭제하기보다 비활성화를 먼저 권하는 이유는?",o:["삭제가 느려서","즉시 차단하면서도 그 키가 무엇을 했는지 조사할 단서를 남기기 위해","비용 때문에","삭제 권한이 없어서"],a:1,e:"비활성화는 <b>차단과 추적을 동시에</b> 만족시킵니다. 다만 조사가 끝나면 반드시 삭제하고, 무엇보다 <b>공격자가 새로 만든 자격증명</b>이 없는지 확인해야 합니다."},
{t:"유출된 키를 비활성화한 뒤에도 침해가 계속되는 가장 흔한 이유는?",o:["비활성화가 반영되지 않아서","공격자가 이미 새 액세스 키·IAM 사용자·역할 등 별도의 지속성을 심어두었기 때문","네트워크 캐시 때문","리전이 달라서"],a:1,e:"ACT 1·3에서 배운 <b>지속성</b> 개념이 클라우드에도 그대로 있습니다. 원래 키만 끄는 건 절반이고, <b>새로 생성된 주체 전수 조사</b>가 나머지 절반입니다."}]},

{d:108,t:"VPC Flow Logs",g:"클라우드 네트워크에서 통신 흔적을 읽는다.",
c:"<p>ACT 2에서 NetFlow를 배웠습니다. AWS판이 <b>VPC Flow Logs</b>입니다. 페이로드는 없고 <b>연결 메타데이터</b>만 남습니다.</p><p>필드 순서(기본 포맷): <code>version account-id interface-id srcaddr dstaddr srcport dstport protocol packets bytes start end action log-status</code>. 여기서 <b><code>action</code></b>이 <code>ACCEPT</code>인지 <code>REJECT</code>인지가 핵심입니다.</p><p>탐지에 쓰는 패턴:<br>• <b>REJECT 폭증</b> — 스캔이나 잘못된 보안그룹 설정<br>• <b>비정상 아웃바운드</b> — 인스턴스가 낯선 외부 IP로 나감(C2·유출)<br>• <b>대량 바이트 전송</b> — <code>bytes</code> 필드가 튀는 흐름<br>• <b>인스턴스 간 예상치 못한 통신</b> — 내부 이동<br>• <b><code>169.254.169.254</code> 관련 이상</b> — 메타데이터 서비스 접근 (SSRF 흔적)</p><p><b>한계도 분명합니다.</b> DNS 질의 내용, HTTP 경로, TLS SNI는 안 보입니다. 그래서 실무에서는 <b>Flow Logs + Route53 Resolver 쿼리 로그 + ALB 액세스 로그</b>를 함께 씁니다. ACT 2에서 배운 <b>계층별 가시성</b> 개념이 그대로입니다.</p>",
lab:["VPC Flow Logs 기본 포맷 14개 필드를 순서대로 적어본다","<code>REJECT</code> 만 골라내는 CloudWatch Logs Insights 쿼리를 작성한다","출발지별 REJECT 개수 Top 10 을 뽑는 쿼리를 만든다","바이트 수 기준 상위 흐름을 찾는 쿼리를 만든다","<code>169.254.169.254</code> 로 향하는 트래픽이 왜 의심스러운지 정리한다","Flow Logs로 '볼 수 없는 것' 4가지를 적어본다"],
term:"# CloudWatch Logs Insights\nfields srcAddr, dstAddr, dstPort, action, bytes\n| filter action = \"<b>REJECT</b>\"\n| stats count(*) as tries by srcAddr\n| sort tries desc | limit 10",
q:[{t:"VPC Flow Logs에서 확인할 수 <b>없는</b> 것은?",o:["출발지·목적지 IP","전송된 바이트 수","HTTP 요청 경로와 페이로드","ACCEPT/REJECT 여부"],a:2,e:"Flow Logs는 <b>메타데이터만</b> 담습니다. 페이로드·URL·DNS 질의 내용은 없어서, ALB 로그나 Resolver 쿼리 로그를 함께 봐야 그림이 완성됩니다."},
{t:"EC2 인스턴스에서 <code>169.254.169.254</code> 로 향하는 예상치 못한 접근이 관측됐다. 의심할 것은?",o:["정상적인 시간 동기화","SSRF 등을 통한 인스턴스 메타데이터 접근 — 역할 자격증명 탈취 시도","DNS 조회","로드밸런서 헬스체크"],a:1,e:"<code>169.254.169.254</code>는 <b>인스턴스 메타데이터 서비스</b>입니다. 여기서 역할의 임시 자격증명을 꺼낼 수 있어, SSRF 취약점과 결합하면 클라우드 계정 침해로 직행합니다. IMDSv2 강제가 방어책입니다."}]},

{d:109,t:"서버리스 보안",g:"Lambda 같은 함수 실행 환경의 위협을 이해한다.",
c:"<p>서버가 없으니 안전할까요? <b>공격면이 옮겨갔을 뿐입니다.</b></p><p><b>주요 위협:</b><br>• <b>과도한 실행 역할</b> — 함수 하나가 <code>s3:*</code> 를 갖고 있으면 그 함수를 뚫는 순간 전체 버킷이 털립니다. <b>서버리스에서 가장 흔한 문제</b>입니다.<br>• <b>이벤트 인젝션</b> — 함수 입력이 곧 신뢰 경계입니다. API Gateway·S3 이벤트·SQS 메시지가 전부 입력이고, 검증 없이 쓰면 인젝션이 됩니다.<br>• <b>의존성 취약점</b> — 배포 패키지에 딸려 들어간 라이브러리.<br>• <b>환경변수에 시크릿 평문 저장</b> — 함수 설정을 읽을 수 있으면 그대로 노출됩니다.<br>• <b>실행 시간 과금 남용</b> — 무한 루프나 대량 호출로 비용 폭탄.</p><p><b>가시성 확보:</b> CloudTrail의 <code>Invoke</code>는 기본으로 안 남습니다(데이터 이벤트). CloudWatch Logs에 함수 로그가 쌓이므로 <b>애플리케이션 레벨 로깅을 직접 잘 짜야</b> 합니다. 그리고 함수는 <b>수 초 만에 사라져</b> 포렌식이 사실상 불가능합니다. <b>남긴 로그가 전부입니다.</b></p><p>탐지 지표: 함수 코드 변경(<code>UpdateFunctionCode</code>), 실행 역할 변경(<code>UpdateFunctionConfiguration</code>), 새 함수 생성 — 특히 <b>지속성 수단</b>으로 쓰입니다.</p>",
lab:["<code>aws lambda list-functions</code> 로 함수 목록과 실행 역할을 확인한다","함수 하나의 실행 역할 정책을 열어 최소 권한인지 점검한다","<code>UpdateFunctionCode</code> 를 탐지하는 CloudTrail 조건을 작성한다","'함수가 몇 초 만에 사라진다'는 점이 포렌식에 주는 영향을 3줄로 적는다","환경변수 대신 Secrets Manager를 쓰는 방법을 조사한다","OWASP Serverless Top 10 문서를 훑어본다"],
q:[{t:"서버리스 환경에서 가장 흔하게 지적되는 보안 문제는?",o:["네트워크 방화벽 부재","함수 실행 역할에 과도한 권한이 부여되는 것","암호화 미적용","패치 관리"],a:1,e:"<b>과도한 실행 역할</b>입니다. 함수 하나가 뚫리면 그 역할이 가진 모든 권한이 공격자 것이 됩니다. 함수마다 <b>딱 필요한 권한만</b> 주는 것이 서버리스 보안의 1원칙입니다."},
{t:"Lambda 함수 침해 조사가 특히 어려운 이유는?",o:["로그가 암호화되어서","실행 환경이 수 초 만에 사라져 사후 수집이 불가능하고, 사전에 남긴 로그가 유일한 증거이기 때문","권한이 없어서","리전이 분산되어서"],a:1,e:"ACT 5의 <b>휘발성 순서</b> 개념의 극단적 사례입니다. 메모리도 디스크도 남지 않으므로, <b>사전에 무엇을 로깅했는가</b>가 조사 가능 여부를 100% 결정합니다."}]},

{d:110,t:"컨테이너 이미지 보안",g:"이미지에 무엇이 들어있는지 확인하고 관리한다.",
c:"<p>도커를 조금 쓰신다니 여기서 제대로 정리합니다. 컨테이너 보안은 <b>이미지</b>와 <b>런타임</b> 두 축인데, 오늘은 이미지입니다.</p><p><b>이미지는 레이어의 쌓임입니다.</b> 여기서 중요한 함정 — <b>레이어에서 파일을 지워도 이전 레이어에는 남아 있습니다.</b> <code>RUN wget secret.txt && rm secret.txt</code> 는 시크릿을 지운 게 아닙니다. <code>docker history</code>나 레이어 추출로 그대로 꺼낼 수 있습니다.</p><p><b>점검 항목:</b><br>• <b>베이스 이미지</b> — <code>latest</code> 태그 금지, 다이제스트로 고정. 가능하면 <code>-slim</code>·<code>alpine</code>·distroless<br>• <b>취약점 스캔</b> — <b>Trivy</b>, Grype, Clair<br>• <b>시크릿 포함 여부</b> — Trivy가 이것도 잡습니다<br>• <b>불필요한 도구</b> — 이미지에 <code>curl</code>·<code>bash</code>가 있으면 공격자도 씁니다<br>• <b>root 실행</b> — <code>USER</code> 지시자로 비root 지정<br>• <b>이미지 서명</b> — Cosign/Sigstore<br>• <b>SBOM</b> — 무엇이 들어있는지 목록. 새 CVE가 터졌을 때 <b>영향 범위를 몇 분 만에</b> 알 수 있습니다</p><p>실습 서버가 리눅스라니 오늘 내용은 바로 해볼 수 있습니다.</p>",
lab:["<code>docker history &lt;이미지&gt;</code> 로 레이어별 명령을 확인한다","Trivy를 설치하고 <code>trivy image &lt;이미지&gt;</code> 로 취약점을 스캔한다","<code>trivy image --scanners secret &lt;이미지&gt;</code> 로 시크릿 포함 여부를 검사한다","<code>docker inspect &lt;이미지&gt; | grep -i user</code> 로 root 실행 여부를 확인한다","<code>trivy image --format cyclonedx -o sbom.json &lt;이미지&gt;</code> 로 SBOM을 뽑아본다","자기 Dockerfile에 <code>USER</code> 지시자가 있는지 확인하고 없으면 추가한다"],
term:"$ trivy image nginx:1.21\nTotal: <b>142</b> (UNKNOWN: 0, LOW: 78, MEDIUM: 41, <b>HIGH: 19, CRITICAL: 4</b>)\n$ trivy image --scanners secret myapp:latest\n<b>AWS Access Key ID</b>  /app/.env  (HIGH)",
q:[{t:"Dockerfile에서 <code>RUN wget key.pem && rm key.pem</code> 로 파일을 지웠다. 이 시크릿은 안전한가?",o:["안전하다. 삭제되었다","안전하지 않다. 이전 레이어에 그대로 남아 추출 가능하다","이미지를 다시 빌드하면 안전하다","암호화되어 있어 안전하다"],a:1,e:"<b>레이어는 누적</b>됩니다. 나중 레이어의 삭제는 이전 레이어를 지우지 못합니다. 멀티스테이지 빌드나 <code>--mount=type=secret</code> 을 써야 하고, 이미 빌드했다면 <b>그 시크릿은 유출된 것으로 간주</b>해야 합니다."},
{t:"컨테이너 이미지의 취약점을 스캔하는 대표적인 도구 이름을 입력하세요.",k:"input",ph:"도구 이름",a:["Trivy","trivy","Grype","grype","Clair"],re:["^(trivy|grype|clair|snyk|anchore)$"],hint:"Aqua Security가 만든, 이름이 짧은 오픈소스 스캐너가 가장 널리 쓰입니다.",e:"<b>Trivy</b>가 사실상 표준입니다. 취약점뿐 아니라 시크릿·설정 오류·SBOM 생성까지 한 도구로 되고, CI에 한 줄로 붙일 수 있어 채택률이 높습니다."}]},

{d:111,t:"컨테이너 런타임 보안",g:"돌고 있는 컨테이너의 위험한 설정을 찾아낸다.",
c:"<p>컨테이너는 <b>가상머신이 아닙니다.</b> 호스트 커널을 공유하므로, 격리는 커널 기능(네임스페이스·cgroups·capabilities·seccomp)에 의존합니다. 이 격리를 헐겁게 만드는 설정이 위험합니다.</p><p><b>위험 설정 목록 — 전부 외워두세요:</b><br>• <b><code>--privileged</code></b> — 거의 모든 격리를 해제합니다. <b>사실상 호스트 root</b>입니다.<br>• <b><code>-v /var/run/docker.sock:...</code></b> — 컨테이너 안에서 도커를 조종할 수 있어 <b>탈출 확정</b>입니다.<br>• <b><code>-v /:/host</code></b> — 호스트 파일시스템 마운트.<br>• <b><code>--pid=host</code> / <code>--net=host</code></b> — 네임스페이스 공유.<br>• <b><code>--cap-add=SYS_ADMIN</code></b> — 위험한 capability.<br>• <b>root 실행</b> — <code>USER</code> 미지정.</p><p><b>방어 설정:</b> <code>--read-only</code>, <code>--cap-drop=ALL</code> 후 필요한 것만 추가, <code>--security-opt=no-new-privileges</code>, seccomp/AppArmor 프로필, 리소스 제한(<code>--memory</code>, <code>--cpus</code>), 비root 사용자.</p><p>우리가 만든 이 사이트의 Dockerfile도 <code>USER app</code> 으로 비root 실행이고 <code>/data</code> 볼륨만 씁니다. <b>직접 확인해보세요.</b></p>",
lab:["<code>docker ps --format '{{.Names}}'</code> 로 돌고 있는 컨테이너를 나열한다","<code>docker inspect --format '{{.Name}} privileged={{.HostConfig.Privileged}}' $(docker ps -q)</code> 실행","<code>docker inspect --format '{{.Name}} {{.HostConfig.Binds}}' $(docker ps -q)</code> 로 마운트를 점검한다","docker.sock 이 마운트된 컨테이너가 있는지 확인한다","이 사이트의 Dockerfile을 열어 <code>USER</code> 지시자를 확인한다","<code>docker run --cap-drop=ALL --read-only</code> 로 컨테이너를 하나 띄워본다"],
q:[{t:"컨테이너에 <code>/var/run/docker.sock</code> 을 마운트하는 것이 위험한 이유는?",o:["디스크를 많이 써서","컨테이너 안에서 호스트의 도커 데몬을 제어할 수 있어 사실상 호스트 장악과 동등하기 때문","로그가 안 남아서","네트워크가 느려져서"],a:1,e:"도커 소켓에 접근하면 <b>새 특권 컨테이너를 만들어 호스트 루트를 마운트</b>할 수 있습니다. CI 도구에서 흔히 쓰는 구성이라 실무에서 자주 발견되는 위험입니다."},
{t:"컨테이너의 모든 리눅스 capability 를 제거하는 docker run 옵션을 입력하세요.",k:"cmd",ph:"--...",a:["--cap-drop=ALL","--cap-drop ALL"],re:["^--cap-drop[=\\s]+all$"],hint:"capability 를 '떨어뜨리는(drop)' 옵션에 ALL 을 줍니다.",e:"<b><code>--cap-drop=ALL</code></b> 로 전부 없앤 뒤 꼭 필요한 것만 <code>--cap-add</code> 로 되돌리는 것이 최소 권한의 정석입니다. 대부분의 애플리케이션은 capability 없이도 잘 돕니다."}]},

{d:112,t:"컨테이너 탈출 탐지",g:"컨테이너 밖으로 빠져나가는 시도를 포착한다.",
c:"<p>공격자가 컨테이너 안에 들어왔습니다. 다음 목표는 <b>호스트</b>입니다.</p><p><b>탈출 경로:</b> ① 특권 컨테이너에서 호스트 디바이스 마운트 ② 도커 소켓 악용 ③ <code>SYS_ADMIN</code> + cgroup <code>release_agent</code> 악용 ④ 커널 취약점(Dirty Pipe·Dirty COW 등) ⑤ 호스트 경로 마운트 오용 ⑥ <code>/proc</code> 노출.</p><p><b>탐지 지표:</b><br>• 컨테이너 안에서 <code>mount</code>, <code>nsenter</code>, <code>unshare</code>, <code>modprobe</code> 실행<br>• <code>/proc/1/cgroup</code>, <code>/proc/self/status</code> 읽기 — <b>공격자가 '내가 컨테이너 안인가'를 확인하는 행동</b><br>• <code>/var/run/docker.sock</code> 접근<br>• 컨테이너에서 호스트 파일시스템 경로 쓰기<br>• 예상 밖의 프로세스 — 웹 컨테이너에서 <code>bash</code>·<code>curl</code>·<code>nc</code></p><p><b>도구는 Falco</b>입니다. 커널 시스템콜을 보고 규칙으로 판단하는 런타임 탐지기이고, 기본 규칙만으로도 위 항목 상당수를 잡습니다. 대표 규칙: <code>Terminal shell in container</code>, <code>Write below etc</code>, <code>Launch Privileged Container</code>.</p><p><b>가장 실용적인 단일 탐지는 '컨테이너 안에서 인터랙티브 셸이 뜨는 것'입니다.</b> 정상 운영에서 거의 없고, 있으면 사람이 들어갔거나 공격자입니다.</p>",
lab:["<code>cat /proc/1/cgroup</code> 을 호스트와 컨테이너 안에서 각각 실행해 차이를 본다","<code>docker run --rm -it alpine sh</code> 로 컨테이너에 들어가 <code>mount</code> 를 쳐본다","Falco 문서의 기본 규칙 목록에서 5개를 골라 무엇을 잡는지 정리한다","'컨테이너 안 인터랙티브 셸' 탐지가 왜 가성비 좋은지 3줄로 적는다","탈출 경로 6가지를 표로 정리한다 (전제 조건 / 탐지 지표)","자기 서버에서 특권 컨테이너가 돌고 있는지 점검한다"],
q:[{t:"컨테이너 안에서 <code>/proc/1/cgroup</code> 을 읽는 행위가 의심스러운 이유는?",o:["시스템이 느려져서","공격자가 자신이 컨테이너 안에 있는지 확인하는 전형적인 정찰 행동이기 때문","파일이 손상되어서","권한이 필요해서"],a:1,e:"침입한 뒤 <b>'여기가 어디인가'를 파악</b>하는 단계입니다. ACT 3의 Discovery 와 같은 개념이고, 이 확인 직후 탈출 시도가 이어지는 경우가 많습니다."},
{t:"컨테이너 런타임 위협 탐지에 널리 쓰이는 오픈소스 도구 이름을 입력하세요.",k:"input",ph:"도구 이름",a:["Falco","falco"],re:["^falco$"],hint:"CNCF 프로젝트이고, 이름이 매(falcon)와 비슷한 네 글자입니다.",e:"<b>Falco</b>는 커널 시스템콜을 관찰해 규칙으로 판정합니다. 컨테이너·쿠버네티스 환경의 사실상 표준 런타임 탐지기이며, Sysmon이 윈도우에서 하는 역할을 리눅스 컨테이너에서 합니다."}]},

{d:113,t:"쿠버네티스 공격 표면",g:"K8s 구조를 이해하고 어디가 뚫리는지 파악한다.",
c:"<p>컨테이너가 많아지면 쿠버네티스로 갑니다. <b>구성 요소부터</b> 잡읍시다.</p><p><b>컨트롤 플레인:</b> <code>kube-apiserver</code>(모든 요청의 관문 — <b>여기가 뚫리면 끝</b>), <code>etcd</code>(전체 상태와 <b>시크릿 저장소</b>), <code>kube-scheduler</code>, <code>controller-manager</code>.<br><b>노드:</b> <code>kubelet</code>(노드 에이전트), <code>kube-proxy</code>, 컨테이너 런타임.</p><p><b>주요 공격 표면:</b><br>• <b>노출된 API 서버</b> — 익명 접근이 허용된 경우<br>• <b>etcd 무인증 노출</b> — 시크릿이 평문으로 들어 있습니다<br>• <b>kubelet 읽기/쓰기 포트(10250)</b> — 노드의 모든 파드에서 명령 실행 가능<br>• <b>과도한 RBAC</b> — 내일 다룹니다<br>• <b>서비스 어카운트 토큰</b> — 파드 안 <code>/var/run/secrets/kubernetes.io/serviceaccount/token</code>. <b>파드를 뚫으면 이 토큰부터 봅니다</b><br>• <b>특권 파드 허용</b><br>• <b>컨테이너 레지스트리 침해</b> — 공급망</p><p><b>가장 중요한 로그는 API 서버 감사 로그</b>입니다. 클라우드의 CloudTrail과 정확히 같은 위치를 차지합니다. 모레 다룹니다.</p>",
lab:["minikube 또는 k3s 로 로컬 클러스터를 하나 띄운다 (리눅스 서버면 <code>curl -sfL https://get.k3s.io | sh -</code>)","<code>kubectl get pods -A</code> 로 전체 파드를 본다","<code>kubectl get pods -o jsonpath='{.items[*].spec.serviceAccountName}'</code> 로 서비스 어카운트를 확인한다","파드에 들어가 <code>cat /var/run/secrets/kubernetes.io/serviceaccount/token</code> 을 확인한다","<code>kubectl auth can-i --list</code> 로 내 권한을 나열한다","컨트롤 플레인 구성 요소 5개의 역할을 표로 정리한다"],
q:[{t:"쿠버네티스에서 모든 요청이 거쳐 가며, 침해 시 클러스터 전체가 장악되는 구성 요소는?",o:["kubelet","kube-apiserver","kube-proxy","kube-scheduler"],a:1,e:"<b>API 서버</b>가 유일한 관문입니다. 인증·인가·감사가 전부 여기서 일어나므로, <b>감사 로그도 여기서 나옵니다</b>. 클라우드의 CloudTrail과 같은 위상입니다."},
{t:"파드를 침해한 공격자가 가장 먼저 노리는 클러스터 자격증명이 담긴 경로는?",o:["/etc/kubernetes/admin.conf","/var/run/secrets/kubernetes.io/serviceaccount/token","/root/.kube/config","/var/lib/etcd"],a:1,e:"파드에는 서비스 어카운트 토큰이 <b>자동으로 마운트</b>됩니다. 이 토큰의 RBAC 권한이 넓으면 파드 하나 침해가 클러스터 장악으로 번집니다. 필요 없으면 <code>automountServiceAccountToken: false</code> 로 꺼야 합니다."}]},

{d:114,t:"쿠버네티스 RBAC",g:"클러스터 권한 모델을 읽고 위험한 조합을 찾는다.",
c:"<p>K8s RBAC은 네 가지 객체로 이뤄집니다.</p><p><b>Role</b>(네임스페이스 범위) / <b>ClusterRole</b>(클러스터 범위) — 무엇을 할 수 있는지.<br><b>RoleBinding</b> / <b>ClusterRoleBinding</b> — 누구에게 줄지.</p><p>규칙은 <code>apiGroups</code> + <code>resources</code> + <code>verbs</code>(<code>get</code>, <code>list</code>, <code>watch</code>, <code>create</code>, <code>update</code>, <code>delete</code>, <code>*</code>)로 씁니다.</p><p><b>위험한 권한 — 이게 오늘의 핵심입니다:</b><br>• <b><code>secrets: get/list</code></b> — 클러스터의 모든 비밀을 읽습니다<br>• <b><code>pods/exec</code>, <code>pods/attach</code></b> — 남의 파드에서 명령 실행<br>• <b><code>pods: create</code></b> — <b>특권 파드를 만들어 노드를 장악</b>할 수 있습니다<br>• <b><code>bind</code>, <code>escalate</code></b> — 자기 권한을 스스로 올립니다<br>• <b><code>nodes/proxy</code></b> — kubelet API 직접 호출<br>• <b><code>cluster-admin</code> 바인딩</b> — 말할 것도 없습니다</p><p><b>흔한 실수:</b> <code>system:authenticated</code> 그룹에 뭔가를 바인딩하는 것. 그건 <b>인증된 모든 주체</b>라서 S3의 <code>AuthenticatedUsers</code>와 똑같은 함정입니다.</p><p>점검 도구: <b>kubectl-who-can</b>, <b>rbac-tool</b>, <b>KubiScan</b>.</p>",
lab:["<code>kubectl get clusterrolebindings -o wide</code> 로 누가 무엇에 바인딩됐는지 본다","<code>kubectl auth can-i --list --as=system:serviceaccount:default:default</code> 실행","<code>kubectl get clusterrole cluster-admin -o yaml</code> 을 열어 규칙을 확인한다","<code>secrets</code> 를 읽을 수 있는 주체를 전부 찾아본다","위험 권한 6가지를 표로 정리한다 (권한 / 가능한 공격)","<code>system:authenticated</code> 에 바인딩된 것이 있는지 점검한다"],
term:"$ kubectl get clusterrolebindings -o json | jq -r '\n  .items[] | select(.roleRef.name==\"<b>cluster-admin</b>\") |\n  \"\\(.metadata.name)  <-  \\(.subjects[]?.kind)/\\(.subjects[]?.name)\"'",
q:[{t:"K8s에서 <code>pods: create</code> 권한만으로도 노드 장악이 가능한 이유는?",o:["파드가 자동으로 root로 실행되어서","특권 파드나 호스트 경로를 마운트한 파드를 만들어 노드 파일시스템에 접근할 수 있기 때문","네트워크가 격리되지 않아서","kubelet이 인증하지 않아서"],a:1,e:"파드 스펙에 <code>privileged: true</code> 나 <code>hostPath: /</code> 를 넣으면 노드를 그대로 만집니다. 그래서 <b>파드 생성 권한은 사실상 노드 권한</b>이고, Pod Security 정책으로 막아야 합니다."},
{t:"<code>system:authenticated</code> 그룹에 권한을 바인딩하면 안 되는 이유는?",o:["존재하지 않는 그룹이라서","인증에 성공한 모든 주체를 의미해 사실상 광범위한 권한 부여가 되기 때문","성능이 저하되어서","네임스페이스가 달라서"],a:1,e:"S3의 <code>AuthenticatedUsers</code>와 같은 함정입니다. <b>'인증됨'은 '허가됨'이 아닙니다.</b> 이름만 보고 안전하다고 착각하기 쉬워 실무에서 반복되는 실수입니다."}]},

{d:115,t:"쿠버네티스 감사 로그",g:"API 서버 감사 로그로 클러스터에서 벌어진 일을 재구성한다.",
c:"<p>K8s 감사 로그는 <b>API 서버로 들어온 모든 요청</b>을 남깁니다. 클라우드의 CloudTrail, 윈도우의 Security 로그와 같은 자리입니다.</p><p><b>감사 정책의 4단계 레벨:</b> <code>None</code>(기록 안 함), <code>Metadata</code>(누가·언제·무엇을), <code>Request</code>(+요청 본문), <code>RequestResponse</code>(+응답 본문).</p><p><b>실무 설정 요령:</b> 전부 <code>RequestResponse</code>로 하면 로그가 폭발합니다. <b>민감한 리소스만 상세히</b> — <code>secrets</code>·<code>configmaps</code>는 Metadata(본문에 비밀이 들어가므로 오히려 낮게!), <code>pods/exec</code>·RBAC 변경은 RequestResponse.</p><p><b>이벤트 핵심 필드:</b> <code>user.username</code>, <code>user.groups</code>, <code>verb</code>, <code>objectRef</code>(resource·namespace·name), <code>responseStatus.code</code>, <code>sourceIPs</code>, <code>userAgent</code>, <code>stage</code>.</p><p><b>탐지할 것:</b><br>• <b><code>pods/exec</code></b> — 파드에 셸로 들어감. <b>가장 중요한 단일 지표</b><br>• <code>secrets</code> 대량 <code>list</code>/<code>get</code><br>• ClusterRoleBinding 생성·수정<br>• 익명 사용자(<code>system:anonymous</code>) 요청<br>• <code>403</code> 폭증 — 권한 열거 (CloudTrail의 AccessDenied와 같은 신호)<br>• 특권 파드 생성</p>",
lab:["감사 정책 YAML 예제를 찾아 레벨별 설정을 읽어본다","<code>pods/exec</code> 를 RequestResponse 로 기록하는 정책 규칙을 직접 써본다","<code>kubectl exec</code> 를 한 번 실행하고 감사 로그에서 그 이벤트를 찾아본다","<code>responseStatus.code == 403</code> 인 이벤트를 세는 jq 명령을 만든다","<code>secrets</code> 를 왜 Metadata 레벨로 낮춰야 하는지 정리한다","위 탐지 항목 6개를 Sigma 스타일 조건으로 스케치한다"],
q:[{t:"K8s 감사 정책에서 <code>secrets</code> 리소스를 <code>RequestResponse</code> 가 아닌 낮은 레벨로 설정하는 이유는?",o:["성능 문제만 있어서","요청·응답 본문에 비밀 값 자체가 평문으로 기록되어 감사 로그가 새로운 유출 경로가 되기 때문","감사가 지원되지 않아서","용량 제한 때문"],a:1,e:"<b>로그가 비밀을 담으면 로그 접근 권한이 곧 비밀 접근 권한</b>이 됩니다. 누가 언제 접근했는지(Metadata)만 남기고 값은 남기지 않는 것이 맞습니다."},
{t:"공격자가 파드 내부에서 명령을 실행했을 때 K8s 감사 로그에 남는 하위 리소스 이름을 입력하세요.",k:"input",ph:"리소스/하위리소스",a:["pods/exec","exec"],re:["^(pods/)?exec$"],hint:"<code>kubectl exec</code> 명령이 호출하는 API 경로 그대로입니다.",e:"<b>pods/exec</b> 는 정상 운영에서 드물고 침해 시에는 거의 반드시 나타나므로, 오탐 대비 적중률이 매우 높은 탐지입니다. ACT 3의 '컨테이너 안 인터랙티브 셸'과 같은 논리입니다."}]},

{d:116,t:"파드 보안과 어드미션 컨트롤",g:"위험한 워크로드를 배포 시점에 막는 구조를 만든다.",
c:"<p>탐지도 좋지만 <b>애초에 못 만들게 하는 게</b> 낫습니다. 그 장치가 <b>어드미션 컨트롤</b>입니다. API 서버가 객체를 저장하기 <b>전에</b> 검사해서 거부할 수 있습니다.</p><p><b>Pod Security Admission</b>(내장, PSP 후속)의 세 단계:<br>• <b>privileged</b> — 제한 없음<br>• <b>baseline</b> — 알려진 권한 상승만 차단<br>• <b>restricted</b> — 강하게 제한. 비root 실행 강제, 특권 금지, capability 드롭, seccomp 필수</p><p>네임스페이스 라벨로 적용합니다: <code>pod-security.kubernetes.io/enforce=restricted</code>. <code>warn</code>·<code>audit</code> 모드로 먼저 관찰한 뒤 <code>enforce</code>로 올리는 것이 안전한 순서입니다.</p><p>더 유연한 것이 필요하면 <b>OPA Gatekeeper</b>나 <b>Kyverno</b>입니다. '이미지는 우리 레지스트리에서만', '모든 파드에 소유팀 라벨 필수', '<code>latest</code> 태그 금지' 같은 <b>조직 고유 정책</b>을 코드로 강제할 수 있습니다.</p><p><b>실무 감각:</b> 처음부터 <code>enforce</code>를 걸면 배포가 전부 깨져서 팀이 반발합니다. <b>audit → warn → enforce</b> 순으로 몇 주에 걸쳐 올리세요. 이건 기술 문제가 아니라 <b>조직 변화 관리</b>입니다.</p>",
lab:["<code>kubectl label ns test pod-security.kubernetes.io/warn=restricted</code> 를 걸어본다","특권 파드를 배포해보고 경고가 뜨는지 확인한다","<code>enforce=restricted</code> 로 올린 뒤 같은 파드가 거부되는지 확인한다","Kyverno 또는 Gatekeeper 예제 정책 3개를 읽어본다","'latest 태그 금지' 정책을 직접 작성해본다","audit → warn → enforce 단계적 도입 계획을 5줄로 써본다"],
q:[{t:"Pod Security Admission 을 도입할 때 <code>enforce</code> 부터 적용하지 않고 <code>audit</code>·<code>warn</code> 을 먼저 쓰는 이유는?",o:["enforce 가 느려서","기존 워크로드가 얼마나 위반하는지 먼저 파악해야 배포 중단 없이 단계적으로 전환할 수 있기 때문","enforce 가 불안정해서","라이선스 때문"],a:1,e:"바로 강제하면 <b>기존 배포가 전부 깨집니다.</b> 관찰 → 경고 → 강제 순으로 올리며 팀이 고칠 시간을 주는 것이 실무 표준이고, 이건 탐지 룰을 튜닝하며 배포하는 과정과 똑같습니다."},
{t:"쿠버네티스에서 객체가 저장되기 전에 정책 위반 여부를 검사해 거부할 수 있는 메커니즘의 이름은?",o:["RBAC","어드미션 컨트롤(Admission Control)","네트워크 정책","서비스 메시"],a:1,e:"<b>어드미션 컨트롤</b>은 인증·인가 <b>이후, 저장 이전</b>에 동작합니다. RBAC이 '누가 할 수 있는가'라면 어드미션은 '무엇이 허용되는가'를 봅니다. 예방 통제의 핵심입니다."}]},

{d:117,t:"Falco로 런타임 탐지하기",g:"컨테이너 환경의 Sysmon을 직접 돌려본다.",
c:"<p>ACT 3에서 윈도우에 Sysmon을 깔았습니다. <b>리눅스 컨테이너의 Sysmon이 Falco</b>입니다.</p><p>동작 원리: 커널에서 <b>시스템콜</b>을 관찰(eBPF 또는 커널 모듈)하고, 규칙과 대조해 알럿을 냅니다. 컨테이너·쿠버네티스 메타데이터도 붙여줍니다.</p><p>규칙 구조는 Sigma와 비슷합니다:</p><p><code>- rule: Terminal shell in container<br>&nbsp;&nbsp;desc: 컨테이너 안에서 인터랙티브 셸이 떴다<br>&nbsp;&nbsp;condition: spawned_process and container and shell_procs and proc.tty != 0<br>&nbsp;&nbsp;output: 컨테이너 셸 (user=%user.name container=%container.name cmd=%proc.cmdline)<br>&nbsp;&nbsp;priority: NOTICE</code></p><p><b>매크로</b>(<code>spawned_process</code>)와 <b>리스트</b>로 조건을 재사용합니다. 필드는 <code>proc.name</code>, <code>proc.cmdline</code>, <code>fd.name</code>, <code>container.image.repository</code>, <code>k8s.pod.name</code> 등.</p><p><b>기본 규칙만으로도</b> 컨테이너 셸, <code>/etc</code> 쓰기, 민감 파일 읽기, 특권 컨테이너 실행, 아웃바운드 연결 등을 잡습니다. 여기에 <b>우리 환경 특화 규칙</b>을 더하면 됩니다.</p><p><b>튜닝은 필수입니다.</b> 기본 규칙은 오탐이 꽤 나옵니다. ACT 4에서 배운 그대로 — <b>알려진 정상을 예외로 빼면서</b> 다듬으세요.</p>",
lab:["Falco를 설치한다 (도커: <code>docker run --rm -i -t --privileged -v /var/run/docker.sock:/host/var/run/docker.sock falcosecurity/falco:latest</code>)","다른 창에서 <code>docker exec -it &lt;컨테이너&gt; sh</code> 를 실행해 알럿이 뜨는지 본다","<code>falco_rules.yaml</code> 을 열어 기본 규칙 5개를 읽는다","<code>Terminal shell in container</code> 규칙의 condition 을 한 줄씩 해설해본다","자기 환경에 맞는 커스텀 규칙을 하나 작성한다","오탐이 난 규칙에 예외(exception)를 추가해본다"],
q:[{t:"Falco가 위협을 탐지하는 근본 데이터 소스는?",o:["네트워크 패킷","커널 시스템콜","애플리케이션 로그","컨테이너 이미지 레이어"],a:1,e:"<b>시스템콜</b>을 관찰합니다. 애플리케이션이 무엇으로 짜였든 파일을 읽거나 프로세스를 만들려면 시스템콜을 거치므로, <b>우회하기 어려운 관측 지점</b>입니다."},
{t:"Falco 기본 규칙을 그대로 운영에 쓰면 생기는 문제는?",o:["아무것도 탐지하지 못한다","환경 특성상 정상인 활동까지 알럿이 나 알럿 피로가 생기므로 예외 튜닝이 필요하다","성능이 심각하게 저하된다","컨테이너가 중지된다"],a:1,e:"ACT 4 Day 72의 <b>오탐과 알럿 피로</b> 이야기가 그대로 적용됩니다. 도구가 바뀌어도 <b>튜닝 없는 탐지는 무용지물</b>이라는 원칙은 같습니다."}]},

{d:118,t:"IaC 보안",g:"인프라 코드 단계에서 위험 설정을 잡는다.",
c:"<p>클라우드 인프라는 이제 <b>코드</b>로 만듭니다(Terraform·CloudFormation·Pulumi). 그렇다면 <b>보안 검사도 코드 리뷰 단계에서</b> 할 수 있습니다. ACT 4의 Detection as Code와 같은 발상입니다.</p><p><b>왜 강력한가:</b> 배포 <b>전에</b> 잡습니다. 운영에서 발견하면 이미 노출된 상태지만, PR 단계에서 잡으면 <b>노출 자체가 없습니다.</b> 그리고 코드라서 <b>같은 실수가 반복되지 않게</b> 규칙으로 못 박을 수 있습니다.</p><p><b>도구:</b> <b>Checkov</b>, <b>tfsec</b>/Trivy, <b>Terrascan</b>, <b>KICS</b>. CI에 한 줄로 붙습니다.</p><p><b>대표 탐지 항목:</b> 공개 S3 버킷, 0.0.0.0/0 에 열린 보안그룹(특히 22·3389), 암호화 미적용, 로깅 비활성, 하드코딩된 시크릿, 과도한 IAM 정책, 퍼블릭 RDS.</p><p><b>드리프트도 중요합니다.</b> 코드는 안전한데 누가 콘솔에서 손으로 바꿔놓는 경우입니다. <code>terraform plan</code> 을 정기적으로 돌려 <b>실제 상태와 코드의 차이</b>를 감시하세요. 콘솔 수동 변경은 그 자체로 알럿감입니다.</p>",
lab:["Checkov 를 설치하고 <code>checkov -d .</code> 로 Terraform 코드를 스캔한다","일부러 <code>0.0.0.0/0</code> 에 22번을 연 보안그룹을 코드로 써보고 걸리는지 확인한다","Trivy 로 <code>trivy config .</code> 를 실행해본다","CI에서 IaC 스캔을 돌리는 워크플로를 작성한다","'드리프트'가 무엇이고 왜 위험한지 3줄로 정리한다","IaC 스캔에서 잡히는 대표 위험 7가지를 표로 만든다"],
q:[{t:"IaC 보안 스캔이 런타임 점검보다 유리한 점은?",o:["더 정확해서","배포 이전에 문제를 잡아 애초에 노출이 발생하지 않게 하기 때문","비용이 없어서","자동으로 수정해줘서"],a:1,e:"<b>예방이 탐지보다 앞섭니다.</b> 운영에서 공개 버킷을 발견하면 이미 노출된 뒤지만, PR에서 막으면 노출 시간이 0입니다. 다만 런타임 점검도 여전히 필요합니다 — 드리프트 때문입니다."},
{t:"IaC 코드는 안전한데 실제 인프라 설정이 다른 상태를 무엇이라 하나?",o:["드리프트(Drift)","롤백","페일오버","프로비저닝"],a:0,e:"<b>드리프트</b>는 대개 누군가 콘솔에서 급하게 손댄 결과입니다. 코드만 믿으면 안 되는 이유이고, 정기적인 <code>plan</code> 비교로 감지합니다."}]},

{d:119,t:"클라우드 침해대응",g:"온프렘 IR 절차를 클라우드에 맞게 바꾼다.",
c:"<p>ACT 5의 IR 4단계는 그대로지만 <b>수단이 달라집니다.</b></p><p><b>봉쇄:</b> 온프렘에서 랜선을 뽑던 것을 클라우드에서는 <b>보안그룹을 전부 거부로 바꾸거나</b> 격리용 그룹으로 교체합니다. <b>인스턴스를 종료하지 마세요</b> — 메모리가 날아갑니다. 자격증명이 문제면 IAM 키 비활성화와 <b>역할 세션 무효화</b>(정책에 <code>DateLessThan aws:TokenIssueTime</code> 조건 추가)를 씁니다.</p><p><b>증거 수집:</b> <b>EBS 스냅샷</b>이 디스크 이미징을 대신합니다. 원본 볼륨은 그대로 두고 스냅샷을 떠서 <b>조사 전용 계정</b>에 복사해 붙입니다. 메모리는 인스턴스가 살아 있을 때 에이전트로 떠야 합니다.</p><p><b>클라우드만의 강점:</b> 인프라가 코드니까 <b>깨끗한 환경을 몇 분 만에 새로 만들 수 있습니다.</b> 온프렘의 '재설치'가 며칠이라면 클라우드는 <code>terraform apply</code> 한 번입니다. 침해된 자원은 통째로 버리고 새로 세우는 <b>불변 인프라(immutable)</b> 접근이 IR을 훨씬 쉽게 만듭니다.</p><p><b>자동화가 필수인 이유:</b> 공격 속도가 사람보다 빠릅니다. GuardDuty 탐지 → EventBridge → Lambda 로 <b>자동 격리·키 비활성화</b>를 걸어두는 구성이 표준입니다.</p>",
lab:["EBS 스냅샷을 떠서 다른 인스턴스에 붙여보는 절차를 문서로 정리한다","격리용 보안그룹(모든 인/아웃바운드 거부)을 하나 만들어둔다","역할 세션을 무효화하는 IAM 정책 조건문을 작성해본다","GuardDuty → EventBridge → Lambda 자동 대응 구조를 그림으로 그린다","'인스턴스를 종료하면 안 되는 이유'를 ACT 5의 휘발성 순서와 연결해 적는다","클라우드 IR 체크리스트를 온프렘 버전과 나란히 비교표로 만든다"],
q:[{t:"침해된 EC2 인스턴스를 격리할 때 종료(terminate)하지 않고 보안그룹을 교체하는 이유는?",o:["종료가 느려서","종료하면 메모리 등 휘발성 증거가 사라지므로, 통신만 끊고 인스턴스는 살려두어야 하기 때문","비용 때문","권한이 없어서"],a:1,e:"ACT 5 Day 82의 <b>휘발성 순서</b>가 클라우드에서도 그대로입니다. <b>격리는 하되 죽이지는 않는다</b>가 원칙이고, 그다음 메모리 수집과 스냅샷을 진행합니다."},
{t:"클라우드 IR이 온프렘보다 유리한 점으로 가장 적절한 것은?",o:["로그가 적어서","인프라가 코드이므로 침해된 자원을 버리고 깨끗한 환경을 몇 분 만에 재구축할 수 있어서","공격자가 접근하지 못해서","증거가 자동 보존되어서"],a:1,e:"<b>불변 인프라</b> 접근입니다. 온프렘 재설치는 며칠이지만 클라우드는 코드 한 번 적용이면 됩니다. 다만 <b>근본 원인을 못 고치면 새 환경도 똑같이 뚫립니다.</b>"}]},

{d:120,t:"[보스·랩] CloudTrail 침해 조사",g:"실제 CloudTrail 로그 파일을 받아 사건 전체를 직접 밝혀낸다.",boss:true,lab_mode:true,mins:45,
files:[{n:"day120-cloudtrail.json",p:"labs/day120-cloudtrail.json",d:"AWS CloudTrail 이벤트 248건 (합성 데이터)"}],
c:"<p><b>상황.</b> 새벽에 GuardDuty가 낯선 IP에서 API 호출이 발생했다는 알럿을 냈습니다. CloudTrail 로그를 확보했습니다. 이 파일 하나로 무슨 일이 있었는지 전부 밝혀야 합니다.</p><p>위 <b>첨부 파일을 내려받아</b> 직접 분석하세요. 아래 문제의 답은 전부 이 파일 안에 있습니다.</p><p><b>조사 순서 힌트</b><br>① 소스 IP 분포를 보고 평소와 다른 IP를 찾습니다<br>② 그 IP의 이벤트를 시간순으로 정렬해 행동을 따라갑니다<br>③ <code>errorCode</code> 가 있는 이벤트를 봅니다 — 실패가 의도를 드러냅니다<br>④ 지속성 확보 흔적을 찾습니다<br>⑤ 무엇을 얼마나 가져갔는지 셉니다</p><p><code>jq</code> 가 없으면 <code>python3 -c</code> 로도 충분합니다. 파일은 <code>{\"Records\": [...]}</code> 구조입니다.</p>",
term:"# 시작점으로 쓸 만한 명령\n$ jq -r '.Records[].sourceIPAddress' day120-cloudtrail.json | sort | uniq -c | sort -rn\n$ jq -r '.Records[] | select(.sourceIPAddress==\"???\") |\n    \"\\(.eventTime) \\(.eventName) \\(.errorCode // \"-\")\"' day120-cloudtrail.json | head -40",
lab:["첨부된 <code>day120-cloudtrail.json</code> 을 내려받는다","소스 IP별 이벤트 수를 세어 이상한 IP를 찾는다","그 IP의 전체 활동을 시간순으로 나열한다","<code>errorCode</code> 가 있는 이벤트만 뽑아 무엇을 시도했다 실패했는지 확인한다","공격자가 심어둔 지속성 수단을 찾는다","유출된 데이터의 버킷 이름과 객체 개수를 센다","전체를 타임라인으로 정리하고 IOC 목록을 만든다"],
q:[{t:"공격자가 사용한 소스 IP 주소를 입력하세요.",k:"input",ph:"예: 1.2.3.4",a:["203.0.113.66"],re:["^203\\.0\\.113\\.66$"],hint:"<code>jq -r '.Records[].sourceIPAddress' ... | sort | uniq -c | sort -rn</code> 로 세어보면 평소 대역과 다른 하나가 보입니다.",e:"<b>203.0.113.66</b> 입니다. 정상 CI 활동은 <code>52.78.x.x</code>(AWS 서울 리전 대역)에서 오는데, 이 IP만 완전히 다른 대역입니다. <b>같은 액세스 키가 갑자기 다른 IP에서 쓰이는 것</b>이 키 유출의 전형적 신호입니다."},
{t:"공격자가 재접속 수단을 확보하기 위해 호출한 IAM API 이름을 입력하세요.",k:"input",ph:"이벤트 이름",a:["CreateAccessKey"],re:["^createaccesskey$"],hint:"원래 키가 회수돼도 계속 들어올 수 있게 만드는 행동입니다.",e:"<b>CreateAccessKey</b> 입니다. 이걸 놓치면 유출된 키를 비활성화해도 침해가 이어집니다. Day 107에서 다룬 <b>'지속성 전수 조사'</b>가 필요한 이유가 바로 이것입니다."},
{t:"공격자가 데이터를 가져간 S3 버킷 이름을 입력하세요.",k:"input",ph:"버킷 이름",a:["acme-customer-exports"],re:["^acme-customer-exports$"],hint:"<code>GetObject</code> 이벤트의 <code>requestParameters.bucketName</code> 을 세어보세요.",e:"<b>acme-customer-exports</b> 에서 180개 객체를 가져갔습니다. 이 숫자를 셀 수 있는 이유는 <b>데이터 이벤트가 켜져 있었기</b> 때문입니다. 꺼져 있었다면 '유출 정황은 있으나 범위 불명'으로 끝났을 것이고, 그건 신고 범위조차 못 정한다는 뜻입니다."},
{t:"공격자가 흔적을 지우려고 시도했다가 권한 부족으로 실패한 API 이름을 입력하세요.",k:"input",ph:"이벤트 이름",a:["StopLogging"],re:["^stoplogging$"],hint:"<code>errorCode</code> 가 있는 이벤트 두 건 중 CloudTrail 서비스를 대상으로 한 것입니다.",e:"<b>StopLogging</b> — CloudTrail 자체를 끄려던 시도입니다. ACT 3 Day 56의 <b>이벤트 로그 삭제(1102)</b>와 정확히 같은 행동이고, 실패했기 때문에 우리가 이 조사를 할 수 있었습니다. <b>감사 로그를 끌 권한은 아무에게도 주면 안 됩니다.</b>"}]}

]},
