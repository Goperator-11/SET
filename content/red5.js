const ACTS=[

{n:5,t:"리버스 엔지니어링 (rev)",s:"소스 없는 바이너리를 읽어 비밀을 풀어낸다. pwn의 짝이자, 멀웨어 분석의 뿌리.",days:[

{d:101,t:"리버싱이란 무엇인가",g:"바이너리를 거꾸로 읽어 동작을 밝혀내는 리버스 엔지니어링의 그림을 잡는다.",
c:"<p><b>리버스 엔지니어링(rev)</b>은 <b>소스 코드 없이 컴파일된 프로그램을 분석</b >해 그 동작을 밝혀내는 기술입니다. CTF의 핵심 분류이자, <b>멀웨어 분석·취약점 연구·크랙</b>의 뿌리입니다.</p><p><b>왜 리버싱인가</b><br>· <b>CTF rev**: 바이너리에 숨은 <b>플래그 검증 로직</b>을 분석해 올바른 입력(플래그)을 알아냅니다.<br>· <b>멀웨어 분석**(방어 코스의 그것!): 악성코드가 뭘 하는지 소스 없이 파악.<br>· <b>취약점 연구**: 패치를 비교해 취약점을 찾음(1-day).<br>· <b>pwn의 전제**: 익스플로잇하려면 먼저 바이너리를 읽어야 함.</p><p><b>컴파일의 역방향</b><br>소스(C) → <b>컴파일</b> → 기계어(바이너리). 리버싱은 이 화살표를 <b>거꾸로</b> 갑니다.<br>· <b>디스어셈블(Disassemble)**: 기계어 → 어셈블리(objdump·Ghidra).<br>· <b>디컴파일(Decompile)**: 어셈블리 → C 유사 코드(Ghidra·IDA). 사람이 읽기 훨씬 쉬움.<br>완벽히 원본 소스로 돌아가진 못하지만(변수명·주석은 사라짐), <b>로직은 복원</b>됩니다.</p><p><b>정적 vs 동적 분석</b><br>· <b>정적(Static)**: 실행 <b>안 하고</b> 코드를 읽음(Ghidra·objdump·strings). 안전하고(멀웨어!) 전체 그림을 봄.<br>· <b>동적(Dynamic)**: <b>실행하며</b> 관찰(gdb·디버거·모니터링). 실제 값·암호화 결과를 봄. 둘을 <b>함께</b> 씁니다.</p><p><b>필요 지식·도구**<br>· <b>어셈블리·프로그램 구조**(ACT 4에서 다짐).<br>· <b>Ghidra**(무료·강력)·<b>IDA**·<b>Binary Ninja**·<b>radare2/cutter**.<br>· <b>gdb**(동적), <b>strings·file·nm·objdump·ltrace/strace**.</p><p><b>CTF rev의 전형**<br>바이너리를 주고 \"플래그를 입력하면 Correct!\"가 나오게 하라. 내부의 <b>검증 로직**(입력을 어떻게 변형·비교하는가)을 분석해, 그 조건을 만족하는 입력을 <b>역산**합니다. 간단한 문자열 비교부터, 복잡한 암호·가상머신·안티디버깅까지.</p><p><b>방어와의 연결**<br>리버싱은 <b>멀웨어 분석의 핵심 기술**입니다(방어 ACT 5 '침해대응과 포렌식'). 공격 도구를 분석하는 능력이 곧 방어입니다. 공격자가 만든 것을 읽을 수 있어야, 막고 탐지합니다.</p><p><b>실전:</b> 바이너리를 받으면 <code>file</code>·<code>strings</code>로 훑고(정적 정찰), Ghidra로 디컴파일해 <code>main</code>과 검증 로직을 읽고, gdb로 실행하며 값을 확인합니다. 이 정적+동적 조합이 rev의 전부입니다.</p>",
term:"kali@kali:~$ file ./crackme\ncrackme: ELF 64-bit LSB executable, x86-64, dynamically linked, not stripped\nkali@kali:~$ strings ./crackme | grep -i flag\nCorrect! The flag is:\nWrong password\n# strings 하나로도 단서가 나온다",
lab:["리버싱이 컴파일의 역방향임을 이해한다","디스어셈블과 디컴파일의 차이를 정리한다","정적 분석과 동적 분석의 장단점을 비교한다","<code>file</code>로 바이너리 종류를 확인한다","<code>strings</code>로 문자열 단서를 찾는다","Ghidra를 설치하고 실행해본다","리버싱이 멀웨어 분석과 어떻게 이어지는지 적는다"],
q:[{t:"리버스 엔지니어링(rev)의 정의는?",o:["소스 코드를 컴파일","소스 없이 컴파일된 바이너리를 분석해 그 동작·로직을 밝혀내는 것","웹을 해킹","비밀번호를 크랙"],a:1,
  e:"rev는 <b>컴파일의 역방향</b>입니다. 소스 없는 바이너리를 디스어셈블·디컴파일해 <b>로직을 복원</b>합니다. CTF 플래그 검증 분석, 멀웨어 분석, 취약점 연구의 뿌리입니다."},
   {t:"멀웨어를 분석할 때 특히 '정적 분석'이 중요한 이유는?",o:["더 빠름","실행하지 않고 코드를 읽으므로 악성코드를 감염 위험 없이 안전하게 분석할 수 있어서","더 정확함","도구가 없어서"],a:1,
    e:"<b>정적 분석은 실행하지 않습니다.</b> 멀웨어를 돌리지 않고 코드를 읽으니 <b>감염 위험이 없습니다.</b> 동적 분석(실행 관찰)은 격리된 샌드박스에서만 합니다."},
   {k:"cmd",t:"바이너리 <code>./crackme</code>에서 사람이 읽을 수 있는 <b>문자열</b>을 추출하는 명령을 입력하세요.",
    ph:"...","a":["strings ./crackme","strings crackme"],"re":["^strings\\s+\\.?/?crackme$"],
    hint:"바이너리 속 아스키 문자열을 뽑는 도구입니다.",
    e:"<code>strings ./crackme</code> — rev의 첫 명령입니다. 플래그·에러 메시지·URL·함수명이 그냥 나오는 경우가 많아, 복잡한 분석 전에 반드시 훑습니다."}]},

{d:102,t:"정적 분석 — file·strings·정찰",g:"실행하지 않고 바이너리의 종류·문자열·구조를 파악한다.",
c:"<p>바이너리를 받으면 <b>실행하기 전에</b> 최대한 많이 알아냅니다. pwn의 정찰처럼, rev도 정찰이 절반입니다.</p><p><b><code>file</code> — 정체 파악**<br><code>file ./bin</code><br>· <b>ELF**(리눅스)·<b>PE**(윈도우)·기타.<br>· <b>32/64비트**, 아키텍처(x86·ARM).<br>· <b>동적/정적 링크**.<br>· <b>stripped vs not stripped**: stripped면 <b>심볼(함수명)이 제거**되어 분석이 어려움. not stripped면 함수 이름이 남아 훨씬 쉬움.</p><p><b><code>strings</code> — 문자열 단서**<br><code>strings -n 8 ./bin | less</code>(8자 이상).<br>플래그·에러 메시지(\"Correct\"·\"Wrong\")·URL·파일 경로·함수명·라이브러리·<b>하드코딩된 비밀번호**가 그대로 나옵니다. rev·멀웨어 분석의 <b>첫 명령**입니다. 쉬운 크랙미는 여기서 플래그가 바로 나오기도 합니다.</p><p><b><code>nm</code>·<code>objdump</code> — 심볼과 코드**<br><code>nm ./bin</code> — 심볼(함수·변수) 목록.<br><code>objdump -d ./bin</code> — <b>디스어셈블**(어셈블리 출력).<br><code>objdump -T</code> — 동적 심볼(임포트한 라이브러리 함수 → 뭘 하는지 힌트: <code>strcmp·socket·crypt</code>).</p><p><b>임포트 함수로 동작 추측**<br>어떤 함수를 쓰는지가 프로그램의 성격을 알려줍니다.<br>· <code>strcmp·strncmp**: 문자열 비교(플래그 검증).<br>· <code>socket·connect·send**: 네트워크(C2?).<br>· <code>fopen·fwrite**: 파일 조작.<br>· <code>crypt·AES·XOR**: 암호화.<br>· <code>ptrace**: 안티디버깅.</p><p><b><code>xxd</code>·<code>hexdump</code> — 바이트 관찰**<br>매직바이트·헤더·패킹 여부·숨은 데이터. 방어 코스의 매직바이트가 여기서도.</p><p><b>패킹 확인**<br>바이너리가 <b>UPX 등으로 압축(패킹)**되면 strings·디스어셈블이 무의미(압축된 상태). <code>strings</code>에 <code>UPX!</code>가 보이거나, 엔트로피가 높으면 패킹. <code>upx -d</code>로 언팩 후 분석.</p><p><b>왜 정적 정찰부터인가:</b> 실행 전에 <b>안전하게**(멀웨어!) 전체 그림을 얻습니다. stripped 여부·아키텍처·임포트 함수·문자열이 <b>분석 전략을 정합니다**. strings 하나로 끝나는 쉬운 문제도 많으니, 항상 여기서 시작합니다.</p><p><b>실전:</b> <code>file</code>로 종류를, <code>strings</code>로 단서를, <code>objdump -T</code>로 임포트를 봅니다. 패킹됐으면 언팩하고, stripped면 동적 분석 비중을 높입니다. 그다음 Ghidra로 들어갑니다.</p>",
term:"kali@kali:~$ file ./bin\nbin: ELF 64-bit LSB pie executable, dynamically linked, stripped   # 심볼 없음\nkali@kali:~$ objdump -T ./bin | grep -iE 'strcmp|xor|crypt'\n  strncmp   # 문자열 비교 -> 플래그 검증 로직\nkali@kali:~$ strings ./bin | grep -iE 'flag|correct|key'\nkey validation failed",
lab:["<code>file</code>로 아키텍처·비트·stripped 여부를 확인한다","<code>strings -n 8</code>으로 문자열 단서를 훑는다","<code>nm</code>으로 심볼(함수) 목록을 본다","<code>objdump -T</code>로 임포트 함수를 보고 동작을 추측한다","임포트 함수로 프로그램 성격을 판단하는 연습을 한다","<code>strings</code>에서 UPX 등 패킹 흔적을 찾는다","패킹됐으면 <code>upx -d</code>로 언팩한다"],
q:[{t:"바이너리가 'stripped'되었다는 것이 분석에 미치는 영향은?",o:["실행 불가","함수·변수 심볼(이름)이 제거되어 어떤 함수가 무엇인지 알기 어려워져 분석이 힘들어짐","암호화됨","크기가 커짐"],a:1,
  e:"<b>stripped = 심볼 제거**입니다. <code>main</code>·함수명이 <code>FUN_00401156</code> 같은 주소로만 보여 분석이 어렵습니다. not stripped면 이름이 남아 훨씬 수월합니다."},
   {t:"<code>objdump -T</code>로 임포트 함수 목록을 보는 이유는?",o:["속도 측정","<code>strcmp</code>(비교)·<code>socket</code>(네트워크)·<code>crypt</code>(암호) 등 사용 함수로 프로그램의 동작·성격을 추측하기 위해","파일 크기","심볼 제거"],a:1,
    e:"프로그램이 <b>어떤 라이브러리 함수를 쓰는지</b>가 성격을 드러냅니다. <code>strncmp</code>가 있으면 문자열 검증, <code>socket</code>이 있으면 통신, <code>ptrace</code>면 안티디버깅입니다."},
   {t:"바이너리가 UPX로 '패킹'되어 있으면?",o:["더 안전","코드·문자열이 압축되어 strings·디스어셈블이 무의미하므로, upx -d 등으로 먼저 언팩해야 분석 가능","실행 불가","자동 분석됨"],a:1,
    e:"패킹은 <b>실행 파일을 압축**한 것입니다. 압축 상태에선 코드·문자열이 안 보이므로, <code>upx -d</code>로 <b>언팩**한 뒤 분석합니다. 멀웨어가 탐지·분석을 피하려 자주 씁니다."}]},

{d:103,t:"Ghidra — 바이너리를 소스처럼",g:"디컴파일러로 바이너리를 C 유사 코드로 보고 로직을 분석한다.",
c:"<p><b>Ghidra**(NSA 제작·무료)는 리버싱의 주력입니다. 기계어를 <b>C 유사 코드로 디컴파일**해, 소스 없는 바이너리를 <b>소스 읽듯**분석합니다.</p><p><b>시작하기**<br>① Ghidra 실행 → 프로젝트 생성 → 바이너리 임포트 → 분석(Analyze, 기본 옵션).<br>② <b>Symbol Tree**에서 <code>main</code>(또는 진입점)을 찾아 더블클릭.<br>③ <b>Decompile 창**(오른쪽)에 C 유사 코드가 뜹니다.<br>④ <b>Listing 창**(가운데)은 어셈블리. 둘을 나란히 봅니다.</p><p><b>핵심 조작**<br>· <b>함수 따라가기**: 함수명 더블클릭 → 그 함수로 이동.<br>· <b>이름 바꾸기(L)**: <code>FUN_00401156</code>·<code>local_28</code>을 의미있는 이름으로. 분석의 핵심 — 이해한 만큼 이름을 붙이면 코드가 읽힙니다.<br>· <b>타입 바꾸기**: 변수 타입을 고쳐 디컴파일 개선.<br>· <b>참조 보기(X)**: 이 함수·변수가 어디서 쓰이는지(cross-reference).<br>· <b>주석(;)**: 이해한 내용을 기록.</p><p><b>CTF rev 분석 패턴**<br>디컴파일된 <code>main</code>에서:<br>① 입력을 받는 곳(<code>scanf·fgets·read</code>).<br>② 입력을 <b>변형**하는 곳(연산·암호·인코딩).<br>③ <b>비교**하는 곳(<code>strcmp·==·memcmp</code>) → 여기가 <b>검증**.<br>④ 성공 분기(\"Correct\")와 실패 분기.<br>이 흐름을 파악하면, '입력이 어떻게 변형되어 무엇과 비교되는가'가 보이고, 그 조건을 <b>역산**해 플래그를 얻습니다.</p><p><b>예: 간단한 크랙미**<br>디컴파일이 <code>if (strcmp(input, \"s3cr3t_p4ss\") == 0) puts(\"Correct\");</code>면 → 플래그는 <code>s3cr3t_p4ss</code>. 하드코딩 비교는 이렇게 바로 보입니다.<br>더 어려운 건 <code>if (check(input))</code>처럼 함수로 숨기거나, <b>입력을 변형한 뒤 비교**(XOR·덧셈)합니다 — 그 변형을 거꾸로 풀어야 합니다.</p><p><b>왜 Ghidra인가:</b> 어셈블리를 한 줄씩 읽는 것보다 <b>디컴파일된 C를 읽는 게 훨씬 빠릅니다**. 무료인데 IDA Pro에 근접합니다. <b>이름 붙이기·주석**으로 큰 함수도 점진적으로 이해할 수 있어, 복잡한 rev·멀웨어 분석의 표준 도구입니다.</p><p><b>실전:</b> Ghidra로 <code>main</code>을 디컴파일해 입력→변형→비교 흐름을 파악하고, 변수·함수에 이름을 붙여가며 검증 로직을 완전히 이해합니다. 그 조건을 만족하는 입력을 역산하면 플래그입니다.</p>",
term:"// Ghidra 디컴파일 결과 (C 유사 코드)\nundefined8 main(void){\n  char input[32];\n  printf(\"Enter flag: \");\n  fgets(input, 32, stdin);\n  if (check_flag(input) == 1)      // <- 검증 함수, 더블클릭해 따라감\n    puts(\"Correct!\");\n  else\n    puts(\"Wrong\");\n}",
lab:["Ghidra에 바이너리를 임포트하고 분석한다","Symbol Tree에서 main을 찾아 디컴파일 창을 연다","Listing(어셈블리)과 Decompile(C)을 나란히 본다","<code>FUN_...</code>·<code>local_...</code>에 의미있는 이름을 붙인다(L)","입력→변형→비교 흐름을 찾는다","cross-reference(X)로 함수 사용처를 추적한다","이해한 내용을 주석으로 기록한다"],
q:[{t:"Ghidra의 디컴파일러가 어셈블리를 직접 읽는 것보다 나은 점은?",o:["더 정확","기계어를 C 유사 코드로 변환해 사람이 로직을 훨씬 빠르게 이해할 수 있어서","더 빠른 실행","암호화"],a:1,
  e:"<b>디컴파일된 C**는 어셈블리보다 읽기 쉽습니다. 변수·조건·반복이 고수준으로 보여, 큰 함수의 로직도 빠르게 파악합니다. 무료로 IDA Pro에 근접해 rev의 표준입니다."},
   {t:"Ghidra에서 <code>FUN_00401156</code>·<code>local_28</code>에 의미있는 이름을 붙이는(L) 것이 중요한 이유는?",o:["예뻐서","이해한 만큼 이름을 붙이면 코드가 점진적으로 읽혀, 크고 복잡한 함수도 분석 가능해지기 때문","실행 속도","암호 해제"],a:1,
    e:"리버싱은 <b>점진적 이해**입니다. 파악한 변수·함수에 이름을 붙이면 다음이 읽히고, 그렇게 큰 바이너리도 조각조각 정복합니다. 이름 붙이기는 분석의 핵심 습관입니다."},
   {t:"CTF rev에서 크랙미를 분석할 때 찾아야 할 핵심 지점은?",o:["main의 첫 줄","입력을 받아 변형한 뒤 무언가와 비교하는(strcmp·memcmp·==) 검증 로직 — 그 조건을 역산하면 플래그","출력 함수","라이브러리"],a:1,
    e:"검증은 '입력을 <b>변형**해 <b>비교**'하는 부분입니다. 그 변형(XOR·덧셈 등)과 비교 대상을 파악하면, 조건을 만족하는 입력을 <b>거꾸로 계산**해 플래그를 얻습니다."}]},

{d:104,t:"동적 분석 — 디버거로 실행하며 보기",g:"gdb로 프로그램을 실행하며 실제 값·분기·변형 결과를 관찰한다.",
c:"<p>정적 분석으로 로직을 파악했으면, <b>동적 분석**으로 실제 동작을 확인합니다. 특히 <b>계산·암호화 결과**는 실행해 보는 게 빠릅니다.</p><p><b>gdb 기본(ACT 4 복습·rev 활용)**<br><code>gdb ./bin</code> → <code>b main</code>·<code>b *0x401234</code> 브레이크 → <code>r</code> 실행 → <code>c</code> 계속.<br>· <code>b check_flag</code> — 검증 함수에 중단.<br>· <code>x/s $rdi</code> — 인자(비교 대상 문자열!)를 봄.<br>· <code>x/20xw addr</code>·<code>x/s addr</code> — 메모리 관찰.<br>· <code>info registers</code>·<code>p $rax</code> — 레지스터·반환값.</p><p><b>동적 분석의 힘 — 비교 지점 가로채기**<br>가장 강력한 기법: <b>비교(strcmp) 직전에 중단**해 <b>무엇과 비교하는지**를 봅니다.<br>입력을 아무거나 넣고 <code>strcmp</code>에 브레이크 → <code>x/s $rsi</code>(둘째 인자)로 <b>정답 문자열**을 그대로 읽습니다. 프로그램이 내부에서 만든 정답을 <b>실행 중에 훔쳐보는**것 — 복잡한 변형 로직을 안 풀고도 답이 나옵니다.</p><p><b>분기 관찰·조작**<br>· <b>조건 분기**에서 어느 쪽으로 가는지 봄(<code>cmp`·`je`/`jne</code>).<br>· <b>레지스터·플래그 조작**: <code>set $rax=1</code>로 검증을 강제 통과시켜 이후 로직을 봄(패치 없이).</p><p><b>strace·ltrace — 호출 추적**<br>· <code>ltrace ./bin</code> — <b>라이브러리 함수 호출**을 인자와 함께 출력. <code>strcmp(\"myinput\", \"realflag\")</code>가 그대로 찍히기도!<br>· <code>strace ./bin</code> — <b>시스템 콜**(파일·네트워크). 프로그램이 뭘 여는지·보내는지.<br>ltrace는 <b>크랙미의 지름길**인 경우가 많습니다.</p><p><b>정적+동적 결합**<br>· 정적으로 <b>구조·검증 위치**파악 → 동적으로 <b>실제 값·정답**확인.<br>· 암호·인코딩이 복잡하면 동적으로 <b>결과만**봄.<br>· 안티디버깅(다음 날)이 있으면 정적 비중을 높임.</p><p><b>왜 동적이 필요한가:</b> 정적 분석은 '무엇을 하는가'를 보지만, <b>실제 값**(런타임에 계산·복호화되는 것)은 실행해야 압니다. 특히 <b>비교 지점 가로채기**와 <b>ltrace**는 어려운 검증 로직을 <b>풀지 않고 우회**하는 강력한 지름길입니다. 고수는 정적·동적을 상황에 맞게 오갑니다.</p><p><b>실전:</b> ltrace로 먼저 호출을 훑고(운 좋으면 정답이 바로), gdb로 <code>strcmp</code> 직전에 중단해 비교 대상을 읽습니다. 복잡한 변형은 실행 결과만 확인해 역산 부담을 줄입니다.</p>",
term:"# 비교 지점 가로채기 — 정답을 실행 중에 훔쳐본다\npwndbg> b strcmp\npwndbg> r\n> AAAA                         # 아무 입력\npwndbg> x/s $rsi               # strcmp의 둘째 인자 = 정답!\n0x404060: \"s3cr3t_fl4g_2024\"\n# 변형 로직을 안 풀고도 플래그 획득\n\n# 또는 ltrace 한 방:\n$ ltrace ./bin\nstrcmp(\"AAAA\", \"s3cr3t_fl4g_2024\") = -1",
lab:["gdb로 검증 함수(check)에 브레이크를 건다","<code>strcmp</code>에 중단해 <code>x/s $rsi</code>로 비교 대상을 읽는다","<code>ltrace ./bin</code>으로 라이브러리 호출을 추적한다","<code>strace</code>로 시스템 콜(파일·네트워크)을 본다","조건 분기에서 어느 쪽으로 가는지 관찰한다","<code>set $rax=1</code>로 검증을 강제 통과시켜 본다","정적·동적 분석을 언제 쓸지 정리한다"],
q:[{t:"gdb에서 '비교 지점(strcmp) 가로채기'가 강력한 이유는?",o:["빠름","프로그램이 내부에서 만든 정답 문자열을 비교 직전에 메모리에서 읽어, 복잡한 변형 로직을 풀지 않고도 답을 얻기 때문","암호화","자동"],a:1,
  e:"<code>strcmp</code> 직전에 중단해 <b>둘째 인자(정답)를 읽으면**, 프로그램이 만든 정답이 그대로 보입니다. 아무리 복잡한 변형이어도 <b>결과(정답)를 실행 중에 훔쳐**답을 얻습니다."},
   {t:"<code>ltrace</code>가 크랙미 분석의 지름길이 되는 경우는?",o:["항상 실패","라이브러리 함수 호출을 인자와 함께 출력해, strcmp(\"입력\", \"정답\") 같은 비교가 그대로 찍힐 때","암호화될 때","네트워크일 때"],a:1,
    e:"<code>ltrace</code>는 <b>라이브러리 호출을 인자와 함께**보여줍니다. 검증이 <code>strcmp</code>로 되어 있으면 <b>정답이 그대로 출력**되기도 해, 분석 없이 플래그가 나옵니다."},
   {t:"정적 분석과 동적 분석을 함께 쓰는 이유는?",o:["관습","정적으로 구조·검증 위치를 파악하고, 동적으로 런타임에만 계산·복호화되는 실제 값을 확인하기 위해","더 느려서","도구가 많아서"],a:1,
    e:"정적은 '<b>무엇을 하는가**'(전체 구조), 동적은 '<b>실제 값은 무엇인가**'(런타임 계산 결과)를 봅니다. 둘을 오가며 — 구조는 정적으로, 복잡한 계산 결과는 동적으로 — 효율적으로 분석합니다."}]},

{d:105,t:"어셈블리 읽기 심화 — 로직 복원",g:"디컴파일이 부실할 때 어셈블리를 직접 읽어 제어 흐름과 연산을 복원한다.",
c:"<p>디컴파일러가 항상 완벽하진 않습니다(최적화·난독화 시 엉킴). 때론 <b>어셈블리를 직접**읽어야 합니다. rev·pwn 공통의 핵심 근육입니다.</p><p><b>제어 흐름 복원**<br>· <b>조건문(if)**: <code>cmp a, b</code> + <code>je/jne/jg/jl</code>(조건 점프). <code>cmp eax, 5; jne fail</code>은 <code>if(eax != 5) goto fail</code>.<br>· <b>반복문(loop)**: 뒤로 가는 <code>jmp`·`jl`, 카운터 증가(`inc`/`add</code>). 위로 점프하면 루프.<br>· <b>switch**: 점프 테이블(<code>jmp [table + idx*8]</code>).</p><p><b>연산 복원**<br>· <code>add·sub·imul·xor·and·or·shl·shr**: 산술·논리. <b>XOR은 암호화의 단골**(<code>xor al, key</code>).<br>· <code>lea**: 주소 계산(곱셈 최적화에도 씀 — <code>lea eax,[rdi+rdi*2]</code>는 ×3).<br>· <b>컴파일러 최적화**: 나눗셈을 곱셈+시프트로, ×2를 시프트로 바꿈 — 겉보기 복잡해도 단순 연산.</p><p><b>함수 경계·인자**<br>프롤로그(<code>push rbp; mov rbp,rsp</code>)·에필로그(<code>leave; ret</code>)로 함수를 구분. 인자는 <b>RDI·RSI·RDX...**(ACT 4). 이걸 알면 '이 함수가 무슨 인자로 뭘 하는가'가 보입니다.</p><p><b>흔한 검증 패턴 읽기**<br>· <b>문자열 비교 루프**: 한 글자씩 <code>movzx`·`cmp`·`jne</code>. 각 글자를 특정 값과 비교 → 그 값들이 플래그.<br>· <b>XOR 복호화**: <code>xor byte[rsi+rcx], key</code>를 루프 → 입력(또는 데이터)을 key로 XOR. 거꾸로 XOR하면 원본.<br>· <b>체크섬·해시**: 여러 연산 후 한 값과 비교.</p><p><b>도구 도움**<br>· Ghidra·gdb의 <b>어셈블리 뷰**를 디컴파일과 <b>나란히**보며 대조.<br>· 모르는 명령은 <b>검색**(x86 instruction reference).<br>· <b>동적으로 확인**: 이 명령이 실제 무슨 값을 만드나(gdb <code>si</code> + 레지스터 관찰).</p><p><b>왜 필요한가:</b> 난독화·최적화·안티디스어셈블(다음 날)이 걸리면 <b>디컴파일이 깨집니다**. 그때 어셈블리를 읽는 힘이 <b>최후의 무기**입니다. 그리고 어셈블리를 정확히 읽어야 <b>pwn의 가젯·오프셋**도 정확히 다룹니다. rev와 pwn의 공통 기반입니다.</p><p><b>실전:</b> 디컴파일이 이상하면 어셈블리로 내려가, 제어 흐름(점프)과 연산(특히 XOR 루프)을 손으로 복원합니다. 모르는 명령은 gdb로 실행해 결과를 확인하며 의미를 채웁니다.</p>",
term:"; XOR 복호화 루프 (어셈블리) — 플래그 검증의 단골\n  mov  rcx, 0            ; i = 0\nloop:\n  mov  al, [input+rcx]   ; al = input[i]\n  xor  al, 0x37          ; al ^= 0x37  (key)\n  cmp  al, [target+rcx]  ; 비교\n  jne  fail\n  inc  rcx\n  cmp  rcx, 16\n  jl   loop              ; 16글자 반복\n; -> target을 0x37로 XOR하면 플래그",
lab:["<code>cmp</code>+조건점프로 if문을 복원한다","뒤로 가는 점프로 반복문을 식별한다","XOR 루프를 찾아 암호화 패턴을 인식한다","<code>lea</code>가 곱셈 최적화로 쓰이는 예를 이해한다","함수 프롤로그·에필로그로 경계를 찾는다","한 글자씩 비교하는 검증 루프를 읽는다","모르는 명령을 gdb <code>si</code>로 실행해 확인한다"],
q:[{t:"어셈블리에서 <code>cmp eax, 5</code> 다음의 <code>jne fail</code>은 어떤 고수준 코드에 해당하나?",o:["eax = 5","<code>if (eax != 5) goto fail;</code> — 비교 후 같지 않으면 점프","eax += 5","반복문"],a:1,
  e:"<code>cmp**는 두 값을 빼서 플래그를 설정하고, <code>jne**(jump if not equal)는 <b>같지 않으면 점프**합니다. 합치면 <code>if(eax!=5) goto fail</code>입니다. 조건문 복원의 기본입니다."},
   {t:"리버싱에서 <code>xor</code> 명령이 루프 안에 있으면 무엇을 의심하나?",o:["곱셈","XOR 기반 암호화·인코딩 — 데이터를 key로 XOR하는 것이라, 거꾸로 XOR하면 원본 복원","덧셈","비교"],a:1,
    e:"<b>XOR 루프는 간단한 암호화의 단골**입니다. <code>xor byte, key</code>를 반복하면 데이터를 key로 XOR한 것이라, <b>같은 key로 다시 XOR하면 원본**이 나옵니다. CTF rev에서 매우 흔합니다."},
   {t:"디컴파일러(Ghidra) 대신 어셈블리를 직접 읽어야 하는 상황은?",o:["항상","난독화·컴파일러 최적화·안티디스어셈블로 디컴파일 결과가 깨지거나 부정확할 때","쉬운 문제","웹 분석"],a:1,
    e:"디컴파일은 편하지만 <b>난독화·최적화·안티디스어셈블**에 취약합니다. 결과가 이상하면 <b>어셈블리가 최후의 진실**이라, 직접 읽어 로직을 복원합니다. pwn 가젯 분석에도 같은 힘이 필요합니다."}]},

{d:106,t:"안티디버깅·안티분석 우회",g:"프로그램이 분석을 방해하는 기법을 알아채고 우회한다.",
c:"<p>진짜 rev·멀웨어는 <b>분석을 방해**합니다. 이 방해를 알아채고 우회하는 게 중급 rev입니다. 방어 코스에서 배운 멀웨어의 회피 기법을, 이제 <b>뚫는 쪽**에서 봅니다.</p><p><b>안티디버깅**<br>디버거(gdb)가 붙었는지 감지해 다르게 행동합니다.<br>· <b><code>ptrace(PTRACE_TRACEME)</code>**: 자기 자신에 ptrace를 걸어봄. 디버거가 이미 붙어 있으면 <b>실패**→ 감지. 우회: gdb에서 ptrace 반환값을 0으로 조작, 또는 <code>LD_PRELOAD</code>로 ptrace를 무력화.<br>· <b><code>/proc/self/status</code>의 TracerPid**: 0이 아니면 디버깅 중. 우회: 그 검사 분기를 패치.<br>· <b>타이밍 체크**: 명령 사이 시간이 길면(디버거로 멈춘 것) 감지. 우회: 해당 검사 우회.</p><p><b>안티디스어셈블**<br>디스어셈블러를 혼란시킵니다.<br>· <b>정크 바이트**: 유효하지 않은 바이트로 디스어셈블러가 명령을 잘못 해석하게. 우회: 수동으로 정렬을 맞춰 재해석.<br>· <b>오버래핑 명령**: 한 바이트가 두 명령에 걸침.</p><p><b>난독화(Obfuscation)**<br>· <b>제어 흐름 평탄화**: 코드를 큰 switch로 뭉개 흐름을 숨김.<br>· <b>문자열 암호화**: 문자열을 런타임에 복호화(strings로 안 보임). 우회: 동적으로 복호화 후 메모리에서 읽기.<br>· <b>패킹**(Day 102): UPX 등. 우회: 언팩, 또는 실행 후 메모리 덤프.<br>· <b>가상화(VM 난독화)**: 커스텀 가상머신으로 실행 — 가장 어려움. 바이트코드와 VM 해석기를 분석.</p><p><b>우회의 두 축**<br>① <b>패치(Patch)**: 방해 검사를 <code>nop</code>이나 무조건 점프로 바꿔 무력화. Ghidra·hex editor로 바이트 수정.<br>② <b>동적 우회**: 실행 중 레지스터·메모리를 조작(gdb <code>set</code>), 또는 검사 <b>이후**에서 시작. <b>메모리 덤프**: 복호화·언팩된 상태를 통째로 떠서 분석(멀웨어 분석의 핵심).</p><p><b>강력한 지름길 — 동적으로 결과만**<br>방해가 심하면 <b>로직을 다 안 풀고**, 방해를 우회한 뒤 <b>비교 지점만 가로채**(Day 104) 정답을 훔칩니다. 안티분석은 '분석을 느리게'할 뿐, <b>실행은 결국 정답을 만들어야**하니, 그 순간을 잡으면 됩니다.</p><p><b>왜 배우나:</b> 실전 멀웨어·상용 보호(DRM)·어려운 CTF는 <b>거의 다 안티분석**이 걸립니다. 이걸 알아채고(왜 동작이 이상한가?) 우회하는 능력이 <b>초보와 실전 리버서를 가릅니다**. 방어 코스의 멀웨어 회피 기법과 정확히 짝을 이룹니다 — 공격을 알아야 분석하고, 분석해야 방어합니다.</p><p><b>실전:</b> 동작이 이상하면(디버거에서만 다르게) 안티디버깅을 의심하고, ptrace·TracerPid 검사를 찾아 패치하거나 우회합니다. 난독화가 심하면 로직 복원 대신 <b>동적으로 정답을 가로채는**쪽으로 전략을 바꿉니다.</p>",
term:"# 안티디버깅(ptrace) 우회 — 검사 분기를 패치\n// Ghidra에서 발견:\nif (ptrace(PTRACE_TRACEME,0,0,0) < 0) { puts(\"debugger!\"); exit(1); }\n# 우회 1: gdb에서 ptrace 반환을 0으로\npwndbg> b ptrace\npwndbg> r ; set $rax=0 ; c\n# 우회 2: LD_PRELOAD로 ptrace를 무력화한 .so",
lab:["ptrace 기반 안티디버깅을 Ghidra에서 찾는다","gdb에서 ptrace 반환값을 0으로 조작해 우회한다","<code>/proc/self/status</code> TracerPid 검사를 이해한다","문자열이 런타임에 복호화되는(strings에 안 보임) 경우를 파악한다","안티디버깅 검사 분기를 nop으로 패치한다","복호화·언팩된 상태를 메모리 덤프로 획득한다","안티분석을 우회한 뒤 비교 지점을 가로채 정답을 얻는다"],
q:[{t:"<code>ptrace(PTRACE_TRACEME)</code> 기반 안티디버깅의 원리는?",o:["암호화","자기 자신에 ptrace를 거는데, 디버거가 이미 붙어 있으면 실패하므로 이를 디버깅 감지에 이용","네트워크 차단","파일 잠금"],a:1,
  e:"프로세스는 <b>ptrace로 한 번만 추적**됩니다. 자기 자신에 <code>TRACEME</code>를 걸어 <b>실패하면 이미 디버거가 붙은 것**입니다. 우회는 반환값을 0으로 조작하거나 검사 분기를 패치합니다."},
   {t:"안티분석이 심한 바이너리를 로직 복원 없이 뚫는 강력한 전략은?",o:["포기","방해를 우회한 뒤, 프로그램이 결국 만들어야 하는 정답을 비교 지점에서 가로챔(동적으로 결과만 획득)","재컴파일","암호 크랙"],a:1,
    e:"안티분석은 '<b>분석을 느리게**'할 뿐, 실행은 결국 <b>정답을 계산**해야 합니다. 그 순간(비교 지점)을 gdb로 잡으면, 아무리 난독화돼도 <b>정답을 훔쳐**냅니다."},
   {t:"안티분석 우회 능력이 방어(멀웨어 분석)와 직결되는 이유는?",o:["무관","실전 멀웨어는 거의 안티분석이 걸려 있어, 이를 우회해 분석할 수 있어야 악성코드의 동작을 파악하고 방어할 수 있어서","더 빨라서","합법이라"],a:1,
    e:"멀웨어는 <b>탐지·분석을 피하려 안티분석**을 씁니다(방어 코스의 회피 기법). 이를 우회해 분석하는 능력이 있어야 악성코드가 뭘 하는지 알고 방어합니다. 공격 기술이 곧 방어 역량입니다."}]},

{d:107,t:"플래그 역산 — 검증 로직 거꾸로 풀기",g:"입력을 변형·비교하는 로직을 파악해 조건을 만족하는 입력을 계산한다.",
c:"<p>rev의 <b>정수**입니다. 프로그램이 '입력을 <b>변형**해 <b>비교**'하면, 그 과정을 <b>거꾸로 풀어**정답 입력(플래그)을 계산합니다.</p><p><b>기본 구조**<br><code>if (transform(input) == target) win();</code><br>· <code>transform**: 입력에 가하는 연산(XOR·덧셈·순열·암호).<br>· <code>target**: 비교 대상(하드코딩된 값·배열).<br>플래그 = <code>transform</code>의 <b>역함수**를 <code>target</code>에 적용.</p><p><b>흔한 변형과 역산**<br>· <b>XOR**: <code>out = in ^ key</code> → <code>in = out ^ key</code>(XOR은 자기 역함수). target을 key로 XOR.<br>· <b>덧셈/뺄셈**: <code>out = in + n</code> → <code>in = out - n</code>.<br>· <b>순열·스왑**: 바이트 순서를 섞음 → 역순열.<br>· <b>Base64·인코딩**: 디코드.<br>· <b>여러 단계**: 순서대로 각 역연산을 <b>역순**으로 적용.</p><p><b>예제 역산**<br>디컴파일: <code>for(i=0;i&lt;len;i++) if((input[i]^0x37) + i != target[i]) fail;</code><br>역산: <code>input[i] = (target[i] - i) ^ 0x37</code>. target 배열을 Ghidra에서 추출해 이 식으로 계산 → 플래그.<br>파이썬으로: <code>flag = bytes((t - i) ^ 0x37 for i,t in enumerate(target))</code>.</p><p><b>target 추출**<br>비교 대상(정답 데이터)을 얻습니다:<br>· Ghidra에서 <b>배열·상수**로 보임 → 복사.<br>· 동적으로 <b>비교 직전 메모리**에서 읽음(Day 104).<br>· <code>strings`·`xxd</code>로 데이터 영역.</p><p><b>역산이 어려울 때 — 제약 해결(z3)**<br>변형이 복잡해 손으로 역산이 어려우면(비선형·여러 조건), <b>z3(SMT 솔버)**를 씁니다. '이 조건들을 만족하는 입력을 찾아라'를 z3에 맡깁니다. 연립방정식·제약을 <b>자동으로 푸는**도구로, 복잡한 rev의 강력한 무기입니다.<br><code>from z3 import *; s=Solver(); ... s.add(제약); s.check(); s.model()</code>.</p><p><b>브루트포스가 답일 때**<br>입력 공간이 작으면(짧은 PIN·글자별 독립 검증) <b>전수 시도**가 빠릅니다. 특히 <b>글자별로 독립 검증**되면 한 글자씩 브루트포스(각 256가지).</p><p><b>왜 이게 rev의 핵심인가:</b> 대부분의 CTF rev가 결국 '<b>검증 로직 → 역산**'입니다. 변형을 정확히 읽고(정적·동적), 역함수를 적용하거나 z3·브루트포스로 <b>조건을 만족하는 입력**을 찾는 것 — 이 사고가 rev 실력의 중심입니다.</p><p><b>실전:</b> 검증 로직을 완전히 파악하고, target을 추출한 뒤, 파이썬으로 역산 스크립트를 짭니다. 손 역산이 어려우면 z3로, 공간이 작으면 브루트포스로 전환합니다.</p>",
term:"# 역산 스크립트 (Ghidra에서 로직·target 파악 후)\n# 검증: (input[i] ^ 0x37) + i == target[i]\ntarget = [0x6a,0x52,0x5c,...]   # Ghidra에서 추출\nflag = bytes(((t - i) & 0xff) ^ 0x37 for i,t in enumerate(target))\nprint(flag)   # -> SFSE{...}\n\n# 복잡하면 z3:\nfrom z3 import *\ns = Solver(); inp = [BitVec(f'c{i}',8) for i in range(16)]\nfor i,t in enumerate(target): s.add(((inp[i]^0x37)+i)&0xff == t)\ns.check(); print(s.model())",
lab:["검증 로직(변형+비교)을 완전히 파악한다","비교 대상 target을 Ghidra·동적으로 추출한다","XOR·덧셈 등의 역함수를 적용한다","여러 단계 변형을 역순으로 역산한다","파이썬으로 역산 스크립트를 작성한다","복잡한 제약은 z3로 자동으로 푼다","입력 공간이 작으면 브루트포스로 전환한다"],
q:[{t:"검증이 <code>(input[i] ^ 0x37) == target[i]</code>일 때 플래그를 구하는 방법은?",o:["불가능","target[i]를 0x37로 XOR (XOR은 자기 역함수) — input[i] = target[i] ^ 0x37","target을 더함","target을 뺌"],a:1,
  e:"XOR은 <b>자기 역함수**입니다: <code>a^k^k = a</code>. <code>input^0x37 == target</code>이면 <code>input = target ^ 0x37</code>. target 배열을 추출해 각 바이트를 0x37로 XOR하면 플래그입니다."},
   {t:"변형 로직이 복잡해 손으로 역산이 어려울 때 쓰는 도구는?",o:["gdb","z3(SMT 솔버) — '이 제약들을 만족하는 입력을 찾아라'를 자동으로 풀어줌","strings","checksec"],a:1,
    e:"<b>z3**는 제약 조건(방정식·부등식)을 만족하는 값을 <b>자동으로 찾는**솔버입니다. 비선형·다조건 검증을 손으로 역산하는 대신, 조건을 z3에 넣으면 플래그를 계산해줍니다. 복잡한 rev의 필수 무기입니다."},
   {t:"글자별로 독립적으로 검증되는(각 글자가 따로 비교되는) 크랙미에 효과적인 방법은?",o:["z3만","글자별 브루트포스 — 각 위치를 256가지로 전수 시도(독립이라 조합 폭발 없음)","포기","암호 크랙"],a:1,
    e:"각 글자가 <b>독립 검증**되면 조합이 폭발하지 않습니다(16글자여도 16×256). 한 글자씩 <b>맞는 값을 전수 시도**하면 빠르게 전체 플래그를 복원합니다."}]},

{d:108,t:"파이썬·고급 언어 리버싱",g:"컴파일된 파이썬·Go·러스트 등 네이티브 아닌 바이너리를 분석한다.",
c:"<p>모든 게 C는 아닙니다. <b>파이썬·Go·Rust·C#·자바**로 만든 것도 rev 대상입니다. 각자 방법이 다릅니다.</p><p><b>파이썬 (PyInstaller·py2exe)**<br>파이썬 스크립트를 실행 파일로 묶은 것. <b>거의 원본 소스로 복원**됩니다.<br>① <b>pyinstxtractor**로 exe에서 <code>.pyc</code>(바이트코드) 추출.<br>② <b>decompyle3·uncompyle6·pycdc**로 <code>.pyc</code> → 파이썬 소스 복원.<br>버전이 맞으면 <b>거의 완벽한 소스**가 나와 rev가 매우 쉽습니다. <code>strings`에 `python`·`PyInstaller</code>가 보이면 이 방법.</p><p><b>.pyc 직접**<br><code>.pyc</code> 파일만 주면 바로 디컴파일. 안 되면 <code>dis</code> 모듈로 바이트코드를 봅니다.</p><p><b>Go**<br>· 정적 링크·큰 바이너리·독특한 런타임. 함수명이 <b>남아 있는 경우**가 많음(<code>main.main</code>).<br>· <b>문제**: stripped Go는 함수 복원이 어려움. <b>도구**: Ghidra + Go 분석 플러그인, <code>GoReSym</code>으로 심볼 복원.<br>· 문자열이 <b>길이 기반**(null 종료 아님)이라 strings가 뭉개짐 — 주의.</p><p><b>Rust**<br>Go처럼 정적·큰 바이너리. 심볼이 <b>망글링(mangling)**됨(<code>_ZN4core...</code>) — <code>rustfilt`·`c++filt</code>로 디망글. 로직은 어셈블리로 읽어야 할 때가 많아 어려운 편.</p><p><b>.NET (C#) — 최고로 쉬움**<br><b>바이트코드(IL)**로 컴파일되어 <b>거의 완벽히 소스 복원**.<br><b>dnSpy·ILSpy·dotPeek**: C# 소스를 그대로 복원하고, <b>실시간 디버깅·편집·재컴파일**까지. <code>.NET assembly</code>면 이 도구로 몇 분 만에.</p><p><b>Java (.jar/.class)**<br>바이트코드 → <b>JD-GUI·CFR·procyon**으로 자바 소스 복원. .NET처럼 쉬움. 안드로이드 APK도 <b>jadx**로(ACT 3 모바일).</p><p><b>왜 언어를 먼저 식별하나:</b> <b>같은 rev여도 언어마다 도구·난이도가 천차만별**입니다. C#·자바·파이썬은 <b>거의 소스 복원**(쉬움), Go·Rust는 <b>네이티브 분석**(어려움). <code>file`·`strings</code>로 언어를 식별하면 <b>맞는 도구로 직행**해 시간을 아낍니다. '컴파일된 파이썬을 어셈블리로 읽는' 삽질을 피합니다.</p><p><b>실전:</b> <code>file</code>·<code>strings</code>로 언어를 식별합니다. 파이썬이면 pyinstxtractor+decompyle, .NET이면 dnSpy, 자바면 jadx/CFR로 <b>소스를 복원**합니다. Go·Rust면 Ghidra로 네이티브 분석하되 심볼 복원 도구를 함께 씁니다.</p>",
term:"# PyInstaller exe -> 파이썬 소스 복원\n$ python pyinstxtractor.py chall.exe        # .pyc 추출\n$ decompyle3 chall.pyc > chall.py           # 소스 복원\n$ cat chall.py                              # 거의 원본!\n\n# .NET은 dnSpy로 즉시 C# 소스 + 디버깅\n# 언어 식별: strings chall | grep -iE 'python|golang|rustc|mscoree'",
lab:["<code>file</code>·<code>strings</code>로 바이너리의 언어를 식별한다","PyInstaller exe를 pyinstxtractor로 추출한다","<code>.pyc</code>를 decompyle3로 소스 복원한다","<code>.NET</code>이면 dnSpy로 C# 소스를 복원한다","자바 .jar를 CFR·jadx로 디컴파일한다","Go의 심볼 복원(GoReSym) 필요성을 이해한다","언어별 도구·난이도를 표로 정리한다"],
q:[{t:"PyInstaller로 만든 exe를 리버싱할 때의 특징은?",o:["불가능","pyinstxtractor로 .pyc를 추출하고 decompyle로 거의 원본 파이썬 소스를 복원할 수 있어 매우 쉬움","네이티브라 어려움","암호화됨"],a:1,
  e:"파이썬 실행 파일은 <b>바이트코드(.pyc)를 묶은 것**이라, 추출 후 디컴파일하면 <b>거의 원본 소스**가 나옵니다. C 네이티브보다 훨씬 쉬워, 언어 식별이 중요합니다."},
   {t:"리버싱 전에 바이너리의 '언어를 먼저 식별'하는 이유는?",o:["호기심","언어마다 도구·난이도가 크게 달라(.NET·자바·파이썬은 소스 복원, Go·Rust는 네이티브 분석), 맞는 도구로 직행해 시간을 아끼려고","실행 위해","암호화"],a:1,
    e:"C#·자바·파이썬은 <b>거의 소스 복원**(dnSpy·jadx·decompyle)으로 쉽고, Go·Rust는 어렵습니다. 언어를 먼저 식별하면 <b>맞는 도구로 직행**해, 컴파일된 파이썬을 어셈블리로 읽는 삽질을 피합니다."},
   {t:".NET(C#) 바이너리가 리버싱하기 가장 쉬운 편인 이유는?",o:["작아서","IL 바이트코드로 컴파일되어 dnSpy·ILSpy로 거의 완벽한 C# 소스 복원과 실시간 디버깅·편집이 가능하기 때문","암호화 안 해서","오래돼서"],a:1,
    e:".NET은 <b>중간 언어(IL)**로 컴파일됩니다. <b>dnSpy**가 이를 <b>거의 완벽한 C# 소스**로 복원하고, 디버깅·편집·재컴파일까지 지원해, 몇 분 만에 분석됩니다."}]},

{d:109,t:"멀웨어 분석 입문 — 방어와 만나는 곳",g:"악성코드를 안전하게 분석해 행위·IOC를 뽑는, 리버싱의 방어적 응용을 익힌다.",
c:"<p>리버싱의 가장 중요한 <b>방어적 응용**입니다. 방어 코스 ACT 5(포렌식)에서 배운 멀웨어 분석을, 이제 <b>리버서의 깊이**로 봅니다. 공격 기술이 방어의 핵심 무기가 됩니다.</p><p><b>안전이 최우선 — 격리**<br>멀웨어는 <b>절대 일반 시스템에서 실행하지 않습니다**.<br>· <b>격리된 VM**(스냅샷·네트워크 차단/시뮬레이션).<br>· <b>REMnux**(멀웨어 분석 리눅스), <b>FLARE VM**(윈도우).<br>· <b>inetsim·FakeNet**으로 네트워크를 흉내(C2 응답 시뮬레이션).<br>동적 분석 후 <b>스냅샷 복원**.</p><p><b>정적 분석(먼저·안전)**<br>· <code>file`·`strings**: 언어·문자열·URL·IP.<br>· <b>임포트 함수(Day 102)**: <code>CreateRemoteThread`(인젝션)·`InternetOpen`(C2)·`RegSetValue`(지속성)·`CryptEncrypt</code>(랜섬웨어). 함수가 <b>의도**를 드러냄.<br>· <b>PE 분석**: 섹션·엔트로피(패킹)·컴파일 시각.<br>· <b>YARA 룰**(방어 코스): 알려진 패밀리 식별.</p><p><b>동적 분석(격리에서)**<br>· <b>행위 모니터링**: <code>procmon`(파일·레지스트리·프로세스), `Process Hacker`, `Wireshark</code>(네트워크).<br>· <b>무엇을 하나**: 어떤 파일 생성·어떤 레지스트리(지속성)·어디로 통신(C2)·무엇을 암호화.<br>· <b>디버거**(x64dbg·gdb)로 언팩·복호화 지점 통과 후 메모리 덤프.</p><p><b>IOC 추출 — 방어의 산출물**<br>분석의 목적은 <b>침해지표(IOC)**입니다(방어 코스!):<br>· C2 <b>도메인·IP**, <b>파일 해시**, <b>파일 경로·레지스트리 키**, <b>뮤텍스**, <b>YARA 시그니처**.<br>이걸로 <b>탐지 규칙**을 만들고 조직을 보호합니다. 리버싱 → IOC → 방어의 사슬입니다.</p><p><b>MITRE ATT&CK 매핑**<br>멀웨어의 각 행위를 ATT&CK 기법에 매핑(방어 코스). '이건 T1055 프로세스 인젝션, 저건 T1071 C2'. 공격을 <b>구조화된 방어 언어**로 옮깁니다.</p><p><b>왜 리버싱이 방어의 정점인가:</b> 자동 탐지(백신·EDR)가 못 잡는 <b>새로운·표적형 멀웨어**는 <b>사람이 리버싱**해야 정체를 압니다. 랜섬웨어의 암호화 방식을 분석해 <b>복호화기를 만들거나**, APT의 백도어를 분석해 <b>탐지 규칙**을 배포합니다. <b>공격을 가장 깊이 이해한 방어자**가 리버서이고, 이게 당신이 배운 공격·방어 양쪽이 만나는 지점입니다.</p><p><b>실전(안전!):</b> 격리 VM에서 정적으로 임포트·문자열·YARA를 보고 의도를 추정한 뒤, 동적으로 행위를 관찰해 IOC를 뽑습니다. 언팩·복호화가 걸리면 디버거로 통과 후 메모리를 덤프합니다. 절대 실 시스템에서 실행하지 않습니다.</p>",
term:"# 격리 VM에서 멀웨어 정적 분석\nremnux@remnux:~$ strings sample.exe | grep -iE 'http|\\.onion|cmd'\nhttp://evil-c2[.]net/gate.php     # C2 (defang!)\nremnux@remnux:~$ yara rules.yar sample.exe\nRansomware_Generic sample.exe     # 패밀리 식별\n# 동적: procmon/wireshark로 행위 -> IOC 추출",
lab:["격리 VM(REMnux·FLARE) 환경을 이해한다","<code>strings</code>·임포트 함수로 멀웨어 의도를 추정한다","YARA 룰로 알려진 패밀리를 식별한다","격리에서 procmon·Wireshark로 행위를 관찰한다","C2·해시·레지스트리 키 등 IOC를 추출한다","행위를 MITRE ATT&CK에 매핑한다","리버싱 → IOC → 탐지 규칙의 사슬을 정리한다"],
q:[{t:"멀웨어를 분석할 때 반드시 지켜야 할 원칙은?",o:["빠르게","격리된 VM(네트워크 차단·스냅샷)에서만 실행하고, 절대 일반 시스템에서 돌리지 않는다","최신 백신","실시간 공유"],a:1,
  e:"멀웨어 동적 분석은 <b>격리 VM에서만**입니다 — 실행하면 감염되니까요. REMnux·FLARE·스냅샷·네트워크 시뮬(inetsim)로 안전하게 관찰하고, 끝나면 스냅샷을 복원합니다."},
   {t:"멀웨어 리버싱의 최종 산출물(방어 목적)은?",o:["소스 코드","침해지표(IOC) — C2 도메인·IP·파일 해시·레지스트리 키·YARA 시그니처로, 탐지 규칙을 만들어 조직을 보호","실행 파일","암호 키만"],a:1,
    e:"목적은 <b>IOC 추출**입니다(방어 코스). C2·해시·경로·뮤텍스·시그니처로 <b>탐지 규칙**을 만들어 조직을 보호합니다. 리버싱 → IOC → 방어의 사슬입니다."},
   {t:"리버싱이 '방어의 정점'이라 불릴 수 있는 이유는?",o:["느려서","자동 탐지가 못 잡는 새로운·표적형 멀웨어의 정체를 사람이 리버싱으로 밝혀, 복호화기·탐지 규칙을 만들어 방어하기 때문","공격이라서","합법이라"],a:1,
    e:"백신·EDR이 못 잡는 <b>신종·APT 멀웨어**는 <b>사람의 리버싱**으로 분석해야 정체가 드러납니다. 랜섬웨어 복호화기 제작, APT 백도어 탐지 규칙 배포 — <b>공격을 가장 깊이 이해한 방어**가 리버싱입니다."}]},

{d:110,t:"[rev 종합] 크랙미를 정복하라",g:"정찰·정적·동적·역산을 총동원해 보호가 걸린 바이너리를 풀어낸다.",boss:true,
c:"<p><b>ACT 5를 마쳤습니다.</b> 이제 <b>소스 없는 바이너리를 읽고**, 그 비밀을 풀어낼 수 있습니다. rev는 pwn의 짝이자 멀웨어 분석의 뿌리 — 바이너리를 지배하는 힘입니다.</p><p><b>rev 공략 표준 흐름 — 몸에 익힐 순서**</p><p><b>1. 정찰**(정적) — <code>file</code>(종류·언어·stripped)·<code>strings</code>(단서)·<code>objdump -T</code>(임포트). 언어 식별로 도구 결정.<br><b>2. 언어별 분기** — 파이썬/.NET/자바면 <b>소스 복원**(쉬움). 네이티브(C/Go/Rust)면 Ghidra.<br><b>3. 구조 파악**(정적) — Ghidra로 main·검증 로직을 디컴파일. 입력→변형→비교 흐름 파악. 이름 붙이며 이해.<br><b>4. 안티분석 확인** — 동작이 이상하면 안티디버깅·난독화·패킹 의심 → 우회/언팩.<br><b>5. 실제 값 확인**(동적) — gdb/ltrace로 비교 대상·계산 결과 확인. 비교 지점 가로채기.<br><b>6. 역산** — 변형의 역함수 적용, 또는 z3·브루트포스로 조건 만족 입력 계산 → 플래그.</p><p><b>지름길 우선순위**<br>① <code>strings</code>에 플래그가 있나? ② <code>ltrace</code>에 정답이 찍히나? ③ 비교 지점을 gdb로 가로챌 수 있나? ④ 안 되면 로직 역산. <b>쉬운 길부터** 시도해 시간을 아낍니다.</p><p><b>막히면?</b> 검증 로직을 정말 다 이해했나? target(비교 대상)을 정확히 추출했나? 변형을 역순으로 풀었나? 안티분석에 속고 있나(디버거에서만 다른 동작)? 동적으로 결과를 직접 볼 수 있나?</p><p><b>당신이 얻은 것**<br>· 소스 없는 바이너리를 <b>읽고 이해**하는 힘.<br>· 정적(Ghidra)·동적(gdb·ltrace) 분석의 조합.<br>· 안티분석 우회, 플래그 역산(z3), 언어별 리버싱.<br>· <b>멀웨어 분석의 핵심 기술** — 방어의 정점.<br>rev와 pwn(ACT 4)을 합치면 <b>바이너리 세계 전체**를 다룰 수 있습니다.</p><p><b>CTF의 봉우리들**<br>웹(1-2)·pwn(4)·rev(5)를 정복했습니다. 남은 것: <b>crypto**(암호)·<b>forensic/misc**. 각각 또 다른 세계입니다.</p><p><b>이제 당신은</b> 컴파일된 프로그램을 <b>거꾸로 읽어**그 비밀을 풀어냅니다. 공격자의 도구를 분석하고, 멀웨어의 정체를 밝히고, 보호를 우회합니다. 공격과 방어가 <b>바이너리 위에서 하나로**만나는 — 깊은 곳까지 온 것을 축하합니다.</p><p>수고하셨습니다.</p>",
term:"# rev 공략 — 이 순서를 외운다\n[1] file + strings + objdump -T   # 언어·단서·임포트\n[2] 언어별: .NET/py/java=소스복원 / native=Ghidra\n[3] Ghidra로 main·검증 로직 (입력->변형->비교)\n[4] 안티분석? -> 우회/언팩\n[5] gdb/ltrace로 비교 지점 가로채기\n[6] 역산 (역함수 / z3 / 브루트포스) -> 플래그",
lab:["크랙미(picoCTF·crackmes.one 등)를 하나 고른다","<code>file</code>·<code>strings</code>로 정찰하고 언어를 식별한다","쉬운 지름길(strings·ltrace·gdb 가로채기)을 먼저 시도한다","Ghidra로 검증 로직을 완전히 파악한다","안티분석이 있으면 우회한다","target을 추출해 역산 스크립트(또는 z3)로 플래그를 구한다","rev 전 과정 치트시트를 완성한다","rev가 멀웨어 분석·방어와 어떻게 이어지는지 정리한다"],
q:[{t:"rev 크랙미를 받았을 때 시간을 아끼는 올바른 접근 순서는?",o:["바로 어셈블리 정독","쉬운 지름길부터: strings에 플래그가? → ltrace에 정답이? → gdb로 비교 가로채기? → 안 되면 로직 역산","즉시 z3","무작정 브루트포스"],a:1,
  e:"<b>쉬운 길부터**입니다. strings·ltrace·비교 지점 가로채기로 <b>안 풀고도 답이 나오는**경우가 많습니다. 그게 안 될 때만 로직을 정밀 역산합니다. 지름길 우선이 시간을 아낍니다."},
   {t:"바이너리의 '언어 식별'이 rev 전략에서 첫 갈림길인 이유는?",o:["관습","파이썬·.NET·자바는 소스를 거의 복원(쉬움)하고 C·Go·Rust는 네이티브 분석(어려움)이라, 언어가 도구와 난이도를 결정하기 때문","실행 위해","암호화"],a:1,
    e:"언어가 <b>도구와 난이도**를 정합니다. 소스 복원 가능한 언어(파이썬·.NET·자바)는 몇 분, 네이티브(C·Rust)는 훨씬 오래 걸립니다. <code>file</code>로 먼저 식별해 맞는 길로 갑니다."},
   {k:"cmd",t:"rev 공략의 첫 명령 — 바이너리 <code>./crackme</code>의 종류·아키텍처·stripped 여부를 확인하는 명령을 입력하세요.",
    ph:"...","a":["file ./crackme","file crackme"],"re":["^file\\s+\\.?/?crackme$"],
    hint:"파일의 정체를 알려주는 도구입니다.",
    e:"<code>file ./crackme</code> — rev의 첫걸음입니다. ELF/PE·32/64비트·언어·stripped 여부가 <b>분석 전략 전체**를 정합니다. ACT 5 rev 완주를 축하합니다 — 바이너리를 읽고 쓰는 양쪽(pwn+rev)을 모두 갖췄습니다."}]}

]}
];
